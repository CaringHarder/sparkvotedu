---
status: diagnosed
phase: 07.1-predictive-auto-resolution
source: 07.1-01-SUMMARY.md, 07.1-02-SUMMARY.md, 07.1-03-SUMMARY.md, 07.1-04-SUMMARY.md
started: 2026-02-13T22:00:00Z
updated: 2026-02-14T05:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Predictive Resolution Mode in Bracket Creation
expected: When creating a predictive bracket, Step 2 shows three resolution mode options including "Predictive" (alongside Manual and Vote-based). Selecting it and proceeding to Step 3 shows it in the review summary.
result: pass

### 2. Prepare Results (Auto-Tabulation)
expected: After predictions close on an auto-mode predictive bracket, the teacher can click "Prepare Results" on the live dashboard. The system tabulates all student predictions as majority votes, filling in winners for every round automatically. A preview bracket diagram appears showing the computed winners.
result: pass

### 3. Override a Matchup Winner
expected: On the preview screen, teacher can click any matchup to open an override modal. The modal shows both entrants with their prediction vote counts. Selecting an alternate winner and confirming updates the bracket diagram, including downstream matchups that are affected by the change.
result: issue
reported: "Override modal for downstream matchups shows stale entrant data. After overriding a quarterfinal matchup (e.g. choosing Vaccines over Computer), the bracket diagram correctly updates the semifinal to show Printing Press vs Vaccines, but clicking that semifinal matchup to override shows the original entrants (Printing Press and Computer) instead of the updated ones (Printing Press and Vaccines). Same issue observed with other overrides (e.g. Printing Press instead of Lightbulb)."
severity: major

### 4. Release Results
expected: After reviewing the preview, teacher clicks "Release Results". A confirmation dialog appears. After confirming, the bracket transitions to revealing state and the first round of results becomes visible.
result: issue
reported: "Confirmation dialog appears correctly. However, after confirming, the first round is NOT revealed. Instead the button changes to 'Reveal Next Round' — teacher must click again to reveal what is actually round 1."
severity: minor

### 5. Progressive Round Reveal (Teacher)
expected: Teacher sees a "Reveal Next Round" button. Each click reveals the next round of winners on the bracket diagram. A 3-2-1 countdown animation plays between rounds (except Round 1 which shows immediately). After all rounds are revealed, the bracket shows as completed.
result: issue
reported: "Progressive reveal works for rounds after the first, but once the final round is revealed and bracket is completed, the Prediction Leaderboard and Prediction Stats sections render twice (duplicated below the bracket diagram)."
severity: minor

### 6. Presentation Mode
expected: Teacher presses the F key (or clicks a presentation button) to enter full-screen presentation mode with a dark overlay background. The view toggles between bracket diagram and leaderboard. Pressing F or Escape exits presentation mode.
result: issue
reported: "Presentation mode works during the reveal phase but becomes unavailable once the bracket is marked completed. No F key toggle or presentation button on the completed state."
severity: minor

### 7. Student Waiting State
expected: While the teacher is preparing/previewing results (before release), students see a waiting state with a spinner and message like "Your teacher is preparing results..." rather than a blank or broken page.
result: pass

### 8. Student Progressive Reveal
expected: When the teacher reveals rounds, students see the bracket fill in progressively in real time. Each matchup shows an accuracy overlay: green highlight for correct predictions, red highlight (with strikethrough) for incorrect predictions.
result: issue
reported: "Student view does live update progressively with accuracy overlay (green/red highlights), but the accuracy badge on each bracket card shows raw ASCII escape text '\\u27' instead of the intended icon/checkmark."
severity: minor

### 9. Student Animated Leaderboard
expected: Students see a leaderboard tab showing the top 10 scorers plus their own rank. As rounds are revealed and scores update, leaderboard entries animate smoothly to their new positions. Tied scores share the same rank number (dense ranking).
result: pass

### 10. Podium Celebration
expected: When all rounds are revealed (bracket complete), a podium celebration appears showing the top 3 students with gold/silver/bronze styling and confetti. The podium auto-dismisses after about 12 seconds.
result: issue
reported: "No podium celebration appears. Final round reveal just shows brackets and leaderboards and the bracket as closed."
severity: major

### 11. LiveDashboard Auto-Mode Routing
expected: When viewing an auto-mode predictive bracket on the live dashboard, SE-style controls (round tabs, action buttons) are hidden. Instead, the PredictiveBracket component renders with its own lifecycle controls appropriate to the current state.
result: pass

### 12. Reopen Predictions
expected: While in the previewing state (before releasing results), teacher can click a "Reopen Predictions" button to go back to predictions_open. This clears all computed winners and lets students modify their predictions.
result: issue
reported: "Reopen Predictions button works on teacher side. However, after teacher clicks 'Close Predictions', students can still navigate to 'Edit Results' and change/resubmit their predictions. The student UI does not enforce the closed state."
severity: major

## Summary

total: 12
passed: 5
issues: 7
pending: 0
skipped: 0

## Gaps

- truth: "Override modal for downstream matchups shows updated entrants after earlier override"
  status: failed
  reason: "User reported: Override modal for downstream matchups shows stale entrant data. After overriding a quarterfinal matchup, the bracket diagram correctly updates the semifinal visually, but clicking that semifinal matchup to override shows the original pre-override entrants instead of the updated ones."
  severity: major
  test: 3
  root_cause: "Two-part failure: (1) overrideWinner server action returns only {success: true}, discarding re-tabulated results from overrideMatchupWinnerDAL. (2) handleOverrideWinner tries to refresh via prepareResults() which is blocked by predictionStatus guard during previewing state. tabulationResults React state is never updated."
  artifacts:
    - path: "src/components/bracket/predictive-bracket.tsx"
      issue: "handleOverrideWinner (lines 137-149) uses prepareResults() which is blocked during previewing"
    - path: "src/actions/prediction.ts"
      issue: "overrideWinner action (lines 239-274) discards re-tabulated results, returns only {success: true}"
    - path: "src/lib/dal/prediction.ts"
      issue: "tabulateBracketPredictions status guard (lines 521-524) rejects during previewing; overrideMatchupWinnerDAL (lines 616-787) computes but discards updated results"
  missing:
    - "overrideMatchupWinnerDAL should return re-tabulated TabulationResult[]"
    - "overrideWinner server action should pass results back to client"
    - "handleOverrideWinner should use returned results directly instead of calling prepareResults()"
  debug_session: ".planning/debug/override-modal-stale-entrants.md"

- truth: "Release Results reveals the first round immediately"
  status: failed
  reason: "User reported: After confirming release, round 1 is not revealed. Button changes to 'Reveal Next Round' and teacher must click again to see round 1."
  severity: minor
  test: 4
  root_cause: "releaseResultsDAL sets predictionStatus to 'revealing' with revealedUpToRound: 0 but does NOT auto-reveal round 1. No chaining of revealRoundDAL at DAL, server action, or client level."
  artifacts:
    - path: "src/lib/dal/prediction.ts"
      issue: "releaseResultsDAL (line 831-838) sets revealedUpToRound: 0, does not call revealRoundDAL"
    - path: "src/actions/prediction.ts"
      issue: "releaseResults action (line 305-306) calls releaseResultsDAL only, no follow-up"
  missing:
    - "releaseResultsDAL should chain revealRoundDAL(bracketId, teacherId, 1) after setting revealing status"
  debug_session: ".planning/debug/predictive-release-no-auto-reveal.md"

- truth: "Completed bracket state renders leaderboard and stats sections once"
  status: failed
  reason: "User reported: Once final round is revealed and bracket completed, Prediction Leaderboard and Prediction Stats sections render twice (duplicated below bracket diagram)."
  severity: minor
  test: 5
  root_cause: "bracket-detail.tsx unconditionally renders PredictionLeaderboard for all predictive bracket types when active/completed (lines 160-167), while predictive-bracket.tsx TeacherPredictiveView completed state also renders its own PredictionLeaderboard (lines 476-481). Both fire simultaneously."
  artifacts:
    - path: "src/components/bracket/bracket-detail.tsx"
      issue: "Lines 153-168 render PredictionLeaderboard unconditionally for all predictive types — missing auto-mode guard"
    - path: "src/components/bracket/predictive-bracket.tsx"
      issue: "Lines 457-483 completed state renders own PredictionLeaderboard (correct for standalone live dashboard usage)"
  missing:
    - "Remove outer PredictionLeaderboard from bracket-detail.tsx or add isPredictiveAuto guard (like live-dashboard.tsx line 1055)"
  debug_session: ".planning/debug/predictive-leaderboard-duplicate.md"

- truth: "Presentation mode available on completed bracket state"
  status: failed
  reason: "User reported: Presentation mode works during reveal phase but becomes unavailable once bracket is marked completed."
  severity: minor
  test: 6
  root_cause: "TeacherPredictiveView completed branch (lines 457-483) is a stripped-down static view omitting: (1) presentationMode overlay guard, (2) Maximize2 toggle button, (3) bracket/leaderboard tab switcher. F key listener fires and toggles state but completed JSX ignores presentationMode."
  artifacts:
    - path: "src/components/bracket/predictive-bracket.tsx"
      issue: "Completed branch (lines 457-483) missing presentation mode overlay, toggle button, and tab switcher present in revealing branch (lines 282-451)"
  missing:
    - "Replicate presentation mode pattern from revealing branch into completed branch"
    - "Add Maximize2 toggle button to completed state header"
    - "Add bracket/leaderboard tab switcher to completed state"
  debug_session: ".planning/debug/presentation-mode-completed-state.md"

- truth: "Student accuracy badges render proper icons, not escape sequences"
  status: failed
  reason: "User reported: Accuracy badge on each bracket card shows raw ASCII escape text '\\u27' instead of intended icon/checkmark."
  severity: minor
  test: 8
  root_cause: "Double-escaped unicode in prediction-reveal.tsx: {'\\\\u2713'} (line 393) and {'\\\\u2717'} (line 417) render as literal text '\\u2713' instead of checkmark/cross characters. Double backslash prevents JavaScript from interpreting the unicode escape."
  artifacts:
    - path: "src/components/bracket/prediction-reveal.tsx"
      issue: "Lines 393 and 417: double-escaped unicode strings in SVG text elements"
  missing:
    - "Change {'\\\\u2713'} to {'\\u2713'} or literal checkmark character"
    - "Change {'\\\\u2717'} to {'\\u2717'} or literal cross character"
  debug_session: ".planning/debug/accuracy-badge-ascii-escape.md"

- truth: "Podium celebration appears when all rounds are revealed"
  status: failed
  reason: "User reported: No podium celebration appears. Final round reveal just shows brackets and leaderboards and bracket as closed."
  severity: major
  test: 10
  root_cause: "Two interconnected defects: (1) usePredictions initializes revealComplete=false and only sets true from live reveal_complete broadcast (line 90-93) — no initialization from bracket state on mount or polling fallback. (2) Cold-start page routing for completed brackets renders PredictiveBracket (read-only) instead of PredictionReveal, so podium component is never mounted."
  artifacts:
    - path: "src/hooks/use-predictions.ts"
      issue: "revealComplete only set from broadcast event, no cold-start initialization from predictionStatus/revealedUpToRound"
    - path: "src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx"
      issue: "Completed bracket routes to generic view with PredictiveBracket, not PredictionReveal"
    - path: "src/components/bracket/prediction-reveal.tsx"
      issue: "Podium trigger has no fallback for bracket.predictionStatus === 'completed'"
  missing:
    - "Initialize revealComplete from bracket state in usePredictions (or accept initialPredictionStatus param)"
    - "Route completed auto-mode predictive brackets to PredictionReveal on student page"
    - "Add fallback podium trigger for predictionStatus === 'completed' on mount"
  debug_session: ".planning/debug/podium-celebration-missing.md"

- truth: "Student UI enforces closed prediction state and blocks edits/resubmissions"
  status: failed
  reason: "User reported: After teacher clicks 'Close Predictions', students can still navigate to 'Edit Results' and change/resubmit their predictions."
  severity: major
  test: 12
  root_cause: "Three interacting deficiencies: (1) PredictiveBracket reads predictionStatus from stale initial bracket prop (never updated with real-time data). (2) 'Edit Predictions' button gated only by stale isPredictionsOpen. (3) handleSubmit silently swallows server-side rejection errors (no else branch). Server-side DAL correctly rejects late submissions."
  artifacts:
    - path: "src/components/bracket/predictive-bracket.tsx"
      issue: "predictionStatus read from stale bracket prop (line 36); Edit button not re-gated (lines 729, 909); error silently swallowed in handleSubmit (lines 674, 848)"
    - path: "src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx"
      issue: "Line 749 passes bracket to PredictiveBracket without overriding predictionStatus with real-time effectiveStatus"
  missing:
    - "Pass real-time effectiveStatus from PredictiveStudentView into PredictiveBracket as prop"
    - "PredictiveBracket should prefer real-time status over initial bracket prop"
    - "Add else branch in handleSubmit to show error and exit editing mode"
  debug_session: ".planning/debug/prediction-edit-after-close.md"
