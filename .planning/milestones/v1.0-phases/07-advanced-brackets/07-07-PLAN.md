---
phase: 07-advanced-brackets
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/lib/dal/bracket.ts
  - src/actions/bracket.ts
  - src/lib/bracket/advancement.ts
  - src/components/bracket/bracket-diagram.tsx
autonomous: true

must_haves:
  truths:
    - "Creating a bracket with non-power-of-two entrants auto-generates bye matchups"
    - "Bye matchups are auto-advanced during creation (present entrant immediately wins)"
    - "Bracket diagram shows grayed 'BYE' slot for bye matchups"
    - "Existing single-elimination bracket creation still works unchanged"
  artifacts:
    - path: "src/lib/dal/bracket.ts"
      provides: "Bye-aware bracket creation in createBracketDAL"
      contains: "generateMatchupsWithByes"
    - path: "src/components/bracket/bracket-diagram.tsx"
      provides: "BYE slot rendering in bracket diagram"
      contains: "BYE"
  key_links:
    - from: "src/lib/dal/bracket.ts"
      to: "src/lib/bracket/byes.ts"
      via: "Imports generateMatchupsWithByes for non-power-of-two sizes"
      pattern: "import.*generateMatchupsWithByes"
    - from: "src/lib/dal/bracket.ts"
      to: "src/lib/bracket/engine.ts"
      via: "Still uses generateMatchups for power-of-two sizes"
      pattern: "import.*generateMatchups"
---

<objective>
Integrate bye support into the existing bracket creation pipeline and render bye slots in the bracket diagram.

Purpose: Non-power-of-two brackets need automatic bye placement during creation, auto-advancement of bye matchups, and visual rendering of bye slots. This wires the bye engine (07-02) into the existing DAL and components.
Output: Updated bracket DAL with bye-aware creation, auto-advancing bye matchups, and bracket diagram with BYE slot rendering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-advanced-brackets/07-RESEARCH.md
@.planning/phases/07-advanced-brackets/07-02-SUMMARY.md
@src/lib/dal/bracket.ts
@src/lib/bracket/byes.ts
@src/lib/bracket/engine.ts
@src/lib/bracket/advancement.ts
@src/actions/bracket.ts
@src/components/bracket/bracket-diagram.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bracket DAL to support bye-aware creation and auto-advance byes</name>
  <files>src/lib/dal/bracket.ts, src/actions/bracket.ts</files>
  <action>
1. Update createBracketDAL in src/lib/dal/bracket.ts:
   - Import `generateMatchupsWithByes` and `calculateBracketSizeWithByes` from '@/lib/bracket/byes'
   - Determine if byes are needed: check if entrant count is a power of two (use bitwise: `(n & (n-1)) === 0`)
   - If NOT power of two:
     - Call `calculateBracketSizeWithByes(data.size)` to get bracketSize and numByes
     - Call `generateMatchupsWithByes(data.size)` to get matchup seeds with isBye flags
     - The `size` field stored on the Bracket should remain the actual entrant count (not the bracket size)
     - Store the effective bracket size as `maxEntrants` field (so the diagram knows the full bracket structure)
   - If power of two:
     - Use existing `generateMatchups(data.size)` (no change)
   - The entrant count validation now checks `entrants.length === data.size` (actual entrants, not bracket size including byes)

2. Update createMatchupsInTransaction to handle byes:
   - Accept an optional `isBye` flag per matchup seed
   - When creating matchup records, set `isBye: seed.isBye ?? false` in the data
   - After creating ALL matchups, auto-advance bye matchups:
     - Find matchups where isBye=true AND one entrant is set (the other is null)
     - For each bye matchup: set winnerId to the present entrant, set status to 'decided'
     - Propagate the bye winner to the next matchup using getSlotForPosition logic
   - This must happen WITHIN the same transaction

3. Update updateBracketEntrantsDAL to work with byes:
   - When regenerating matchups for a draft bracket with non-power-of-two entrants, use generateMatchupsWithByes
   - Auto-advance bye matchups after regeneration (same as creation flow)

4. Update createBracket action in src/actions/bracket.ts:
   - Add bracketType from the parsed input to the DAL call:
     ```typescript
     const bracketType = bracketData.bracketType ?? 'single_elimination'
     ```
   - Pass bracketType to createBracketDAL (add bracketType to the data parameter)
   - Update createBracketDAL to accept and store bracketType
   - Pass type-specific options (roundRobinPacing, predictiveMode, etc.) through to the DAL
  </action>
  <verify>
  Create a 5-entrant bracket via the createBracket action. Verify:
  - 8-team bracket structure is created (7 matchups)
  - 3 matchups in round 1 have isBye=true
  - Those 3 bye matchups have winnerId set and status='decided'
  - The bye winners are propagated to round 2 matchups
  Run existing bracket tests: `npx vitest run src/lib/bracket/__tests__/`
  </verify>
  <done>Non-power-of-two bracket creation generates correct bye structure, auto-advances bye matchups, and propagates winners. Power-of-two brackets still work unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render BYE slots in bracket diagram</name>
  <files>src/components/bracket/bracket-diagram.tsx</files>
  <action>
Update bracket-diagram.tsx to handle bye matchups:

1. Detect bye matchups: a matchup where `isBye === true` (from the new MatchupData field).

2. For bye matchup rendering:
   - Show the present entrant name in the filled slot (normal styling)
   - Show "BYE" in the empty slot with grayed-out styling:
     - Text: "BYE" in italic, text-muted-foreground / gray-400
     - Background: bg-muted / gray-100 dark:gray-800 (dimmed compared to regular slots)
     - No hover effects on the BYE slot
   - The matchup connector lines should still render but can be dimmed/dashed for bye matchups

3. For decided bye matchups (which they all should be after creation):
   - Show the winner indicator (existing green highlight/checkmark) on the present entrant
   - The "BYE" slot should remain grayed

4. Do NOT change the layout algorithm. Bye matchups occupy the same space as regular matchups in the bracket structure. The visual difference is purely in slot rendering.

5. Wrap the bracket diagram with BracketZoomWrapper when the bracket has 32+ entrants:
   - Import BracketZoomWrapper from '@/components/bracket/bracket-zoom-wrapper'
   - Conditionally wrap: if bracket size >= 32, wrap with BracketZoomWrapper
   - For 64+ entrants, set initialScale={0.75}
   - For < 32 entrants, render diagram without zoom wrapper (unchanged behavior)
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Visually verify by viewing a bracket with byes (e.g., 5-entrant bracket): bye slots show "BYE" in gray, winner indicators on auto-advanced entrants.</verify>
  <done>Bracket diagram renders BYE slots with grayed styling, auto-advanced bye matchups show winner indicators. Large brackets (32+) are wrapped in zoom container.</done>
</task>

</tasks>

<verification>
1. Create a 5-entrant bracket: 3 byes auto-advanced, BYE slots visible
2. Create a 4-entrant bracket: no byes, works exactly as before
3. Create a 7-entrant bracket: 1 bye auto-advanced
4. `npx vitest run src/lib/bracket/__tests__/` -- existing tests pass
5. `npx tsc --noEmit` -- no new type errors
</verification>

<success_criteria>
- Non-power-of-two brackets create with correct bye structure
- Bye matchups auto-advance during creation (no manual step needed)
- Bracket diagram shows "BYE" text in grayed slots
- Power-of-two brackets unchanged
- Large brackets (32+) have zoom controls
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-07-SUMMARY.md`
</output>
