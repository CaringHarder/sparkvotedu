---
phase: 07-advanced-brackets
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bracket/double-elim-diagram.tsx
  - src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Student can click on DE bracket entrant in Winners tab to cast a vote"
    - "Student can click on DE bracket entrant in Losers tab to cast a vote"
    - "Student can click on Grand Finals entrant to cast a vote"
    - "Student sees DE bracket update in real-time when teacher opens/closes voting (no hard refresh)"
  artifacts:
    - path: "src/components/bracket/double-elim-diagram.tsx"
      provides: "Vote-capable DE diagram with onEntrantClick/votedEntrantIds prop threading"
      contains: "onEntrantClick"
    - path: "src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx"
      provides: "DE student voting with real-time subscription"
      contains: "useRealtimeBracket"
  key_links:
    - from: "student bracket page (DE path)"
      to: "DoubleElimDiagram"
      via: "onEntrantClick + votedEntrantIds props"
      pattern: "onEntrantClick.*handleEntrantClick"
    - from: "DoubleElimDiagram"
      to: "BracketDiagram"
      via: "prop forwarding to child BracketDiagram in Winners/Losers tabs"
      pattern: "onEntrantClick.*votedEntrantIds"
    - from: "student bracket page (DE path)"
      to: "useRealtimeBracket"
      via: "hook subscription for live matchup updates"
      pattern: "useRealtimeBracket"
---

<objective>
Enable student voting on Double Elimination brackets by threading vote interaction props through DoubleElimDiagram and adding real-time subscription to the student page.

Purpose: Tests 5 and 6 fail because DE renders as read-only on the student page -- DoubleElimDiagram lacks voting props and the student page has no real-time subscription for DE brackets. This is Root Causes RC1 (DE read-only), RC1a (DoubleElimDiagram missing props), RC1b (GrandFinalsCard read-only), and RC5 (no real-time for non-SE types on student page, DE portion).

Output: Students can click entrants to vote in DE brackets (Winners, Losers, Grand Finals tabs) with live updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/student-voting-broken-all-types.md

Key code references:
@src/components/bracket/double-elim-diagram.tsx
@src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
@src/components/student/advanced-voting-view.tsx (reference pattern for voting wrapper)
@src/hooks/use-realtime-bracket.ts
@src/components/bracket/bracket-diagram.tsx (already accepts onEntrantClick/votedEntrantIds)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voting props to DoubleElimDiagram and GrandFinalsCard</name>
  <files>src/components/bracket/double-elim-diagram.tsx</files>
  <action>
    1. Add optional voting props to DoubleElimDiagramProps interface:
       - onEntrantClick?: (matchupId: string, entrantId: string) => void
       - votedEntrantIds?: Record<string, string | null>
       These match the existing BracketDiagram prop signatures.

    2. Thread these props to every BracketDiagram rendered in the component:
       - Winners tab BracketDiagram (both the zoom-wrapped and non-zoom-wrapped versions, ~4 BracketDiagram instances total across winners and losers tabs)
       - Losers tab BracketDiagram (same pattern)
       Pass: onEntrantClick={onEntrantClick} votedEntrantIds={votedEntrantIds}

    3. Add voting interaction to GrandFinalsCard:
       - Add props: onEntrantClick, votedEntrantIds, matchup
       - When matchup.status === 'voting' AND onEntrantClick exists:
         - Make each entrant div clickable (onClick => onEntrantClick(matchup.id, entrant.id))
         - Add cursor-pointer and hover:border-primary/50 styling when clickable
         - Show check mark or primary color highlight when votedEntrantIds[matchup.id] === entrant.id
       - When matchup.status !== 'voting' or no onEntrantClick: keep current read-only rendering

    4. Pass onEntrantClick and votedEntrantIds to GrandFinalsCard from the grand_finals tab section.

    IMPORTANT: Do NOT change the isTeacher prop or remove any existing functionality. All additions are additive optional props.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Verify no type errors. DoubleElimDiagram still works with only the original 4 props (bracket, entrants, matchups, isTeacher).
  </verify>
  <done>DoubleElimDiagram accepts onEntrantClick and votedEntrantIds, threads them to all child BracketDiagrams and GrandFinalsCard. Existing call sites (live-dashboard, bracket-detail, completed student view) compile unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DE voting and real-time subscription on student bracket page</name>
  <files>src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx</files>
  <action>
    The student bracket page currently renders DoubleElimDiagram as read-only for DE brackets (line 348-363). Transform the DE path into a voting-capable view following the pattern established by AdvancedVotingView.

    1. Import useRealtimeBracket and castVote at the top of the file:
       import { useRealtimeBracket } from '@/hooks/use-realtime-bracket'
       import { castVote } from '@/actions/vote'

    2. For the 'ready' state DE bracket path (line 348-363), refactor to use a self-contained DEVotingSection inline component (or extract to a separate component within the same file). This component should:

       a. Call useRealtimeBracket(bracket.id) to subscribe to real-time matchup updates.
       b. Compute currentMatchups from realtimeMatchups ?? bracket.matchups (same pattern as AdvancedVotingView line 44).
       c. Maintain a votes state initialized from initialVotes (useState<Record<string, string | null>>).
       d. Create a handleEntrantClick callback that:
          - Optimistically updates votes state: setVotes(prev => ({...prev, [matchupId]: entrantId}))
          - Calls castVote({ matchupId, participantId, entrantId }) in a startTransition
          - Reverts on error
       e. Render DoubleElimDiagram with the NEW voting props:
          <DoubleElimDiagram
            bracket={bracket}
            entrants={bracket.entrants}
            matchups={currentMatchups}  // from real-time
            isTeacher={false}
            onEntrantClick={handleEntrantClick}
            votedEntrantIds={votes}
          />
       f. Show a voting status indicator (e.g., "Tap to vote" badge and voted count) like AdvancedVotingView does.

    3. Also add useRealtimeBracket to the RR and predictive paths for real-time updates:
       - Extract wrapper components (RRLiveView, PredictiveLiveView) that call useRealtimeBracket(bracket.id)
       - Use the returned realtimeMatchups to override bracket.matchups
       - For the RR wrapper (RRLiveView), ALSO wire voting support:
         a. Import castVote from '@/actions/vote'
         b. Maintain a votedMatchups state: useState<Record<string, string>>({})
         c. Create a handleStudentVote callback: (matchupId: string, entrantId: string) => {
              setVotedMatchups(prev => ({...prev, [matchupId]: entrantId}))
              castVote({ matchupId, participantId, entrantId })
            }
         d. Pass onStudentVote={handleStudentVote} and votedMatchups={votedMatchups} to RoundRobinMatchups
            (These props will be added by Plan 07-18 -- if executing before 07-18, pass them anyway; they'll be ignored until 07-18 adds them to the interface)
       - This addresses RC5 for all non-SE types AND wires RR student voting to castVote

    NOTE: The completed state (line 244-289) should remain read-only -- no voting on completed brackets. Only the 'ready' state paths need real-time and voting.

    IMPORTANT: useRealtimeBracket is a hook and must be called unconditionally in a component. If needed, create small wrapper components (DEVotingView, RRLiveView, etc.) to isolate the hook call within each bracket type branch. Do NOT call hooks conditionally.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Verify no type errors.
    Manually verify: The DE branch in 'ready' state now renders DoubleElimDiagram with onEntrantClick and votedEntrantIds.
    Verify useRealtimeBracket is called in all non-SE bracket type paths (DE, RR, predictive).
  </verify>
  <done>DE brackets on student page are fully interactive with voting. All non-SE bracket types have real-time subscriptions. Students see matchup status changes without hard refresh.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with no errors
- DoubleElimDiagram renders identically when called without new props (backwards compatible)
- Student page DE path renders DoubleElimDiagram with onEntrantClick and votedEntrantIds
- useRealtimeBracket is imported and called for DE, RR, and predictive bracket types on student page
- GrandFinalsCard shows clickable entrants when status is 'voting' and onEntrantClick is provided
</verification>

<success_criteria>
Students viewing a DE bracket can click entrants to vote on Winners, Losers, and Grand Finals matchups. The bracket updates in real-time when the teacher opens/closes voting. Tests 5 and 6 pass for DE voting interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-17-SUMMARY.md`
</output>
