---
phase: 07-advanced-brackets
plan: 34
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/teacher/live-dashboard.tsx
  - src/components/bracket/bracket-detail.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "64-entrant brackets render as QuadrantBracketLayout on teacher live-dashboard"
    - "64-entrant brackets render as QuadrantBracketLayout on teacher bracket-detail page"
    - "Fallback uses bracket.size (not 0) when maxEntrants is null"
  artifacts:
    - path: "src/components/teacher/live-dashboard.tsx"
      provides: "Correct 64+ detection using bracket.size fallback"
      contains: "bracket.size"
    - path: "src/components/bracket/bracket-detail.tsx"
      provides: "Correct 64+ detection using bracket.size fallback"
      contains: "bracket.size"
---

<objective>
Fix teacher page 64-entrant quadrant layout by using correct fallback value for size detection.

Purpose: The live-dashboard uses `(bracket.maxEntrants ?? 0) >= 64` which fails when maxEntrants is null because `0 >= 64` is always false. The bracket.size field is always present and should be used as fallback.
Output: Teacher pages render 64-entrant brackets using QuadrantBracketLayout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/teacher-quadrant-layout-r6.md
@src/components/teacher/live-dashboard.tsx
@src/components/bracket/bracket-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 64+ detection fallback in live-dashboard.tsx</name>
  <files>src/components/teacher/live-dashboard.tsx</files>
  <action>
Find the QuadrantBracketLayout conditional around line 1026 that looks like:

```tsx
{(bracket.maxEntrants ?? 0) >= 64 ? (
  <QuadrantBracketLayout
```

Change the fallback from `0` to `bracket.size`:

```tsx
{(bracket.maxEntrants ?? bracket.size) >= 64 ? (
  <QuadrantBracketLayout
```

Also update the `bracketSize` prop passed to QuadrantBracketLayout to use the same fallback:

```tsx
bracketSize={bracket.maxEntrants ?? bracket.size}
```
  </action>
  <verify>
Search for `bracket.maxEntrants ?? bracket.size` in live-dashboard.tsx. Verify no `?? 0` remains in the quadrant conditional.
  </verify>
  <done>Live-dashboard uses bracket.size fallback for 64+ detection.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 64+ detection fallback in bracket-detail.tsx</name>
  <files>src/components/bracket/bracket-detail.tsx</files>
  <action>
Find the QuadrantBracketLayout conditional around line 219 that uses `bracket.entrants.length` as fallback:

```tsx
{(bracket.maxEntrants ?? bracket.entrants.length) >= 64 ? (
```

Change to use `bracket.size` for consistency (bracket.size is always present, entrants.length may not be loaded):

```tsx
{(bracket.maxEntrants ?? bracket.size) >= 64 ? (
```

Also update the `bracketSize` prop:

```tsx
bracketSize={bracket.maxEntrants ?? bracket.size}
```
  </action>
  <verify>
Search for `bracket.maxEntrants ?? bracket.size` in bracket-detail.tsx. Verify consistent fallback pattern across both teacher files.
  </verify>
  <done>Bracket-detail uses bracket.size fallback for 64+ detection.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. live-dashboard.tsx uses `bracket.maxEntrants ?? bracket.size` (not `?? 0`)
3. bracket-detail.tsx uses `bracket.maxEntrants ?? bracket.size` (not `?? entrants.length`)
4. Both teacher pages render QuadrantBracketLayout for 64-entrant brackets
</verification>

<success_criteria>
- 64-entrant brackets display as 2x2 quadrant grid on teacher live-dashboard
- 64-entrant brackets display as 2x2 quadrant grid on teacher bracket-detail page
- Consistent with student page behavior (which already works)
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-34-SUMMARY.md`
</output>
