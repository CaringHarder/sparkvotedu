---
phase: 07-advanced-brackets
plan: 13
type: execute
wave: 5
depends_on: ["07-09", "07-10", "07-12"]
files_modified:
  - src/actions/bracket.ts
  - src/lib/dal/bracket.ts
  - src/app/(dashboard)/brackets/[bracketId]/page.tsx
autonomous: false

must_haves:
  truths:
    - "All bracket types (single-elim, double-elim, round-robin, predictive) can be created and viewed"
    - "Non-power-of-two sizes work for single-elim, double-elim, and predictive brackets"
    - "Feature gating enforces tier limits: free=single-elim only, pro=+double-elim +round-robin, pro_plus=+predictive"
    - "Bracket detail page routes to correct component by bracketType"
    - "Existing single-elimination brackets and voting flow are not broken"
  artifacts:
    - path: "src/actions/bracket.ts"
      provides: "Unified bracket creation routing by type"
      contains: "bracketType"
    - path: "src/app/(dashboard)/brackets/[bracketId]/page.tsx"
      provides: "Type-aware bracket detail page"
      contains: "double_elimination"
  key_links:
    - from: "src/actions/bracket.ts"
      to: "src/lib/dal/bracket.ts"
      via: "Routes to correct DAL function by bracketType"
      pattern: "switch.*bracketType"
    - from: "src/app/(dashboard)/brackets/[bracketId]/page.tsx"
      to: "src/components/bracket/double-elim-diagram.tsx"
      via: "Conditional rendering by bracketType"
      pattern: "bracketType.*DoubleElimDiagram"
---

<objective>
Integration verification: ensure all bracket types work end-to-end, feature gating is enforced, and no regressions to existing functionality.

Purpose: Phase 7 adds significant complexity (4 bracket types, byes, prediction lifecycle). This plan verifies everything works together and fixes any integration gaps.
Output: Verified end-to-end flows for all bracket types with comprehensive human verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-advanced-brackets/07-CONTEXT.md
@.planning/phases/07-advanced-brackets/07-09-SUMMARY.md
@.planning/phases/07-advanced-brackets/07-10-SUMMARY.md
@.planning/phases/07-advanced-brackets/07-12-SUMMARY.md
@src/actions/bracket.ts
@src/lib/dal/bracket.ts
@src/lib/gates/features.ts
@src/lib/gates/tiers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix bracket creation routing and feature gating</name>
  <files>src/actions/bracket.ts, src/lib/dal/bracket.ts</files>
  <action>
Review and fix any integration gaps in the bracket creation pipeline:

1. Verify createBracket action in bracket.ts correctly:
   - Extracts bracketType from validated input
   - Calls canUseBracketType(tier, bracketType) for feature gating
   - Routes to correct DAL function based on bracketType:
     - 'single_elimination': createBracketDAL (with byes if non-power-of-two)
     - 'double_elimination': createBracketDAL or createDoubleElimBracketDAL
     - 'round_robin': createRoundRobinBracketDAL
     - 'predictive': createBracketDAL (uses single-elim structure + prediction overlay)
   - Passes type-specific options through to the DAL

2. Verify the bracket detail page at src/app/(dashboard)/brackets/[bracketId]/page.tsx correctly routes by type:
   - single_elimination: BracketDiagram (existing)
   - double_elimination: DoubleElimDiagram (from 07-09)
   - round_robin: RoundRobinStandings + RoundRobinMatchups (from 07-10)
   - predictive: PredictiveBracket + PredictionLeaderboard (from 07-11, 07-12)

3. Verify the bracket list page shows bracket type badge on each card:
   - Show bracketType as a small badge on BracketCard (e.g., "Double Elim", "Round Robin", "Predictive")
   - Single elimination doesn't need a badge (default)

4. Verify entrant count validation works for each type:
   - Single-elim: 3-128 (with byes for non-power-of-two)
   - Double-elim: 3-64 (with byes), or up to 72 with play-in
   - Round-robin: 3-8
   - Predictive: 3-128 (with byes)

5. Fix any type errors or missing imports from integration.

6. Run the full test suite to catch regressions:
   - `npx vitest run` -- all existing tests pass
   - `npx tsc --noEmit` -- no type errors
  </action>
  <verify>
  Run `npx vitest run` -- all tests pass.
  Run `npx tsc --noEmit` -- no type errors.
  Create one bracket of each type via the UI and verify it persists correctly.
  </verify>
  <done>All bracket types route correctly through creation and detail view. Feature gating enforced. Entrant count validation correct per type. No regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 7: four bracket types (single-elim, double-elim, round-robin, predictive), non-power-of-two support with byes, prediction lifecycle with leaderboard, and pan/zoom for large brackets.</what-built>
  <how-to-verify>
  Run `npm run dev` and verify each success criterion:

  1. SINGLE-ELIMINATION with BYES:
     a. Create a single-elim bracket with 5 entrants
     b. Verify: 8-team bracket renders with 3 BYE slots grayed out
     c. Verify: bye matchups show auto-advanced winners
     d. Verify: existing 4/8/16 brackets still work normally

  2. DOUBLE-ELIMINATION:
     a. Create a double-elim bracket with 8 entrants
     b. Verify: Tabbed layout (Winners/Losers/Grand Finals/Overview)
     c. Verify: Winners tab shows seeded first round
     d. Advance a WB matchup: verify loser appears in LB
     e. Advance through to grand finals: verify bracket completion

  3. ROUND-ROBIN:
     a. Create a round-robin bracket with 4 entrants
     b. Verify: 3 rounds, 6 matchups created
     c. Record results: verify standings update (W-L-T, points, rank)
     d. Verify: round-by-round pacing shows one round at a time

  4. PREDICTIVE:
     a. Create a predictive bracket with 8 entrants
     b. Open predictions
     c. Submit predictions as student (all 7 matchups)
     d. Close predictions, resolve matchups
     e. Verify: leaderboard shows scores with per-round breakdown (teacher view)
     f. Verify: student sees rank + score only

  5. FEATURE GATING:
     a. As free tier: verify only single-elim is available
     b. Verify double-elim/round-robin/predictive show upgrade prompt

  6. NO REGRESSIONS:
     a. Create a standard single-elim bracket (4 entrants)
     b. Vote on matchups, advance bracket
     c. Verify: existing voting and advancement flow unchanged
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All 4 bracket types create and render correctly
2. Non-power-of-two brackets have correct byes
3. Feature gating enforces tier limits
4. Existing single-elim flow unchanged
5. All tests pass
6. No type errors
</verification>

<success_criteria>
- All 5 phase success criteria from ROADMAP.md verified:
  1. Double-elim bracket with winners/losers/grand finals rendered correctly
  2. Round-robin bracket with standings displayed
  3. Predictive bracket with predictions and scored leaderboard
  4. Non-power-of-two brackets with automatic byes
  5. Predictive leaderboard shows scoring breakdown
- No regressions to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-13-SUMMARY.md`
</output>
