---
phase: 17-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/dal/admin.ts
  - src/app/proxy.ts
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/teachers/page.tsx
  - scripts/promote-admin.ts
autonomous: true

must_haves:
  truths:
    - "Admin user with role=admin can access /admin routes"
    - "Non-admin users hitting /admin are silently redirected to /dashboard"
    - "Admin layout has its own sidebar with Overview and Teachers nav items"
    - "Teacher model has a role column defaulting to 'teacher'"
    - "A seed script can promote an existing teacher email to admin role"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Teacher model with role column"
      contains: "role"
    - path: "src/lib/dal/admin.ts"
      provides: "Admin authentication and authorization functions"
      exports: ["getAuthenticatedAdmin"]
    - path: "src/app/proxy.ts"
      provides: "Admin route protection via redirect"
      contains: "/admin"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin-specific layout with sidebar navigation"
      min_lines: 30
    - path: "scripts/promote-admin.ts"
      provides: "CLI script to promote teacher to admin"
      min_lines: 10
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "src/lib/dal/admin.ts"
      via: "getAuthenticatedAdmin() call"
      pattern: "getAuthenticatedAdmin"
    - from: "src/app/proxy.ts"
      to: "/admin routes"
      via: "redirect check for admin role"
      pattern: "admin"
---

<objective>
Add admin role to the database, create admin authentication, build the admin dashboard shell with its own layout, and set up a seed script for bootstrapping the first admin user.

Purpose: Establishes the protected admin interface that all subsequent admin features build on. Without this, there is no admin access layer.
Output: Admin route group with layout, role-based DB column, proxy protection, and admin promotion script.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-admin-panel/17-CONTEXT.md

@prisma/schema.prisma
@src/lib/dal/auth.ts
@src/app/proxy.ts
@src/app/(dashboard)/layout.tsx
@src/components/dashboard/sidebar-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role column to Teacher model and create admin DAL</name>
  <files>
    prisma/schema.prisma
    src/lib/dal/admin.ts
    scripts/promote-admin.ts
  </files>
  <action>
1. In prisma/schema.prisma, add a `role` field to the Teacher model:
   ```
   role  String  @default("teacher")
   ```
   Place it after `name` and before `avatarUrl`. This supports "teacher" (default) and "admin" values. No enum -- just a string for simplicity and future extensibility.

2. Run `npx prisma db push` to apply the schema change (no migration file needed -- project uses db push).

3. Create `src/lib/dal/admin.ts` with:
   - `getAuthenticatedAdmin()` function that:
     a. Calls `getAuthenticatedTeacher()` from `@/lib/dal/auth`
     b. If null, returns null (not authenticated)
     c. Checks `teacher.role === 'admin'`; if not admin, returns null
     d. Returns the teacher record if admin
   - `isAdmin(teacherId: string)` function that queries Prisma for the teacher's role and returns boolean
   - Export both functions

4. Create `scripts/promote-admin.ts` -- a standalone Node script that:
   - Takes an email as CLI argument: `npx tsx scripts/promote-admin.ts user@example.com`
   - Uses Prisma client to find teacher by email
   - Updates their role to "admin"
   - Prints success/failure message
   - Include dotenv loading: `import 'dotenv/config'` at top for DATABASE_URL
  </action>
  <verify>
    - `npx prisma validate` passes
    - `npx tsx scripts/promote-admin.ts --help` runs without error (or prints usage)
    - TypeScript compiles: `npx tsc --noEmit src/lib/dal/admin.ts`
  </verify>
  <done>Teacher model has role column, admin DAL functions exist, promote script is ready to use</done>
</task>

<task type="auto">
  <name>Task 2: Update proxy for admin route protection</name>
  <files>
    src/app/proxy.ts
  </files>
  <action>
Update `src/app/proxy.ts` to handle admin routes:

1. Add admin route detection: `if (pathname.startsWith('/admin'))` check.

2. For /admin routes when user IS authenticated:
   - Need to check if the user is an admin. Since the proxy only has JWT claims (not Prisma data), use one of these approaches:
     a. PREFERRED: Import prisma and check the teacher's role directly in the proxy. The proxy already creates a Supabase client and gets claims. Add a lightweight Prisma query: `prisma.teacher.findUnique({ where: { supabaseAuthId: claims.sub }, select: { role: true } })`.
     b. If the user's role is NOT "admin", redirect to `/dashboard` (silent redirect per user decision -- no 403 page).
     c. If the user IS admin, allow the request through.

3. For /admin routes when user is NOT authenticated:
   - Redirect to `/login` (already handled by existing auth redirect logic, but ensure /admin is NOT in PUBLIC_PAGES).

Keep the proxy fast -- only do the Prisma role check when the path starts with `/admin`.
  </action>
  <verify>
    - `npx tsc --noEmit src/app/proxy.ts` compiles without errors
    - Non-admin users accessing /admin should be redirected to /dashboard
  </verify>
  <done>Proxy silently redirects non-admin users from /admin to /dashboard; admin users pass through</done>
</task>

<task type="auto">
  <name>Task 3: Create admin layout and page shells</name>
  <files>
    src/app/(admin)/layout.tsx
    src/app/(admin)/admin/page.tsx
    src/app/(admin)/admin/teachers/page.tsx
  </files>
  <action>
1. Create `src/app/(admin)/layout.tsx` -- Admin-specific layout, distinct from teacher dashboard:
   - Server component (no "use client")
   - Call `getAuthenticatedAdmin()` at top; if null, redirect to `/dashboard`
   - Structure: branded header (similar to dashboard but with "Admin" badge), sidebar, main content area
   - Header: SparkVotEDU logo + "Admin" label, sign-out button
   - Sidebar with two nav items using lucide icons:
     - "Overview" -> `/admin` (LayoutDashboard icon)
     - "Teachers" -> `/admin/teachers` (Users icon)
   - Use a client component `AdminSidebarNav` for active-link highlighting (create inline or as separate component within the layout file for simplicity)
   - Sidebar width: w-56 (same as teacher dashboard for consistency)
   - Use the same Tailwind patterns as the teacher dashboard layout (brand accent line at top, border styling, etc.) but differentiate with a subtle admin indicator (e.g., amber accent instead of blue, or a small "Admin" badge in the header)

2. Create `src/app/(admin)/admin/page.tsx` -- Overview page shell:
   - Server component
   - Page title: "Admin Overview"
   - Placeholder text: "Stat bar and teacher list coming in next plan."
   - This will be replaced in 17-02 with the actual stat bar + teacher list

3. Create `src/app/(admin)/admin/teachers/page.tsx` -- Teachers page shell:
   - Server component
   - Page title: "Teachers"
   - Placeholder text: "Teacher management coming in next plan."
   - This will be replaced in 17-02 with the dedicated teacher list

Note: The (admin) route group keeps admin pages in a separate layout from the (dashboard) group. The /admin URL prefix provides the namespace.
  </action>
  <verify>
    - `npx tsc --noEmit` passes for all new files
    - Admin layout renders with sidebar containing Overview and Teachers links
    - /admin and /admin/teachers pages load with placeholder content
  </verify>
  <done>Admin layout with distinct sidebar exists, /admin and /admin/teachers render as placeholders, admin auth gate enforced at layout level</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes (schema is valid)
2. Admin DAL functions compile and follow established DAL patterns
3. Proxy correctly handles /admin routes (redirects non-admin to /dashboard)
4. Admin layout loads for admin users with correct sidebar navigation
5. Non-admin users are silently redirected away from /admin
6. Seed script runs: `npx tsx scripts/promote-admin.ts test@example.com`
</verification>

<success_criteria>
- Teacher model has `role` column with default "teacher"
- `getAuthenticatedAdmin()` function gates admin access
- Proxy redirects non-admin users from /admin to /dashboard
- Admin layout has its own sidebar with Overview and Teachers nav items
- Admin shell pages exist at /admin and /admin/teachers
- `scripts/promote-admin.ts` can promote a teacher email to admin
</success_criteria>

<output>
After completion, create `.planning/phases/17-admin-panel/17-01-SUMMARY.md`
</output>
