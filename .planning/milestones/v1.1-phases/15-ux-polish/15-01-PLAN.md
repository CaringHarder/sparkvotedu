---
phase: 15-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bracket/bracket-form.tsx
  - src/app/(dashboard)/brackets/new/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visual bracket placement matchup grid uses the full available page width, not a narrow 2xl column"
    - "List-based entrant reorder and other wizard steps remain centered at max-w-2xl"
    - "Entrant pool sidebar is visible and usable alongside the full-width matchup grid"
    - "Step indicator (1-2-3) and navigation buttons remain accessible in visual placement mode"
  artifacts:
    - path: "src/components/bracket/bracket-form.tsx"
      provides: "Bracket creation wizard with full-width visual placement step"
      contains: "max-w-2xl"
    - path: "src/app/(dashboard)/brackets/new/page.tsx"
      provides: "Create bracket page without hardcoded width constraint"
  key_links:
    - from: "src/components/bracket/bracket-form.tsx"
      to: "src/components/bracket/visual-placement/placement-bracket.tsx"
      via: "PlacementBracket component render in step 2"
      pattern: "PlacementBracket"
---

<objective>
Move visual bracket placement to use full page width during bracket creation step 2.

Purpose: The visual placement interface is currently constrained inside a `max-w-2xl` container (672px), which forces the entrant pool sidebar and matchup grid into a very narrow space. For brackets with 16+ entrants, this makes placement awkward. The placement step should expand to use the full dashboard content area width while keeping other wizard steps (info, review) at the readable max-w-2xl width.

Output: Updated bracket creation wizard that dynamically removes width constraint when visual placement mode is active in step 2.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/bracket/bracket-form.tsx
@src/components/bracket/visual-placement/placement-bracket.tsx
@src/app/(dashboard)/brackets/new/page.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make bracket creation page and form width-responsive to visual placement mode</name>
  <files>
    src/app/(dashboard)/brackets/new/page.tsx
    src/components/bracket/bracket-form.tsx
  </files>
  <action>
The current bracket form wraps everything in `<div className="mx-auto max-w-2xl">` at line 335 of bracket-form.tsx. The page.tsx also wraps in `<div className="space-y-6">`. The dashboard layout provides `<main className="flex-1 overflow-y-auto p-4 sm:p-6">` as the content area.

**Changes to bracket-form.tsx:**

1. The outermost wrapper `<div className="mx-auto max-w-2xl">` needs to become dynamic. When `step === 2` AND `placementMode === 'visual'`, remove the `max-w-2xl` constraint so the visual placement grid can use full available width.

2. Compute a dynamic container class:
   ```tsx
   const isFullWidth = step === 2 && placementMode === 'visual'
   ```

3. Change the outermost div to:
   ```tsx
   <div className={isFullWidth ? 'mx-auto' : 'mx-auto max-w-2xl'}>
   ```

4. The step indicator bar (the 1-2-3 circles) should remain centered and readable. Wrap it in its own `max-w-2xl mx-auto` container so it stays centered even when the outer container is full-width.

5. In Step 2, when `placementMode === 'visual'`, the Card wrapper that currently constrains the placement components should also expand. The Card's content should allow the PlacementBracket/PlacementMatchupGrid to use full width. The entrant input tabs (Manual/CSV/Topics) and the counter should stay above the placement area at a readable width. Add `max-w-2xl` wrapping around just the input controls when in full-width mode.

6. Keep Step 1 (Info) and Step 3 (Review) unchanged -- they stay at max-w-2xl naturally via the conditional container class.

**Changes to page.tsx:**

No changes needed to page.tsx -- the `space-y-6` wrapper does not constrain width, and the form component handles its own width.

**Do NOT:**
- Change the PlacementBracket or PlacementMatchupGrid components (they already have responsive layouts)
- Remove the Card wrapper in step 2 -- just let it flow to full width
- Break the step indicator centering
  </action>
  <verify>
    1. Run `npx tsc --noEmit` to confirm no TypeScript errors
    2. Run `npm run build` to confirm build succeeds
    3. Visually inspect in dev: navigate to /brackets/new, proceed to step 2, add entrants, switch to "Visual Placement" mode -- the placement grid should now use full dashboard width. Switch back to "List Reorder" -- the form should return to centered max-w-2xl width.
  </verify>
  <done>
    Visual bracket placement in step 2 uses full page width. Step indicator remains centered. Steps 1 and 3 remain at max-w-2xl. List reorder mode in step 2 also stays at max-w-2xl. Switching between list and visual modes dynamically adjusts the width.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. Visual: Step 1 (Info) renders at centered max-w-2xl width
4. Visual: Step 2 in list mode renders at centered max-w-2xl width
5. Visual: Step 2 in visual placement mode expands to full dashboard width
6. Visual: Step 3 (Review) renders at centered max-w-2xl width
7. Visual: Entrant pool sidebar is usable in full-width mode
8. Visual: Step indicator (1-2-3) remains centered in all modes
</verification>

<success_criteria>
- Visual bracket placement step fills the full page width instead of being constrained to a sidebar
- No regressions in other wizard steps or list reorder mode
- TypeScript and build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/15-ux-polish/15-01-SUMMARY.md`
</output>
