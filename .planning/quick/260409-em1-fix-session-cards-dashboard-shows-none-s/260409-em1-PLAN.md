---
phase: quick
plan: 260409-em1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/shell.tsx
  - src/app/(dashboard)/sessions/page.tsx
autonomous: true
must_haves:
  truths:
    - "Dashboard shows up to 6 session cards for the most recently active sessions"
    - "Sessions page card grid shows only top 6 active sessions, not all sessions"
    - "Both pages show session name, code, participant count, and status badge on cards"
  artifacts:
    - path: "src/components/dashboard/shell.tsx"
      provides: "Session card grid below dropdown"
      contains: "activeSessions.slice(0, 6)"
    - path: "src/app/(dashboard)/sessions/page.tsx"
      provides: "Filtered active session cards"
      contains: "activeSessions.slice(0, 6)"
  key_links:
    - from: "src/components/dashboard/shell.tsx"
      to: "/sessions/{id}"
      via: "Link component on each card"
      pattern: "href=.*sessions/"
---

<objective>
Fix session card display on both dashboard and sessions page. Dashboard currently shows NO session cards (only a dropdown and empty state). Sessions page shows ALL sessions as cards regardless of status. Both should show a grid of up to 6 active session cards.

Purpose: Teachers need quick visual access to their active sessions from both the dashboard and sessions management page.
Output: Updated shell.tsx with session card grid, updated sessions/page.tsx with filtered card grid.
</objective>

<execution_context>
@/Users/davidreynoldsjr/VibeCoding/SparkVotEDU/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/VibeCoding/SparkVotEDU/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/dashboard/shell.tsx
@src/app/(dashboard)/sessions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session card grid to dashboard and limit sessions page cards to 6 active</name>
  <files>src/components/dashboard/shell.tsx, src/app/(dashboard)/sessions/page.tsx</files>
  <action>
**File 1: src/components/dashboard/shell.tsx**

Add imports for Card, CardContent, CardHeader, CardTitle from `@/components/ui/card` and Badge from `@/components/ui/badge`.

After the DashboardSessionDropdown block (line ~145) and BEFORE the empty state block (line ~148), add an "Active Sessions" card grid section. This should render when `activeSessions.length > 0`:

```
{activeSessions.length > 0 && (
  <div className="space-y-4">
    <div className="flex items-center justify-between">
      <h2 className="text-lg font-semibold">Active Sessions</h2>
      <Link
        href="/sessions"
        className="inline-flex items-center gap-1 text-sm font-medium text-brand-blue hover:underline"
      >
        View all <ArrowRight className="h-3 w-3" />
      </Link>
    </div>
    <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {activeSessions.slice(0, 6).map((session) => (
        <Link key={session.id} href={`/sessions/${session.id}`} className="block">
          <Card className="transition-shadow hover:shadow-md cursor-pointer">
            <CardHeader>
              <div className="flex items-start justify-between">
                <CardTitle className="text-base pr-8">
                  {session.name || `Unnamed Session — ${new Date(session.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}`}
                </CardTitle>
                <Badge variant="default">active</Badge>
              </div>
            </CardHeader>
            <CardContent>
              <div className="flex items-center justify-between text-sm">
                <span className="font-mono text-lg font-bold">{session.code}</span>
                <span className="text-muted-foreground">
                  {session._count.participants} student{session._count.participants !== 1 ? 's' : ''}
                </span>
              </div>
            </CardContent>
          </Card>
        </Link>
      ))}
    </div>
  </div>
)}
```

The card pattern matches exactly what the sessions page already uses (Card, CardHeader, CardTitle, CardContent, Badge), ensuring visual consistency.

**File 2: src/app/(dashboard)/sessions/page.tsx**

Change line 54-55 from:
```
{sessions.map((session) => (
```
to:
```
{activeSessions.slice(0, 6).map((session) => (
```

Also update the section heading on line 48 from "Your Sessions" to "Active Sessions" to reflect the filtered content.

Keep the rest of the card rendering exactly the same (it already has proper status badge, name, code, participant count, and SessionCardMenu). The SessionCardMenu remains on each card for actions.

If there are zero active sessions, show a brief message like "No active sessions." instead of the grid. Keep the existing empty state check for `sessions.length === 0` as-is since that covers the "no sessions at all" case; add a separate check: if `sessions.length > 0` but `activeSessions.length === 0`, show "No active sessions. Activate a session to see it here."
  </action>
  <verify>
    <automated>cd /Users/davidreynoldsjr/VibeCoding/SparkVotEDU && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Dashboard shell.tsx renders up to 6 active session cards in a responsive grid below the dropdown
    - Sessions page card grid shows only activeSessions.slice(0, 6), not all sessions
    - Both card grids use identical Card/Badge visual pattern
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Visit localhost:3001 — dashboard shows session cards for active sessions (up to 6)
3. Visit localhost:3001/sessions — card grid shows only active sessions (max 6), not closed/all
4. Both pages show matching card design: session name, code, participant count, status badge
</verification>

<success_criteria>
- Dashboard has visible session cards (not just a dropdown) when active sessions exist
- Sessions page card grid is limited to top 6 active sessions
- Both pages render consistent card UI
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260409-em1-fix-session-cards-dashboard-shows-none-s/260409-em1-SUMMARY.md`
</output>
