---
phase: 07-advanced-brackets
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bracket/round-robin-matchups.tsx
  - src/components/teacher/live-dashboard.tsx
  - src/lib/dal/bracket.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "RR round 1 matchups transition from pending to voting when bracket is activated"
    - "Teacher sees Open Round N+1 button after current round is complete"
    - "Student can click to vote on an RR matchup in voting status"
    - "Student in round_by_round pacing only sees the current round (future rounds hidden)"
  artifacts:
    - path: "src/components/bracket/round-robin-matchups.tsx"
      provides: "Student voting UI in MatchupCard and round visibility filtering"
      contains: "onStudentVote"
    - path: "src/components/teacher/live-dashboard.tsx"
      provides: "Open Round 1 button for RR brackets"
      contains: "Open Round 1"
    - path: "src/lib/dal/bracket.ts"
      provides: "Auto-open RR round 1 on activation"
      contains: "round_robin"
  key_links:
    - from: "RoundRobinMatchups (student mode)"
      to: "onStudentVote callback"
      via: "MatchupCard click handler when isTeacher=false and status=voting"
      pattern: "onStudentVote.*matchup\\.id"
    - from: "live-dashboard.tsx"
      to: "advanceRound server action"
      via: "Open Round 1 button calling advanceRound with roundNumber 1"
      pattern: "advanceRound.*roundNumber.*1"
    - from: "updateBracketStatusDAL"
      to: "advanceRoundRobinRound"
      via: "auto-open round 1 on RR activation"
      pattern: "round_robin.*advanceRoundRobinRound.*1"
---

<objective>
Fix the Round Robin voting lifecycle: auto-open round 1 on activation, add student vote UI to matchup cards, add Open Round 1 button as fallback, and hide future rounds from students in round_by_round pacing.

Purpose: Tests 10 and 11 fail because (1) RR round 1 matchups never transition to 'voting' status (chicken-and-egg problem -- no button exists to open round 1), (2) RoundRobinMatchups has zero student voting UI, (3) students can see all rounds even in round_by_round mode. These are Root Causes RC2, RC3, RC4 from the debug analysis.

Output: Complete RR voting lifecycle from activation through student voting and round advancement.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/student-voting-broken-all-types.md
@.planning/debug/rr-live-controls-missing.md

Key code references:
@src/components/bracket/round-robin-matchups.tsx
@src/components/teacher/live-dashboard.tsx
@src/lib/dal/bracket.ts
@src/lib/dal/round-robin.ts (advanceRoundRobinRound function at line 209-240)
@src/actions/round-robin.ts (advanceRound server action)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-open RR round 1 on activation + Open Round 1 button fallback</name>
  <files>src/lib/dal/bracket.ts, src/components/teacher/live-dashboard.tsx</files>
  <action>
    **Part A: Auto-open round 1 in DAL on activation (src/lib/dal/bracket.ts)**

    In updateBracketStatusDAL, after the bracket status is set to 'active', add bracket-type-specific logic:

    ```
    // After setting bracket.status = 'active':
    if (bracket.bracketType === 'round_robin') {
      // Auto-open round 1 for voting
      await prisma.matchup.updateMany({
        where: {
          bracketId: bracket.id,
          roundRobinRound: 1,
          status: 'pending',
        },
        data: { status: 'voting' },
      })
    }
    ```

    This needs to happen within the activation flow. Look at the existing updateBracketStatusDAL function. Find where it transitions from 'draft' to 'active' and add the RR round 1 opening there. Import prisma if not already available in that scope.

    Also broadcast the round_advanced event after opening round 1:
    ```
    broadcastBracketUpdate(bracketId, 'round_advanced', { round: 1 }).catch(console.error)
    ```

    **Part B: Open Round 1 button fallback in live dashboard (src/components/teacher/live-dashboard.tsx)**

    Add a fallback "Open Round 1" button for cases where auto-open didn't fire (e.g., bracket was already active before this fix). Add this condition near line 424 alongside the existing canAdvanceRoundRobin button:

    ```
    // Add new condition: round 1 is all pending (needs opening)
    const needsRound1Open = isRoundRobin &&
      currentMatchups.filter(m => m.roundRobinRound === 1).every(m => m.status === 'pending') &&
      currentMatchups.filter(m => m.roundRobinRound === 1).length > 0
    ```

    Add a useMemo for this check near the other RR useMemos (around line 256-270).

    Then add the button in the action bar (near line 424):
    ```
    {isRoundRobin && needsRound1Open && (
      <button
        onClick={() => {
          setError(null)
          startTransition(async () => {
            const result = await advanceRound({ bracketId: bracket.id, roundNumber: 1 })
            if (result && 'error' in result) setError(result.error as string)
          })
        }}
        disabled={isPending}
        className="rounded-md bg-blue-600 px-3 py-1.5 text-xs font-medium text-white hover:bg-blue-700 disabled:opacity-50"
      >
        {isPending ? 'Opening...' : 'Open Round 1'}
      </button>
    )}
    ```

    IMPORTANT: The advanceRound import is already at the top of live-dashboard.tsx (line 15). Do NOT duplicate the import.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Verify no type errors.
    Check that updateBracketStatusDAL includes the RR round 1 auto-open logic.
    Check that live-dashboard.tsx has the needsRound1Open button.
  </verify>
  <done>When a round-robin bracket is activated, round 1 matchups automatically transition to 'voting' status. Teachers also see an "Open Round 1" fallback button if round 1 is still pending.</done>
</task>

<task type="auto">
  <name>Task 2: Add student voting UI to RoundRobinMatchups + hide future rounds</name>
  <files>src/components/bracket/round-robin-matchups.tsx</files>
  <action>
    **Part A: Add student voting callback prop**

    Add a new optional prop to RoundRobinMatchupsProps:
    ```
    onStudentVote?: (matchupId: string, entrantId: string) => void
    ```

    Thread this prop down to MatchupCard via MatchupCardProps.

    **Part B: Add student vote UI in MatchupCard**

    In the MatchupCard component, when isTeacher=false AND matchup.status === 'voting' AND onStudentVote exists, render clickable entrant vote buttons. Add this BEFORE the existing status/actions div:

    When isVoting and !isTeacher and onStudentVote exists:
    - Replace the static entrant name display with clickable buttons for each entrant
    - Each button calls onStudentVote(matchup.id, entrantId) on click
    - Style: Similar to the teacher win buttons but student-oriented -- use primary color, show "Vote" label
    - Layout: Two side-by-side buttons within the matchup card, one for each entrant
    - When the student has already voted (track via optional votedMatchupIds prop or entrant highlight), show the voted entrant with a checkmark and primary color

    Add a votedMatchupId prop to MatchupCardProps:
    ```
    votedMatchupId?: string | null  // The entrant ID the student voted for in this matchup
    ```

    Also add votedMatchups prop to RoundRobinMatchupsProps:
    ```
    votedMatchups?: Record<string, string>  // matchupId -> voted entrantId
    ```

    Thread votedMatchups[matchup.id] down as votedMatchupId to each MatchupCard.

    Student vote button rendering in MatchupCard when isVoting && !isTeacher && onStudentVote:
    ```
    <div className="flex items-center gap-1.5">
      <button
        type="button"
        onClick={() => onStudentVote(matchup.id, matchup.entrant1Id!)}
        disabled={!!votedMatchupId}
        className={`rounded px-2.5 py-1 text-xs font-medium transition-colors ${
          votedMatchupId === matchup.entrant1Id
            ? 'bg-primary text-primary-foreground'
            : votedMatchupId
              ? 'bg-muted text-muted-foreground'
              : 'bg-primary/10 text-primary hover:bg-primary/20'
        }`}
      >
        {entrant1Name} {votedMatchupId === matchup.entrant1Id ? '✓' : ''}
      </button>
      <span className="text-xs text-muted-foreground">vs</span>
      <button
        type="button"
        onClick={() => onStudentVote(matchup.id, matchup.entrant2Id!)}
        disabled={!!votedMatchupId}
        className={`rounded px-2.5 py-1 text-xs font-medium transition-colors ${
          votedMatchupId === matchup.entrant2Id
            ? 'bg-primary text-primary-foreground'
            : votedMatchupId
              ? 'bg-muted text-muted-foreground'
              : 'bg-primary/10 text-primary hover:bg-primary/20'
        }`}
      >
        {entrant2Name} {votedMatchupId === matchup.entrant2Id ? '✓' : ''}
      </button>
    </div>
    ```

    When isVoting && !isTeacher && onStudentVote, hide the "Upcoming" badge (it shows even during voting for students currently) and show the vote buttons instead. Keep the existing teacher controls untouched for isTeacher=true.

    **Part C: Hide future rounds for round_by_round pacing on student side**

    In the main RoundRobinMatchups component, when isTeacher=false and pacing is 'round_by_round', filter out rounds that are entirely 'pending' (i.e., future rounds not yet opened for voting).

    Before the roundNumbers.map, add filtering:
    ```
    const visibleRounds = !isTeacher && pacing === 'round_by_round'
      ? roundNumbers.filter(rn => {
          const rm = roundsMap.get(rn) ?? []
          // Show rounds that have at least one non-pending matchup, OR are the current round
          return rm.some(m => m.status !== 'pending') || rn === currentRound
        })
      : roundNumbers
    ```

    Then map over visibleRounds instead of roundNumbers.

    IMPORTANT: Keep all existing props optional to maintain backwards compatibility. All existing call sites (live-dashboard.tsx, bracket-detail.tsx, student bracket page) should compile unchanged.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Verify no type errors.
    Check: RoundRobinMatchups still renders with only the original props (backwards compatible).
    Check: Student side filters future rounds when pacing is round_by_round.
  </verify>
  <done>RoundRobinMatchups shows clickable vote buttons for students on voting-status matchups. Future rounds are hidden from students in round_by_round pacing mode. Teacher controls remain unchanged.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with no errors
- updateBracketStatusDAL auto-opens RR round 1 matchups to 'voting' on activation
- Live dashboard shows "Open Round 1" button when RR round 1 is all pending
- RoundRobinMatchups shows vote buttons for students when matchup status is 'voting'
- In round_by_round pacing, students cannot see or expand future rounds that are all pending
- All existing call sites compile without changes (new props are optional)
</verification>

<success_criteria>
When a teacher activates an RR bracket, round 1 opens for voting automatically. Students see vote buttons on voting matchups and can click to cast votes. In round_by_round pacing, only the current round is visible to students. Test 10 (RR matchup grid voting) and test 11 (round advancement controls) pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-18-SUMMARY.md`
</output>
