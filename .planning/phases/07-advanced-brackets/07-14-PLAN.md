---
phase: 07-advanced-brackets
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/brackets/[bracketId]/state/route.ts
  - src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Student sees double-elimination bracket with Winners/Losers/Grand Finals tabs"
    - "Student sees round-robin standings table and matchup grid (not SE bracket)"
    - "Student sees predictive bracket prediction UI when predictions are open"
    - "State API returns bracketType, predictionStatus, predictiveMode, and per-matchup bracketRegion/isBye/roundRobinRound"
  artifacts:
    - path: "src/app/api/brackets/[bracketId]/state/route.ts"
      provides: "Enriched bracket state API with type-specific fields"
      contains: "bracketType"
    - path: "src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx"
      provides: "Bracket-type-aware student rendering with DE/RR/Predictive paths"
      contains: "DoubleElimDiagram"
  key_links:
    - from: "state/route.ts"
      to: "student page.tsx"
      via: "BracketStateResponse interface"
      pattern: "bracketType.*BracketStateResponse"
    - from: "student page.tsx"
      to: "DoubleElimDiagram"
      via: "conditional render on bracketType"
      pattern: "bracketType.*double_elimination"
    - from: "student page.tsx"
      to: "PredictiveBracket"
      via: "conditional render on bracketType"
      pattern: "bracketType.*predictive"
---

<objective>
Fix the bracket state API and student bracket page to support all bracket types (double-elimination, round-robin, predictive) instead of hardcoding to single-elimination.

Purpose: Students currently only see a single-elimination bracket view regardless of the actual bracket type. The state API omits type-specific fields and the student page hardcodes bracketType to 'single_elimination'. This blocks UAT tests 5, 9, 10, 12, and 13.

Output: Enriched state API response with bracketType and per-matchup fields; student page with bracket-type routing that renders DoubleElimDiagram, RoundRobinStandings+RoundRobinMatchups, or PredictiveBracket based on bracketType.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/bracket-type-routing-missing.md
@.planning/debug/predictive-lifecycle-disconnect.md

Key reference: src/components/bracket/bracket-detail.tsx (lines 37-222) shows the correct bracketType routing pattern with imports for DoubleElimDiagram, RoundRobinStandings, RoundRobinMatchups, and PredictiveBracket. Mirror this pattern in both the state API and student page.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich bracket state API with type-specific fields</name>
  <files>src/app/api/brackets/[bracketId]/state/route.ts</files>
  <action>
  Modify the GET handler in state/route.ts to include bracket-type-specific fields in the response:

  1. Add these bracket-level fields to the JSON response (after the existing fields on lines 63-71):
     - `bracketType: bracket.bracketType` (string: 'single_elimination' | 'double_elimination' | 'round_robin' | 'predictive')
     - `predictionStatus: bracket.predictionStatus` (string | null)
     - `predictiveMode: bracket.predictiveMode` (string | null)
     - `roundRobinPacing: bracket.roundRobinPacing` (string | null)
     - `roundRobinVotingStyle: bracket.roundRobinVotingStyle` (string | null)
     - `roundRobinStandingsMode: bracket.roundRobinStandingsMode` (string | null)
     - `maxEntrants: bracket.maxEntrants` (number | null)
     - `playInEnabled: bracket.playInEnabled` (boolean)

  2. Add per-matchup fields to each matchup object in matchupsWithCounts (the map on lines 44-61):
     - `bracketRegion: matchup.bracketRegion` (string | null: 'winners' | 'losers' | 'grand_finals' | null)
     - `isBye: matchup.isBye` (boolean)
     - `roundRobinRound: matchup.roundRobinRound` (number | null)
     - `nextMatchupId: matchup.nextMatchupId` (string | null)

  The Prisma query on lines 21-37 already includes these fields in the bracket object (they are on the model). No query changes needed -- just add them to the JSON response.

  Do NOT change the Prisma query. Do NOT change the existing response fields. Only ADD new fields.
  </action>
  <verify>
  Run `npx tsc --noEmit` -- zero type errors. The response shape is not type-checked at the API boundary (it's a NextResponse.json call) so this is additive and safe.
  </verify>
  <done>State API at /api/brackets/[bracketId]/state returns bracketType, predictionStatus, predictiveMode, roundRobinPacing, roundRobinVotingStyle, roundRobinStandingsMode, maxEntrants, playInEnabled at bracket level, plus bracketRegion, isBye, roundRobinRound, nextMatchupId at matchup level.</done>
</task>

<task type="auto">
  <name>Task 2: Add bracket type routing to student page</name>
  <files>src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx</files>
  <action>
  This task has four sub-changes to the student bracket page:

  **2a. Update BracketStateResponse interface (lines 14-35)**
  Add the new fields from Task 1 to the TypeScript interface:
  - Bracket-level: `bracketType: string`, `predictionStatus: string | null`, `predictiveMode: string | null`, `roundRobinPacing: string | null`, `roundRobinVotingStyle: string | null`, `roundRobinStandingsMode: string | null`, `maxEntrants: number | null`, `playInEnabled: boolean`
  - Per-matchup: `bracketRegion: string | null`, `isBye: boolean`, `roundRobinRound: number | null`, `nextMatchupId: string | null`

  **2b. Fix toBracketWithDetails conversion (lines 266-327)**
  Replace hardcoded values with actual API data:
  - Line 280: `bracketRegion: m.bracketRegion ?? null` (was hardcoded null)
  - Line 281: `isBye: m.isBye ?? false` (was hardcoded false)
  - Line 282: `roundRobinRound: m.roundRobinRound ?? null` (was hardcoded null)
  - Add: `nextMatchupId: m.nextMatchupId ?? null` (was hardcoded null)
  - Line 306: `bracketType: data.bracketType ?? 'single_elimination'` (was hardcoded 'single_elimination')
  - Line 314: `predictionStatus: data.predictionStatus ?? null` (was hardcoded null)
  - Line 315: `roundRobinPacing: data.roundRobinPacing ?? null` (was hardcoded null)
  - Line 316: `roundRobinVotingStyle: data.roundRobinVotingStyle ?? null` (was hardcoded null)
  - Line 317: `roundRobinStandingsMode: data.roundRobinStandingsMode ?? null` (was hardcoded null)
  - Line 318: `predictiveMode: data.predictiveMode ?? null` (was hardcoded null)
  - Line 319: `predictiveResolutionMode: data.predictiveResolutionMode ?? null` (keep null, not in API)
  - Line 320: `playInEnabled: data.playInEnabled ?? false` (was hardcoded false)
  - Line 321: `maxEntrants: data.maxEntrants ?? null` (was hardcoded null)

  **2c. Add component imports (top of file, after existing imports)**
  Add these imports:
  ```
  import { DoubleElimDiagram } from '@/components/bracket/double-elim-diagram'
  import { RoundRobinStandings } from '@/components/bracket/round-robin-standings'
  import { RoundRobinMatchups } from '@/components/bracket/round-robin-matchups'
  import { PredictiveBracket } from '@/components/bracket/predictive-bracket'
  ```

  **2d. Add bracket type routing in render logic**

  For the "ready" state (currently lines 231-259), replace the simple viewingMode routing with bracketType routing. The pattern must mirror bracket-detail.tsx (lines 151-222):

  For `bracket.bracketType === 'predictive'`:
  - When `bracket.predictionStatus === 'predictions_open'`, render `<PredictiveBracket bracket={bracket} participantId={participantId} isTeacher={false} />` where students can submit predictions.
  - When bracket is active/completed, render the AdvancedVotingView (bracket is resolving, students watch).
  - Wrap in the backLink div.

  For `bracket.bracketType === 'round_robin'`:
  - Render `<RoundRobinStandings standings={[]} isLive={bracket.roundRobinStandingsMode === 'live'} />` (empty standings array since student page doesn't fetch standings -- the component handles empty gracefully).
  - Render `<RoundRobinMatchups matchups={bracket.matchups} entrants={bracket.entrants} currentRound={1} pacing={(bracket.roundRobinPacing ?? 'round_by_round') as 'round_by_round' | 'all_at_once'} isTeacher={false} />`.
  - Wrap in the backLink div with bracket name heading.

  For `bracket.bracketType === 'double_elimination'`:
  - Render `<DoubleElimDiagram bracket={bracket} entrants={bracket.entrants} matchups={bracket.matchups} isTeacher={false} />`.
  - Wrap in the backLink div with bracket name heading.

  For `bracket.bracketType === 'single_elimination'` (or default fallback):
  - Keep the existing viewingMode routing (SimpleVotingView vs AdvancedVotingView), unchanged.

  Also update the "completed" state (lines 214-228) to use bracket type routing:
  - For DE completed: render read-only DoubleElimDiagram
  - For RR completed: render read-only RoundRobinStandings + RoundRobinMatchups
  - For predictive completed: render read-only PredictiveBracket
  - For SE completed: keep existing AdvancedVotingView

  Important: The "draft" and "predictions_open for non-predictive" should still show the "not ready yet" message. For predictive brackets, `predictionStatus === 'predictions_open'` AND bracket.status === 'active' means the student should see the PredictiveBracket component.
  </action>
  <verify>
  Run `npx tsc --noEmit` -- zero type errors.
  Run `npx next build 2>&1 | tail -20` OR just run tsc to verify the page compiles. The imports should resolve since DoubleElimDiagram, RoundRobinStandings, RoundRobinMatchups, and PredictiveBracket all exist from Phase 7 plans 09, 10, and 11.
  </verify>
  <done>Student bracket page reads bracketType from API, routes to appropriate component (DoubleElimDiagram for DE, RoundRobinStandings+RoundRobinMatchups for RR, PredictiveBracket for predictive, SimpleVotingView/AdvancedVotingView for SE), and passes correct data from the enriched state API response.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `npx vitest run` -- all existing tests pass (no regressions)
3. Manual: Create a double-elimination bracket, activate it, assign to session. Student navigating to the bracket page should see the DoubleElimDiagram with Winners/Losers/Grand Finals tabs (not a single-elimination bracket diagram).
4. Manual: Create a round-robin bracket, activate it, assign to session. Student should see standings table + matchup grid (not SE diagram).
5. Manual: Create a predictive bracket, open predictions. Student should see PredictiveBracket with prediction submission cards (not SE voting view).
</verification>

<success_criteria>
- State API returns bracketType and all type-specific fields
- Student page renders correct component for each bracket type
- No TypeScript errors, no test regressions
- UAT tests 5, 9, 12, 13 closable after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-14-SUMMARY.md`
</output>
