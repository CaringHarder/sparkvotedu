---
phase: 07-advanced-brackets
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/teacher/live-dashboard.tsx
  - src/lib/bracket/advancement.ts
  - src/app/(dashboard)/brackets/[bracketId]/live/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Teacher can open/close voting and advance matchups in the losers bracket"
    - "Teacher can open/close voting and advance matchups in grand finals"
    - "isBracketComplete only fires after grand finals completes for DE brackets"
    - "Round tabs show WB, LB, and GF regions for DE brackets instead of flat round numbers"
  artifacts:
    - path: "src/components/teacher/live-dashboard.tsx"
      provides: "Region-based navigation for DE brackets"
      contains: "deRegion"
    - path: "src/lib/bracket/advancement.ts"
      provides: "DE-aware bracket completion check"
      contains: "bracketRegion"
    - path: "src/app/(dashboard)/brackets/[bracketId]/live/page.tsx"
      provides: "DE-aware totalRounds calculation for live page"
  key_links:
    - from: "src/components/teacher/live-dashboard.tsx"
      to: "src/lib/bracket/advancement.ts"
      via: "advanceMatchup action uses DE-aware completion"
      pattern: "isBracketComplete.*grand_finals"
---

<objective>
Fix the DE live dashboard so it navigates by region (Winners -> Losers -> Grand Finals) rather than flat round numbers. The dashboard must allow teachers to open voting, close & advance, and navigate across all three DE regions. Also fix isBracketComplete to require GF completion before declaring a DE bracket finished.

Purpose: GAP-R3-01 -- Teacher can only interact with Winners bracket matchups. LB and GF are invisible to the round-based controls. After WB final, bracket completes prematurely.

Output: Live dashboard with region-based DE navigation and DE-aware completion logic.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-advanced-brackets/07-VERIFICATION-R3.md
@src/components/teacher/live-dashboard.tsx
@src/lib/bracket/advancement.ts
@src/app/(dashboard)/brackets/[bracketId]/live/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DE-aware isBracketComplete and region-aware totalRounds</name>
  <files>src/lib/bracket/advancement.ts, src/app/(dashboard)/brackets/[bracketId]/live/page.tsx</files>
  <action>
    1. In `advancement.ts`, modify `isBracketComplete` to accept an optional `bracketType` parameter. When bracketType is `double_elimination`:
       - Query matchups with `bracketRegion: 'grand_finals'` and check if the highest-round GF matchup has a winnerId.
       - If no GF matchups exist yet, return null (bracket not complete).
       - If GF matchups exist, only return winnerId from the highest-round GF matchup.
       - For all other bracket types, keep current behavior (highest-round matchup with winnerId).

    2. In `live/page.tsx`, update the totalRounds calculation for DE brackets:
       - For DE brackets, compute totalRounds as the sum of WB rounds + LB rounds + GF rounds (1-2). This ensures the live dashboard understands the full DE lifecycle.
       - However, since the live dashboard will use region-based navigation (Task 2), pass additional metadata to LiveDashboard: `bracketType` is already in the serialized bracket. Add a `deTotalRoundsByRegion` prop (optional) with `{ wb: number, lb: number, gf: number }` for DE brackets only.
       - Actually, simpler approach: the LiveDashboard can compute region-specific round info from matchups. Just ensure all matchup data including bracketRegion is serialized (already done on line 138). No new prop needed -- the component will derive region info from matchups.

    3. For DE brackets, the totalRounds passed to LiveDashboard should represent the FULL bracket lifecycle, not just WB. Calculate as:
       ```
       const wbMatchups = bracket.matchups.filter(m => m.bracketRegion === 'winners')
       const lbMatchups = bracket.matchups.filter(m => m.bracketRegion === 'losers')
       const gfMatchups = bracket.matchups.filter(m => m.bracketRegion === 'grand_finals')
       const wbRounds = wbMatchups.length > 0 ? Math.max(...wbMatchups.map(m => m.round)) : 0
       const lbRounds = lbMatchups.length > 0 ? Math.max(...lbMatchups.map(m => m.round)) - Math.min(...lbMatchups.map(m => m.round)) + 1 : 0
       const gfRounds = gfMatchups.length > 0 ? Math.max(...gfMatchups.map(m => m.round)) - Math.min(...gfMatchups.map(m => m.round)) + 1 : 0
       totalRounds = wbRounds + lbRounds + gfRounds
       ```
       But since the dashboard uses region-based navigation, totalRounds is less important. The key fix is in the LiveDashboard component itself (Task 2).
  </action>
  <verify>TypeScript compiles with no errors: `npx tsc --noEmit`</verify>
  <done>isBracketComplete checks GF completion for DE brackets. Live page serializes all necessary data for region-based navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement region-based DE navigation in LiveDashboard</name>
  <files>src/components/teacher/live-dashboard.tsx</files>
  <action>
    Refactor LiveDashboard to use region-based navigation for DE brackets. The core changes:

    1. **Add DE region state and derived data:**
       - Add `deRegion` state: `'winners' | 'losers' | 'grand_finals'`, default `'winners'`.
       - Derive region-specific matchup lists from `currentMatchups` using `bracketRegion` filter:
         ```
         const wbMatchups = currentMatchups.filter(m => m.bracketRegion === 'winners')
         const lbMatchups = currentMatchups.filter(m => m.bracketRegion === 'losers')
         const gfMatchups = currentMatchups.filter(m => m.bracketRegion === 'grand_finals')
         ```
       - For the currently selected DE region, compute `regionMatchups`, `regionCurrentRound`, `regionTotalRounds`, `regionRoundStatus` using the same logic as the existing SE round logic but scoped to region matchups only.
       - For WB: rounds are 1..wbMaxRound. For LB: normalize rounds by subtracting the offset (lbMinRound - 1). For GF: normalize similarly.

    2. **Replace flat round tabs with DE region tabs (when isDoubleElim):**
       - Render three region tabs: "Winners", "Losers", "Grand Finals" (with active matchup count badges).
       - Within the selected region, show round sub-tabs using the existing round tab logic but scoped to the region's normalized rounds.
       - Grand Finals tab badge and visibility: show when at least one GF matchup exists with both entrants set.

    3. **Fix Open Voting button for DE:**
       - When DE is active and a region is selected, `handleOpenVoting` must filter pending matchups by BOTH `currentRound` (within the region) AND the region's bracketRegion.
       - Concretely: for the active region, find pending matchups in the region-specific current round and open them.
       - The existing `openMatchupsForVoting` server action takes matchupIds, so no server change needed -- just filter correctly on the client.

    4. **Fix Close & Advance button for DE:**
       - Same as above: `handleCloseAndAdvance` must filter voting matchups by the active region and region-specific current round.
       - The existing `advanceMatchup` server action already handles DE advancement (it's routing-aware). No server change needed.

    5. **Fix bracket completion detection for DE:**
       - The existing `bracketDone` check (`currentRound === totalRounds && allRoundDecided`) won't work for DE.
       - For DE: bracket is done when GF matchups all have `status === 'decided'`. Check: `gfMatchups.length > 0 && gfMatchups.every(m => m.status === 'decided')`.
       - Show "Complete!" badge only when this condition is met.

    6. **Keep the DoubleElimDiagram rendering as-is** in the main content area -- it already has its own tab navigation for Winners/Losers/GF. The top bar controls (Open Voting, Close & Advance) now operate on the region selected in the TOP BAR region tabs, independent of the diagram tabs.

    7. **Ensure non-DE behavior is completely unchanged.** All new logic gates on `isDoubleElim`. SE/RR/Predictive brackets use existing code paths.

    Important implementation detail: For DE, the `currentRound` concept needs to be region-aware. Compute `deCurrentRound` by finding the first non-fully-decided round within the currently selected region. Use the region's round offset to convert between DB rounds and display rounds.
  </action>
  <verify>`npx tsc --noEmit` passes. Manually verify the component renders without errors by checking the live dashboard route renders for a DE bracket.</verify>
  <done>DE live dashboard shows region tabs (Winners/Losers/Grand Finals), Open Voting and Close & Advance work across all three regions, completion only fires after GF is decided.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. LiveDashboard renders DE brackets with region tabs in the top bar
3. Open Voting button opens pending matchups in the selected region's current round
4. Close & Advance processes voting matchups in the selected region's current round
5. Bracket completion only triggers after Grand Finals are decided for DE brackets
</verification>

<success_criteria>
- Teacher can navigate Winners/Losers/Grand Finals regions in the live dashboard
- Open Voting and Close & Advance work for all three DE regions
- DE bracket does not prematurely complete after WB final
- SE/RR/Predictive brackets are completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-21-SUMMARY.md`
</output>
