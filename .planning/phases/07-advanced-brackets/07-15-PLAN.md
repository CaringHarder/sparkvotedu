---
phase: 07-advanced-brackets
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/teacher/live-dashboard.tsx
  - src/app/(dashboard)/brackets/[bracketId]/live/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Teacher live view shows DoubleElimDiagram with tabs for double-elimination brackets"
    - "Teacher live view shows RoundRobinStandings + RoundRobinMatchups with round advancement for round-robin brackets"
    - "Teacher live view retains full SE functionality (vote counts, open/close/advance, pick-winner modal) for single-elimination brackets"
  artifacts:
    - path: "src/components/teacher/live-dashboard.tsx"
      provides: "Bracket-type-aware live dashboard rendering"
      contains: "DoubleElimDiagram"
    - path: "src/app/(dashboard)/brackets/[bracketId]/live/page.tsx"
      provides: "Standings data and correct totalRounds for all bracket types"
      contains: "roundRobinStandingsMode"
  key_links:
    - from: "live/page.tsx"
      to: "LiveDashboard"
      via: "serializedBracket.bracketType prop"
      pattern: "bracketType.*serializedBracket"
    - from: "live-dashboard.tsx"
      to: "DoubleElimDiagram"
      via: "conditional render on bracket.bracketType"
      pattern: "bracket\\.bracketType.*double_elimination"
    - from: "live-dashboard.tsx"
      to: "RoundRobinMatchups"
      via: "conditional render on bracket.bracketType"
      pattern: "bracket\\.bracketType.*round_robin"
---

<objective>
Add bracket-type routing to the teacher LiveDashboard component so double-elimination and round-robin brackets render their type-specific views instead of always falling back to BracketDiagram.

Purpose: The LiveDashboard always renders BracketDiagram (single-elimination view) regardless of bracketType. Teachers see a broken SE view when they "Go Live" on DE or RR brackets. This blocks UAT tests 6, 10, and 11.

Output: LiveDashboard conditionally renders DoubleElimDiagram, RoundRobinStandings+RoundRobinMatchups, or BracketDiagram based on bracket.bracketType. The live page server component provides standings data for round-robin brackets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/bracket-type-routing-missing.md

Key reference: src/components/bracket/bracket-detail.tsx (lines 37-222) shows the correct bracketType routing pattern. The live/page.tsx server component already serializes bracketType and all type-specific fields (lines 82-133). LiveDashboard receives the full BracketWithDetails via the `bracket` prop -- it just never reads bracketType.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bracket type routing to LiveDashboard component</name>
  <files>src/components/teacher/live-dashboard.tsx</files>
  <action>
  **1a. Add imports** (after existing imports at top of file):
  ```
  import { DoubleElimDiagram } from '@/components/bracket/double-elim-diagram'
  import { RoundRobinStandings } from '@/components/bracket/round-robin-standings'
  import { RoundRobinMatchups } from '@/components/bracket/round-robin-matchups'
  import { recordResult, advanceRound } from '@/actions/round-robin'
  ```

  **1b. Add bracketType detection** (inside the component, after existing hooks/state around line 45):
  ```
  const isDoubleElim = bracket.bracketType === 'double_elimination'
  const isRoundRobin = bracket.bracketType === 'round_robin'
  const isPredictive = bracket.bracketType === 'predictive'
  ```

  **1c. Add round-robin handlers** (near the existing action handlers around line 240):
  Add a `handleRecordRoundRobinResult` function that calls `recordResult({ bracketId: bracket.id, matchupId, winnerId })` inside startTransition, mirroring bracket-detail.tsx lines 59-67.
  Add a `handleAdvanceRoundRobin` function that calls `advanceRound({ bracketId: bracket.id, roundNumber: nextRoundRobinRound })` inside startTransition, mirroring bracket-detail.tsx lines 69-77.
  Compute `currentRoundRobinRound` (first roundRobinRound with non-decided matchups) and `canAdvanceRoundRobin` (all current round decided AND next round exists).

  **1d. Add standings prop to LiveDashboardProps** (line 15-22):
  Add `standings?: Array<{ rank: number; entrantId: string; entrantName: string; wins: number; losses: number; ties: number; points: number }>` to the props interface.

  **1e. Replace the bracket diagram rendering** (lines 428-438):
  Currently the "Main content" section always renders:
  ```
  <div className="flex-1 overflow-auto rounded-lg border bg-card p-4">
    <BracketDiagram ... />
  </div>
  ```

  Replace with bracketType-based routing:

  For `isDoubleElim`:
  - Render `<DoubleElimDiagram bracket={bracket} entrants={bracket.entrants} matchups={currentMatchups} isTeacher={true} />` inside the same container div.
  - The existing action bar (Open Voting, Close & Advance) should still work -- DE brackets use the same matchup status model for the winners bracket. Keep the existing SE action bar as-is.

  For `isRoundRobin`:
  - Render RoundRobinStandings (with standings prop, default []) + RoundRobinMatchups (with currentMatchups, bracket.entrants, currentRoundRobinRound, pacing from bracket.roundRobinPacing, isTeacher=true, onRecordResult=handleRecordRoundRobinResult).
  - Add an "Open Round N" button (matching bracket-detail.tsx pattern) when canAdvanceRoundRobin is true, calling handleAdvanceRoundRobin.
  - The existing SE-specific action bar (Open Voting / Close & Advance / Next Round) should be hidden for round-robin brackets since RR uses its own advancement pattern. Wrap the SE action buttons section with `{!isRoundRobin && !isDoubleElim && ( ... )}` or more precisely `{!isRoundRobin && ( ... )}` for the round-tab and action buttons that only apply to SE/DE.

  For `isPredictive`:
  - Predictive brackets go live as standard SE brackets (the prediction phase is BEFORE go-live). So render the normal BracketDiagram with vote counts etc. The existing SE live view is correct for predictive during the active phase.

  For default (SE):
  - Keep existing BracketDiagram rendering unchanged.

  **1f. Update top bar round tabs** for non-SE types:
  The round tabs (lines 306-327) showing R1/Semis/Finals only make sense for SE brackets. For RR brackets, hide them. For DE brackets, they can remain (they show WB rounds).

  Wrap the round tabs div with `{!isRoundRobin && ( ... )}`.

  For RR brackets, show the current round indicator instead:
  ```
  {isRoundRobin && (
    <span className="rounded px-2 py-0.5 text-xs font-medium bg-primary text-primary-foreground">
      Round {currentRoundRobinRound}
    </span>
  )}
  ```
  </action>
  <verify>
  Run `npx tsc --noEmit` -- zero type errors.
  Run `npx vitest run` -- all existing tests pass.
  </verify>
  <done>LiveDashboard renders DoubleElimDiagram for DE brackets, RoundRobinStandings+RoundRobinMatchups with round advancement for RR brackets, standard BracketDiagram for SE and predictive brackets. Round tabs hidden for RR, replaced with current round indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Pass standings data and fix totalRounds for live page</name>
  <files>src/app/(dashboard)/brackets/[bracketId]/live/page.tsx</files>
  <action>
  The live page server component needs two fixes:

  **2a. Fix totalRounds calculation** (line 79):
  Currently: `const totalRounds = Math.log2(bracket.size)` -- This is wrong for DE (size includes entrant count, not bracket size) and meaningless for RR.

  Replace with type-aware calculation:
  ```
  let totalRounds: number
  if (bracket.bracketType === 'round_robin') {
    // RR doesn't use totalRounds in LiveDashboard (matchups grouped by roundRobinRound)
    totalRounds = 1
  } else if (bracket.bracketType === 'double_elimination') {
    // DE: use maxEntrants for WB rounds calculation
    const effectiveSize = bracket.maxEntrants ?? bracket.size
    totalRounds = Math.ceil(Math.log2(effectiveSize))
  } else {
    // SE / Predictive: use maxEntrants if available (bye brackets), else size
    const effectiveSize = bracket.maxEntrants ?? bracket.size
    totalRounds = Math.ceil(Math.log2(effectiveSize))
  }
  ```

  **2b. Fetch standings for round-robin brackets**:
  Import `getRoundRobinStandings` from `@/lib/dal/round-robin`.

  After the participant fetch (around line 62), add:
  ```
  let standings: Array<{ rank: number; entrantId: string; entrantName: string; wins: number; losses: number; ties: number; points: number }> = []
  if (bracket.bracketType === 'round_robin') {
    standings = await getRoundRobinStandings(bracketId)
  }
  ```

  **2c. Pass standings to LiveDashboard** (line 142-149):
  Add `standings={standings}` prop to the LiveDashboard component.
  </action>
  <verify>
  Run `npx tsc --noEmit` -- zero type errors.
  </verify>
  <done>Live page provides correct totalRounds for all bracket types and fetches/passes round-robin standings data to LiveDashboard.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `npx vitest run` -- all existing tests pass
3. Manual: Create a double-elimination bracket, activate, go live. Teacher should see DoubleElimDiagram with Winners/Losers/Grand Finals tabs in the live view.
4. Manual: Create a round-robin bracket, activate, go live. Teacher should see standings table + matchup grid with round advancement controls (not SE bracket diagram).
</verification>

<success_criteria>
- LiveDashboard renders correct diagram type for DE and RR brackets
- Round-robin live view shows standings + matchup grid + Open Round button
- Double-elimination live view shows tabbed DE diagram
- Single-elimination live view unchanged (no regressions)
- UAT tests 6, 10, 11 closable after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-brackets/07-15-SUMMARY.md`
</output>
