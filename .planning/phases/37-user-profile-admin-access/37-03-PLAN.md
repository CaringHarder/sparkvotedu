---
phase: 37-user-profile-admin-access
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/app/set-password/layout.tsx
  - src/app/set-password/page.tsx
  - src/components/auth/set-password-form.tsx
autonomous: false
requirements:
  - PROFILE-03

must_haves:
  truths:
    - "Admin-created teacher logging in for the first time is redirected to /set-password"
    - "Set-password page has welcoming onboarding tone: Welcome to SparkVote!"
    - "Teacher sets new password (8+ chars + confirm) and lands on dashboard"
    - "Set-password page is non-dismissable -- no sidebar, no navigation to dashboard"
    - "After setting password, teacher is NOT forced to re-login"
  artifacts:
    - path: "src/app/set-password/layout.tsx"
      provides: "Standalone layout matching (auth) visual style without being in AUTH_PAGES"
      min_lines: 15
    - path: "src/app/set-password/page.tsx"
      provides: "Server component page for forced password reset"
      min_lines: 10
    - path: "src/components/auth/set-password-form.tsx"
      provides: "Client form with welcoming copy, new password + confirm fields, submit button"
      min_lines: 40
  key_links:
    - from: "src/components/auth/set-password-form.tsx"
      to: "src/actions/profile.ts"
      via: "useActionState with forceSetPassword"
      pattern: "forceSetPassword"
    - from: "src/app/proxy.ts"
      to: "src/app/set-password/page.tsx"
      via: "redirect when mustChangePassword is true"
      pattern: "/set-password"
---

<objective>
Create the forced password reset page for admin-created accounts with temporary passwords.

Purpose: When an admin creates a teacher account via the admin panel, that teacher must set their own password on first login before accessing any dashboard features. This plan creates the standalone /set-password route with a welcoming onboarding experience.

Output: /set-password page with layout matching (auth) style, welcoming tone, password set form that clears the flag and redirects to dashboard
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-user-profile-admin-access/37-RESEARCH.md
@.planning/phases/37-user-profile-admin-access/37-CONTEXT.md
@.planning/phases/37-user-profile-admin-access/37-01-SUMMARY.md

@src/app/(auth)/layout.tsx
@src/app/proxy.ts
@src/actions/profile.ts

<interfaces>
<!-- Server action from Plan 01 -->
From src/actions/profile.ts (created in Plan 01):
```typescript
export async function forceSetPassword(
  _prevState: { error?: string; success?: string } | null,
  formData: FormData
): Promise<{ error?: string; success?: string }>
// FormData fields: newPassword (min 8 chars), confirmPassword (must match)
// On success: clears mustChangePassword flag, redirects to /dashboard
// No current password required (forced reset flow)
```

From src/app/(auth)/layout.tsx (visual pattern to replicate):
```tsx
// Centered card layout with SparkVotEDU branding
// gradient background, logo, title, Card with CardContent
// This layout is for the (auth) route group
// /set-password gets its own standalone layout replicating this style
```

Proxy behavior (from Plan 01):
- /set-password is NOT in AUTH_PAGES
- Authenticated users with mustChangePassword=true: allowed to access /set-password
- Authenticated users without flag: redirected to /dashboard
- Unauthenticated users: redirected to /login
- /dashboard routes: flagged users redirected to /set-password
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set-password standalone route and form</name>
  <files>src/app/set-password/layout.tsx, src/app/set-password/page.tsx, src/components/auth/set-password-form.tsx</files>
  <action>
1. Create `src/app/set-password/layout.tsx` -- a standalone layout that replicates the (auth) layout visual style:
   - IMPORTANT: This is a standalone route at `app/set-password/`, NOT under `(auth)/` or `(dashboard)/` route groups. This avoids the proxy redirect loops described in the research (Pitfall 4).
   - Copy the visual structure from `src/app/(auth)/layout.tsx`:
     - Centered card layout with gradient background
     - SparkVotEDU logo and branding at top
     - Card container for the form
   - MODIFY the branding copy to match the welcoming onboarding tone (per user decision):
     - Replace subtitle "Interactive brackets and polls for your classroom" with "Welcome to SparkVote! Set your password to get started."
   - No sidebar, no navigation, no links to dashboard -- this must be non-dismissable (per user decision: full-page takeover using auth-style layout)

2. Create `src/app/set-password/page.tsx` as a simple server component:
   - Import and render `<SetPasswordForm />`
   - No auth check needed here -- the proxy handles access control (only flagged users reach this page)

3. Create `src/components/auth/set-password-form.tsx` as a client component:
   - Use `'use client'` directive
   - Use `useActionState` (from React) with `forceSetPassword` action from `@/actions/profile`
   - Render a welcoming heading inside the card: "Set Your Password" (text-xl font-semibold, centered)
   - Subtle description text: "Choose a password you'll remember. You'll use this to sign in." (text-sm text-muted-foreground, centered)
   - Two input fields (type="password"):
     - New Password (`name="newPassword"`, placeholder "At least 8 characters")
     - Confirm Password (`name="confirmPassword"`, placeholder "Confirm your password")
   - Submit button: "Get Started" (full-width, primary variant) -- welcoming language, not "Set Password"
   - Show error message in red/destructive text if action returns error
   - Disable button during pending state
   - Use existing Input, Label, Button components from `@/components/ui/`
   - Wrap in `<form action={formAction}>`
   - On success, the forceSetPassword action calls redirect('/dashboard') server-side, so no client-side redirect handling needed

  Style notes:
  - Match the (auth) page styling for inputs and buttons (same sizing, spacing)
  - The welcoming tone should feel like onboarding, not a security warning (per user decision)
  - No "Forgot password?" link (per user decision -- that lives on the login page only)
  - No sign-out button on this page -- the teacher cannot dismiss or navigate away
  </action>
  <verify>
    <automated>cd /Users/davidreynoldsjr/VibeCoding/SparkVotEDU && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Standalone /set-password route exists with its own layout matching (auth) visual style. Page renders SetPasswordForm with welcoming copy ("Welcome to SparkVote!"), two password fields (new + confirm), and "Get Started" submit button. Form wired to forceSetPassword action. No navigation or dismissal possible. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete profile and forced reset flows</name>
  <files>n/a</files>
  <action>Human verification of all Phase 37 features across Plans 01, 02, and 03. No code changes -- this is a verification-only checkpoint.</action>
  <verify>
  1. Start dev server at localhost:3001
  2. **Profile page:** Log in as a teacher, click "Profile" in the sidebar (below Billing). Verify:
     - Three card sections visible: Display Name, Change Password, Account Info
     - Edit display name and click Save -- name updates, success message shown
     - Enter current password + new password + confirm, click "Change Password" -- success message shown
     - Account Info shows email (read-only) and role
  3. **Admin gear icon:** Log in as an admin user. Verify:
     - Gear icon visible in the header near sign out button
     - Hover shows "Admin Panel" tooltip
     - Click opens /admin in a new tab
     - Log in as a non-admin teacher -- gear icon is NOT visible
  4. **Forced password reset:** From the admin panel, create a new teacher account with a temporary password. Open an incognito window, log in with that account's email and temp password. Verify:
     - Immediately redirected to /set-password (not dashboard)
     - Page shows welcoming copy "Welcome to SparkVote! Set your password to get started."
     - Enter new password + confirm, click "Get Started"
     - Redirected to dashboard -- no re-login required
     - Navigate to /set-password manually -- should redirect to /dashboard (flag cleared)
  </verify>
  <done>All four success criteria from the roadmap are met: profile page with name editing, password change, forced reset on first login, and admin gear icon. Human confirms all flows work end-to-end.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. /set-password route has standalone layout replicating (auth) style
3. SetPasswordForm wired to forceSetPassword action
4. Human verification of full end-to-end flows (profile, admin gear, forced reset)
</verification>

<success_criteria>
- /set-password page renders with welcoming onboarding tone
- Non-dismissable: no sidebar, no navigation links, no way to reach dashboard without setting password
- Two password fields (new + confirm) with "Get Started" submit button
- After setting password, teacher lands on dashboard without re-login
- Attempting to access /set-password after clearing flag redirects to /dashboard
- Full end-to-end flow verified by human: admin creates account -> teacher logs in -> forced to /set-password -> sets password -> lands on dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/37-user-profile-admin-access/37-03-SUMMARY.md`
</output>
