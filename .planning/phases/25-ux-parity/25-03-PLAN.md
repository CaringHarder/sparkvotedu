---
phase: 25-ux-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bracket/bracket-card-list.tsx
  - src/components/poll/poll-card-list.tsx
autonomous: true
requirements: [UXP-01]
gap_closure: true

must_haves:
  truths:
    - "Bracket card title updates immediately in the UI after inline rename save (no manual refresh needed)"
    - "Poll card title updates immediately in the UI after inline rename save (no manual refresh needed)"
    - "Card removal animations (delete fade, archive slide) still work correctly after the fix"
  artifacts:
    - path: "src/components/bracket/bracket-card-list.tsx"
      provides: "BracketCardList with prop sync useEffect"
      contains: "useEffect"
    - path: "src/components/poll/poll-card-list.tsx"
      provides: "PollCardList with prop sync useEffect"
      contains: "useEffect"
  key_links:
    - from: "src/components/bracket/bracket-card-list.tsx"
      to: "src/app/(dashboard)/brackets/page.tsx"
      via: "brackets prop from server component"
      pattern: "useEffect.*brackets"
    - from: "src/components/poll/poll-card-list.tsx"
      to: "src/app/(dashboard)/polls/page.tsx"
      via: "polls prop from server component"
      pattern: "useEffect.*polls"
---

<objective>
Fix stale UI after inline rename in both BracketCardList and PollCardList.

Purpose: After a rename + router.refresh(), the server component re-fetches and passes updated props to these client components, but useState(initialProps) on line 29/23 never syncs with updated incoming props. The old name stays visible until manual page refresh.

Output: Both card list components sync local state with incoming props via useEffect, so rename changes reflect immediately.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ux-parity/25-02-SUMMARY.md
@src/components/bracket/bracket-card-list.tsx
@src/components/poll/poll-card-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useEffect prop sync to BracketCardList and PollCardList</name>
  <files>src/components/bracket/bracket-card-list.tsx, src/components/poll/poll-card-list.tsx</files>
  <action>
In both BracketCardList (bracket-card-list.tsx) and PollCardList (poll-card-list.tsx), the component initializes local state with `useState(props)` but never updates when props change after router.refresh().

Fix both files identically:

1. Add `useEffect` to the existing `import { useState } from 'react'` line -- change to `import { useState, useEffect } from 'react'`.

2. Add a useEffect that syncs the items state when the prop array changes. Place it immediately after the useState declarations:

```typescript
// Sync local state when server data changes (e.g., after rename + router.refresh)
useEffect(() => {
  setItems(brackets) // or setItems(polls) for PollCardList
}, [brackets]) // or [polls] for PollCardList
```

This ensures that when router.refresh() delivers fresh server-rendered data with the updated name, the local state picks it up. The AnimatePresence exit animations still work because removals go through handleRemove which sets local state directly -- the useEffect only fires on prop changes from the parent server component.

IMPORTANT: Do NOT change the AnimatePresence logic, the motion.div wrappers, the getExitAnimation function, or the handleRemove function. Only add the useEffect sync.

For bracket-card-list.tsx: `useEffect(() => { setItems(brackets) }, [brackets])`
For poll-card-list.tsx: `useEffect(() => { setItems(polls) }, [polls])`
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Run `npm run build` to verify the build succeeds. Visually confirm the useEffect is present in both files by reading them.
  </verify>
  <done>
Both BracketCardList and PollCardList have useEffect that syncs local items state when the props (brackets/polls) array changes. Build passes. Card removal animations are preserved unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Both files contain `useEffect` that references the prop array in its dependency list
4. Both files still contain AnimatePresence with exit animations
</verification>

<success_criteria>
- BracketCardList syncs items when brackets prop changes
- PollCardList syncs items when polls prop changes
- No regressions in card removal animations
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-parity/25-03-SUMMARY.md`
</output>
