---
phase: 25-ux-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bracket/bracket-card.tsx
  - src/components/poll/poll-card.tsx
autonomous: true
requirements: [UXP-01]
gap_closure: true

must_haves:
  truths:
    - "Bracket card title updates instantly in the UI after inline rename save -- no flash of old name"
    - "Poll card title updates instantly in the UI after inline rename save -- no flash of old question"
  artifacts:
    - path: "src/components/bracket/bracket-card.tsx"
      provides: "Optimistic rename display via renameValue local state"
      contains: "{renameValue}"
    - path: "src/components/poll/poll-card.tsx"
      provides: "Optimistic rename display via renameValue local state"
      contains: "{renameValue}"
  key_links:
    - from: "src/components/bracket/bracket-card.tsx"
      to: "renameValue state"
      via: "h3 renders renameValue instead of bracket.name"
      pattern: "<h3[^>]*>\\{renameValue\\}"
    - from: "src/components/poll/poll-card.tsx"
      to: "renameValue state"
      via: "h3 renders renameValue instead of poll.question"
      pattern: "<h3[^>]*>\\{renameValue\\}"
---

<objective>
Fix the stale name flash after inline rename on bracket and poll cards by rendering the optimistic local state (`renameValue`) instead of the server prop (`bracket.name` / `poll.question`) in the `<h3>` display element.

Purpose: When a teacher renames a bracket or poll via inline edit, pressing Enter currently flashes the old name for ~1 second before the new name appears (after router.refresh() + useEffect prop sync). The `renameValue` state already holds the correct value -- the `<h3>` just needs to read from it.

Output: Two single-line fixes that eliminate the stale name flash.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-ux-parity/25-UAT-v2.md
@src/components/bracket/bracket-card.tsx
@src/components/poll/poll-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render renameValue in card h3 elements for optimistic display</name>
  <files>src/components/bracket/bracket-card.tsx, src/components/poll/poll-card.tsx</files>
  <action>
In `src/components/bracket/bracket-card.tsx` line 155: change `{bracket.name}` to `{renameValue}` inside the `<h3>` element. The `renameValue` state is initialized from `bracket.name` (line 46) and stays in sync via the useEffect prop sync in the parent list component, so it always has the correct value -- either the optimistic new name after edit, or the server-refreshed name after router.refresh().

In `src/components/poll/poll-card.tsx` line 123: change `{poll.question}` to `{renameValue}` inside the `<h3>` element. Same pattern -- `renameValue` is initialized from `poll.question` (line 35) and stays in sync.

Do NOT change any other references to `bracket.name` or `poll.question` in these files (they are used correctly in handleRenameSave comparisons, CardContextMenu itemName prop, etc.). Only the `<h3>` display text changes.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no type errors
2. In bracket-card.tsx, the `<h3>` element contains `{renameValue}` not `{bracket.name}`
3. In poll-card.tsx, the `<h3>` element contains `{renameValue}` not `{poll.question}`
4. All other references to `bracket.name` and `poll.question` remain unchanged
  </verify>
  <done>Both card components render the optimistic local state in their title h3, eliminating the ~1s stale name flash after inline rename save</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -n 'renameValue' src/components/bracket/bracket-card.tsx` shows renameValue used in h3
- `grep -n 'renameValue' src/components/poll/poll-card.tsx` shows renameValue used in h3
- No other behavioral changes to these components
</verification>

<success_criteria>
After inline rename and pressing Enter on a bracket or poll card, the new name appears immediately without any flash of the old name. The renameValue local state serves as the optimistic display value, and the existing useEffect prop sync in the parent list component handles eventual consistency with server state.
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-parity/25-05-SUMMARY.md`
</output>
