---
phase: 20-name-based-student-identity
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/teacher/student-roster.tsx
  - src/components/teacher/participation-sidebar.tsx
  - src/app/(dashboard)/sessions/[sessionId]/page.tsx
  - src/app/(dashboard)/sessions/[sessionId]/session-detail.tsx
  - src/components/student/session-header.tsx
  - src/components/student/edit-name-dialog.tsx
  - src/app/(student)/session/[sessionId]/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher dashboard student roster shows 'Fun Name (Real Name)' format for each participant"
    - "Teacher participation sidebar shows 'Fun Name (Real Name)' in student tiles"
    - "Student session header settings dropdown includes 'Edit Name' option"
    - "Student can edit their first name via a dialog, with validation and duplicate-name rejection"
    - "Session layout reads firstName from localStorage and passes it to session header"
  artifacts:
    - path: "src/components/teacher/student-roster.tsx"
      provides: "Student roster displaying funName (firstName) per teacher mapping requirement"
      contains: "firstName"
    - path: "src/components/teacher/participation-sidebar.tsx"
      provides: "Participation sidebar with funName (firstName) display"
      contains: "firstName"
    - path: "src/components/student/edit-name-dialog.tsx"
      provides: "Dialog for editing student first name with validation and duplicate check"
      min_lines: 40
    - path: "src/components/student/session-header.tsx"
      provides: "Session header with Edit Name option in settings dropdown"
      contains: "EditNameDialog"
  key_links:
    - from: "src/components/student/edit-name-dialog.tsx"
      to: "src/actions/student.ts"
      via: "updateParticipantName server action call"
      pattern: "updateParticipantName"
    - from: "src/app/(dashboard)/sessions/[sessionId]/page.tsx"
      to: "src/components/teacher/student-roster.tsx"
      via: "firstName included in participant serialization"
      pattern: "firstName"
    - from: "src/components/student/session-header.tsx"
      to: "src/components/student/edit-name-dialog.tsx"
      via: "renders EditNameDialog in dropdown"
      pattern: "EditNameDialog"
---

<objective>
Update the teacher dashboard to show real-name mappings and add name-edit capability to the student session.

Purpose: Teachers need to see which real student is behind each fun name (per locked decision: "Speedy Penguin (Jake M)"). Students need the ability to edit their name after joining (IDENT-06). The session layout needs to include firstName in the stored participant data.

Output: Updated teacher roster/sidebar with name mappings, new edit-name-dialog component, updated session header with edit option, and updated session layout localStorage shape.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-name-based-student-identity/20-01-SUMMARY.md
@src/components/teacher/student-roster.tsx
@src/components/teacher/participation-sidebar.tsx
@src/app/(dashboard)/sessions/[sessionId]/page.tsx
@src/app/(dashboard)/sessions/[sessionId]/session-detail.tsx
@src/components/student/session-header.tsx
@src/app/(student)/session/[sessionId]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update teacher dashboard to show real-name mappings</name>
  <files>
    src/app/(dashboard)/sessions/[sessionId]/page.tsx
    src/app/(dashboard)/sessions/[sessionId]/session-detail.tsx
    src/components/teacher/student-roster.tsx
    src/components/teacher/participation-sidebar.tsx
  </files>
  <action>
    **1. Update `src/app/(dashboard)/sessions/[sessionId]/page.tsx` (server component):**
    - Add `firstName` to the participant serialization in the `serializedSession` object
    - Currently serializes: `id, funName, banned, rerollUsed, lastSeenAt, createdAt`
    - Add: `firstName: p.firstName` to the map callback
    - The `getSessionWithParticipants` DAL function already includes all participant fields (no select filter), so `firstName` is already available from Prisma

    **2. Update `src/app/(dashboard)/sessions/[sessionId]/session-detail.tsx`:**
    - Add `firstName: string` to the `ParticipantData` interface (currently has: id, funName, banned, rerollUsed, lastSeenAt, createdAt)
    - No other changes needed -- the data flows through to StudentRoster via props

    **3. Update `src/components/teacher/student-roster.tsx`:**
    - Add `firstName: string` to the `ParticipantData` interface
    - Per locked decision: Teacher dashboard shows "Speedy Penguin (Jake M)" format
    - Update the participant display in the `<li>` element: change `{p.funName}` to `{p.funName}{p.firstName ? ` (${p.firstName})` : ''}`
    - Use a conditional to handle participants that may not have a firstName yet (from before Phase 20 migration -- though data was wiped in Phase 19, it's defensive coding)
    - Keep all existing functionality (connected dots, banned state, management controls)

    **4. Update `src/components/teacher/participation-sidebar.tsx`:**
    - Add `firstName?: string` to the participant type in `ParticipationSidebarProps`: currently `Array<{ id: string; funName: string }>`
    - Update the display in the student tile grid: change `{participant.funName}` to a two-line display or inline format: fun name on first line, real name in smaller text below, e.g.:
      ```
      <span className="truncate">{participant.funName}</span>
      {participant.firstName && (
        <span className="truncate text-[10px] text-muted-foreground">{participant.firstName}</span>
      )}
      ```
    - Wrap the content in a flex-col layout within each tile to stack the two names
    - Keep the status indicator dot (connected/voted/disconnected) on the first line with funName
    - The tile may need slightly more height -- adjust py from 1.5 to 2 if the two lines don't fit
    - Note: The caller of ParticipationSidebar (bracket live page) will need to include firstName in its participant data. Check `src/app/(dashboard)/brackets/[bracketId]/live/page.tsx` -- if it passes participants from a query that already includes all fields, no change needed. If it selects specific fields, add firstName. **Only modify the bracket live page if it explicitly selects participant fields and excludes firstName.**
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all teacher components compile. Check that the participant data flows correctly: server page -> session-detail -> student-roster.
  </verify>
  <done>
    - Teacher session detail page serializes firstName for each participant
    - StudentRoster shows "Fun Name (Real Name)" format
    - ParticipationSidebar shows fun name with real name below in smaller text
    - All TypeScript types updated consistently across the data flow
  </done>
</task>

<task type="auto">
  <name>Task 2: Add edit-name capability and update session layout for firstName</name>
  <files>
    src/components/student/edit-name-dialog.tsx
    src/components/student/session-header.tsx
    src/app/(student)/session/[sessionId]/layout.tsx
  </files>
  <action>
    **1. Create `src/components/student/edit-name-dialog.tsx`:**
    - 'use client' component
    - Props: `{ participantId: string; currentFirstName: string; onNameUpdated: (newName: string) => void }`
    - Renders as a `DropdownMenuItem` that opens a dialog (use shadcn `Dialog` component -- import from `@/components/ui/dialog`)
    - Dialog content: title "Edit Your Name", input field pre-filled with `currentFirstName`, Save/Cancel buttons
    - Input validation: use `validateFirstName` from `@/lib/validations/first-name` for client-side validation. Show inline error messages.
    - On save: call `updateParticipantName({ participantId, firstName: newName })` server action
    - Handle error response (e.g., "That name is already taken in this session. Try a different name.") -- show as inline error in dialog
    - On success: call `onNameUpdated(newName)` callback, close dialog
    - Loading state on Save button during server action call
    - Pattern follows existing RecoveryCodeDialog style (DropdownMenuItem that triggers a dialog)

    **2. Update `src/components/student/session-header.tsx`:**
    - Add `firstName` to `SessionHeaderProps` interface: `firstName?: string`
    - Import and render `EditNameDialog` in the DropdownMenuContent, between RerollButton and RecoveryCodeDialog
    - Pass `participantId`, `currentFirstName: firstName`, and `onNameUpdated` callback
    - The `onNameUpdated` callback should:
      - Update localStorage: find the `sparkvotedu_session_*` entry for this participant and update the `firstName` field
      - Optionally show a brief toast or visual confirmation (discretion area -- keep it simple, just update the state)
    - Add state management for firstName (similar to how funName is managed with useState)

    **3. Update `src/app/(student)/session/[sessionId]/layout.tsx`:**
    - Add `firstName` to the `ParticipantStore` interface: `firstName?: string` (optional for backward compat with existing localStorage entries that don't have it)
    - Pass `firstName` to `SessionHeader` component: `firstName={participant.firstName}`
    - The join flow (Plan 02) stores firstName in localStorage when creating the `sparkvotedu_session_*` entry. This layout reads it back.
    - Handle case where localStorage entry doesn't have firstName (older sessions) -- pass undefined, session header handles it gracefully
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all components compile. Verify that EditNameDialog follows the same pattern as RecoveryCodeDialog (DropdownMenuItem + Dialog).
  </verify>
  <done>
    - EditNameDialog component exists with validation, server action call, and error handling
    - Session header settings dropdown shows "Edit Name" option (alongside Reroll and Recovery Code)
    - Name edit validates input and rejects duplicate names with clear error message
    - Session layout reads and passes firstName from localStorage to session header
    - localStorage is updated when name is edited
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Teacher roster: each participant shows "Fun Name (Real Name)" format
3. Participation sidebar: each tile shows fun name with real name in smaller text
4. Student settings dropdown: "Edit Name" option visible between Reroll and Recovery Code
5. Edit name dialog: validates input, calls updateParticipantName, shows errors for duplicates
6. Session layout: reads firstName from localStorage and passes to header
7. Data flow verified: server page serializes firstName -> session-detail passes it -> roster displays it
</verification>

<success_criteria>
- Teacher sees "Speedy Penguin (Jake M)" in participant list (per locked decision)
- Students see only their fun name in the UI (no real name shown to other students -- per locked decision)
- Student can edit their name via settings dropdown (IDENT-06)
- Name edit rejects duplicates with clear error message
- Session layout supports firstName in localStorage shape
</success_criteria>

<output>
After completion, create `.planning/phases/20-name-based-student-identity/20-03-SUMMARY.md`
</output>
