---
phase: 20-name-based-student-identity
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/student/join-form.tsx
  - src/components/student/name-entry-form.tsx
  - src/components/student/name-disambiguation.tsx
  - src/components/student/welcome-screen.tsx
  - src/components/student/home-join-input.tsx
  - src/app/(student)/join/page.tsx
  - src/app/(student)/join/[code]/page.tsx
  - src/app/(student)/session/[sessionId]/welcome/page.tsx
autonomous: true

must_haves:
  truths:
    - "Student can enter a 6-digit session code and then enter their first name on a second screen"
    - "Session code auto-fills from localStorage on same device (fail-silent if unavailable)"
    - "When a duplicate name is found, student sees 'Is this you?' disambiguation with fun names listed"
    - "Student can claim an existing identity (rejoin) or differentiate as a new person"
    - "For 3+ duplicates, all existing matches are listed for claim or differentiation"
    - "After joining, student sees a welcome screen showing their fun name assignment"
    - "Returning students see 'Welcome back!' with their existing fun name"
    - "Ended sessions show final results rather than 'session ended' error"
  artifacts:
    - path: "src/components/student/join-form.tsx"
      provides: "Step 1: session code entry (no device identity, stores code in localStorage)"
      min_lines: 40
    - path: "src/components/student/name-entry-form.tsx"
      provides: "Step 2: name input with validation, calls joinSessionByName"
      min_lines: 60
    - path: "src/components/student/name-disambiguation.tsx"
      provides: "'Is this you?' UI showing duplicate name candidates with claim/differentiate actions"
      min_lines: 50
    - path: "src/components/student/welcome-screen.tsx"
      provides: "Updated welcome showing 'You're now [Fun Name]!' with real-name-to-fun-name mapping"
      min_lines: 50
  key_links:
    - from: "src/components/student/join-form.tsx"
      to: "src/app/(student)/join/[code]/page.tsx"
      via: "router.push to /join/[code] after code validation"
      pattern: "router.push.*join"
    - from: "src/components/student/name-entry-form.tsx"
      to: "src/actions/student.ts"
      via: "joinSessionByName server action call"
      pattern: "joinSessionByName"
    - from: "src/components/student/name-disambiguation.tsx"
      to: "src/actions/student.ts"
      via: "claimIdentity server action call"
      pattern: "claimIdentity"
    - from: "src/components/student/name-entry-form.tsx"
      to: "src/components/student/name-disambiguation.tsx"
      via: "renders disambiguation when duplicates returned"
      pattern: "NameDisambiguation"
---

<objective>
Build the two-step student join flow UI: code entry -> name entry -> disambiguation -> welcome screen.

Purpose: Replace the single-step device-fingerprint join form with the name-based two-step flow per locked decisions. Students enter session code first, then their name. Duplicate names trigger an "Is this you?" disambiguation flow. Welcome screen shows fun name assignment.

Output: Rewritten join-form.tsx (code-only), new name-entry-form.tsx, new name-disambiguation.tsx, updated welcome-screen.tsx, and updated route pages.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-name-based-student-identity/20-01-SUMMARY.md
@src/components/student/join-form.tsx
@src/components/student/welcome-screen.tsx
@src/components/student/home-join-input.tsx
@src/app/(student)/join/page.tsx
@src/app/(student)/join/[code]/page.tsx
@src/app/(student)/session/[sessionId]/welcome/page.tsx
@src/lib/validations/first-name.ts
@src/types/student.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite join-form and create name entry + disambiguation components</name>
  <files>
    src/components/student/join-form.tsx
    src/components/student/name-entry-form.tsx
    src/components/student/name-disambiguation.tsx
  </files>
  <action>
    **1. Rewrite `src/components/student/join-form.tsx` as Step 1 (code-only entry):**
    - Remove `useDeviceIdentity` hook dependency entirely -- no more device identity in join flow
    - Keep the 6-digit code input with same styling (rounded-xl, text-4xl, tracking, shake animation)
    - On load, try to auto-fill from `localStorage.getItem('sparkvotedu_last_session_code')` -- fail-silent with try/catch (per locked decision: fail-silent if localStorage unavailable)
    - On submit: validate 6-digit format client-side, then navigate to `/join/${code}` (the [code] route will handle Step 2)
    - Remove the "Preparing your device identity..." message since device identity is no longer needed
    - Remove `ready` state gating -- the form is immediately ready
    - Keep loading state and error display for the navigation
    - Button text: "Next" (not "Join Session" -- joining happens after name entry)

    **2. Create `src/components/student/name-entry-form.tsx` as Step 2 (name entry):**
    - Props: `{ code: string; sessionInfo: { id: string; name: string | null; teacherName: string | null; status: string } }`
    - Show session context above the input: "Joining {teacherName}'s class" or "Joining {sessionName}" or "Joining session" (discretion: show session info when available)
    - Name input: single text field, `autoFocus`, placeholder "Your first name", standard input styling consistent with the code input
    - Client-side validation using `validateFirstName` from `@/lib/validations/first-name` for instant feedback (min 2 chars, emoji rejection, profanity)
    - On submit: call `joinSessionByName({ code, firstName })` server action
    - Handle response cases:
      - `result.error` -> show error message with shake animation
      - `result.duplicates` -> switch to disambiguation mode (render `NameDisambiguation` component inline, passing duplicates, code, firstName, and sessionInfo)
      - `result.participant && result.session` -> success! Store participant in localStorage as `sparkvotedu_session_{sessionId}` (same shape as existing but add firstName). Also store session code: `localStorage.setItem('sparkvotedu_last_session_code', code)`. Navigate to welcome page.
      - `result.sessionEnded` -> navigate to `/session/{sessionId}` to show results (per locked decision: ended sessions show results)
    - Loading spinner on button while submitting
    - Back button/link to return to code entry (`/join`)

    **3. Create `src/components/student/name-disambiguation.tsx` ("Is this you?" flow):**
    - Props: `{ duplicates: Array<{ id: string; funName: string }>; firstName: string; code: string; sessionInfo: { id: string; name: string | null; teacherName: string | null; status: string } }`
    - Heading: "Someone with that name is already here" (for 1 match) or "Multiple students with that name are here" (for 2+ matches)
    - List each duplicate as a card/button showing the fun name: "Are you [Speedy Penguin]?" with a "That's me!" button
    - Per locked decision (discretion: rejoin confirmation approach): When student taps "That's me" on a candidate, show confirmation: "You'll rejoin as [Speedy Penguin]. Is that right?" with Confirm/Cancel buttons. On confirm, call `claimIdentity({ participantId, sessionCode: code })`. On success, store in localStorage and navigate to welcome page with `returning=true`.
    - Below the candidate list, show "I'm someone different" button that reveals a text input pre-filled with the entered name and prompt: "Add something to make your name unique (like a last initial)". The input uses `firstNameSchema` validation. On submit, call `joinSessionByName({ code, firstName: newName })` which will either succeed (new unique name) or return more duplicates (unlikely but handle recursively).
    - Handle loading states for all async operations
    - Handle errors from claimIdentity (e.g., banned student: "You have been removed from this session")
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all components compile. Verify no imports reference `useDeviceIdentity` in join-form.tsx.
  </verify>
  <done>
    - join-form.tsx is code-only entry with localStorage auto-fill, no device identity dependency
    - name-entry-form.tsx shows session context, validates name client-side, calls joinSessionByName
    - name-disambiguation.tsx shows "Is this you?" with fun name candidates, supports claim and differentiation
    - All three components compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update routes, welcome screen, and home-join-input for two-step flow</name>
  <files>
    src/app/(student)/join/page.tsx
    src/app/(student)/join/[code]/page.tsx
    src/app/(student)/session/[sessionId]/welcome/page.tsx
    src/components/student/welcome-screen.tsx
    src/components/student/home-join-input.tsx
  </files>
  <action>
    **1. Update `src/app/(student)/join/page.tsx`:**
    - Keep the same visual structure (logo, title, subtitle)
    - Change subtitle from "Enter your 6-digit class code to get started" to "Enter your class code to get started"
    - JoinForm now only handles code entry -- no changes needed to how it's rendered
    - Remove the `searchParams` code pre-fill -- the JoinForm handles localStorage auto-fill internally now

    **2. Rewrite `src/app/(student)/join/[code]/page.tsx` as the name entry step:**
    - This page is now Step 2 of the join flow
    - Server component that validates the code format (keep existing 6-digit regex check)
    - On valid code: call `findSessionByCode(code)` server-side to validate the session exists and get session info
    - If session not found: show error with a fallback JoinForm for re-entry
    - If session found: render `NameEntryForm` component with `code` and `sessionInfo` props (sessionInfo: `{ id, name, teacherName: session.teacher.name, status: session.status }`)
    - Page layout: logo + "Enter your name" heading + NameEntryForm component
    - If session is ended: still render NameEntryForm but pass status 'ended' -- the name entry form handles ended-session flow

    **3. Update `src/app/(student)/session/[sessionId]/welcome/page.tsx`:**
    - Keep existing structure reading from searchParams
    - No major changes needed -- the welcome page already reads `name`, `returning`, `teacher` from search params
    - Ensure it works with the new navigation pattern from NameEntryForm

    **4. Update `src/components/student/welcome-screen.tsx`:**
    - Per locked decision: "Brief welcome screen after joining showing fun name assignment"
    - For new students: update text from just showing the fun name to "You're now {funName}!" to emphasize the name-to-fun-name transformation
    - Keep the 3-second countdown auto-redirect for new students
    - For returning students: keep "Welcome back!" with "Rejoin Session" button (existing behavior is correct)
    - Keep all existing Motion animations -- no changes needed to animation approach (per discretion: keep existing 3-second countdown)
    - Keep the `teacherName` context display

    **5. Update `src/components/student/home-join-input.tsx`:**
    - Currently navigates to `/join/{code}` -- this is correct since that route now shows Step 2
    - No functional changes needed, but verify the redirect target works with the new Step 2 page
    - Keep the existing compact inline form design
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all pages and components compile. Manually trace the flow: /join -> enter code -> /join/[code] -> enter name -> joinSessionByName -> /session/[id]/welcome.
  </verify>
  <done>
    - /join page renders code-only JoinForm with localStorage auto-fill
    - /join/[code] page validates session server-side and renders NameEntryForm
    - NameEntryForm calls joinSessionByName, handles duplicates and success
    - Welcome screen shows "You're now [Fun Name]!" for new students
    - HomeJoinInput still works correctly with the two-step flow
    - Full join flow works end-to-end: code -> name -> disambiguation (if needed) -> welcome
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Full join flow traceable: /join -> code entry -> /join/[code] -> name entry -> welcome page
3. Disambiguation flow works: duplicate name -> "Is this you?" -> claim (rejoin) or differentiate (new name)
4. No references to `useDeviceIdentity` in join-form.tsx
5. localStorage auto-fill for session code works (fail-silent if unavailable)
6. Welcome screen shows fun name assignment for new students and "Welcome back!" for returning students
7. Ended sessions handled: student entering code for ended session sees results (not error)
</verification>

<success_criteria>
- Two-step join flow works: code first, then name (per locked decision)
- Duplicate names trigger "Is this you?" disambiguation (per locked decision)
- Fun name shown on welcome screen after joining (per locked decision)
- Returning students see their preserved identity with all previous data intact
- No device fingerprint dependency in the join flow
</success_criteria>

<output>
After completion, create `.planning/phases/20-name-based-student-identity/20-02-SUMMARY.md`
</output>
