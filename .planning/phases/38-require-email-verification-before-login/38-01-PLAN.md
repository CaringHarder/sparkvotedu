---
phase: 38-require-email-verification-before-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/auth.ts
  - src/app/proxy.ts
  - src/app/auth/callback/route.ts
autonomous: true

must_haves:
  truths:
    - "signUp action returns redirectToVerify with the email after successful signup instead of inline success message"
    - "signIn action detects email_not_confirmed error and returns redirectToVerify with the email instead of showing generic error"
    - "resendVerification server action calls supabase.auth.resend with type signup and returns success or error"
    - "/verify-email is recognized as an auth page by the proxy and accessible to unauthenticated users"
    - "Expired verification links redirect to /verify-email?expired=true&email=xxx instead of /login?error=auth_callback_failed"
  artifacts:
    - path: "src/actions/auth.ts"
      provides: "Modified signUp, signIn actions + new resendVerification action"
      contains: "resendVerification"
    - path: "src/app/proxy.ts"
      provides: "Updated AUTH_PAGES array"
      contains: "/verify-email"
    - path: "src/app/auth/callback/route.ts"
      provides: "Expired link detection and redirect"
      contains: "expired=true"
  key_links:
    - from: "src/actions/auth.ts"
      to: "supabase.auth.signInWithPassword"
      via: "email_not_confirmed error interception"
      pattern: "email_not_confirmed|Email not confirmed"
    - from: "src/actions/auth.ts"
      to: "supabase.auth.resend"
      via: "resendVerification server action"
      pattern: "auth\\.resend"
    - from: "src/app/auth/callback/route.ts"
      to: "/verify-email"
      via: "expired link redirect"
      pattern: "verify-email.*expired"
---

<objective>
Modify server-side auth actions and routing to support email verification enforcement.

Purpose: Lay the backend foundation for the email verification blocking flow. The signUp action must redirect to the verification page instead of showing inline text. The signIn action must intercept the email_not_confirmed error and redirect instead of showing a generic error. A new resendVerification action enables the resend button. The proxy must allow unauthenticated access to /verify-email. The auth callback must handle expired verification links gracefully.

Output: Modified src/actions/auth.ts (signUp, signIn, resendVerification), updated proxy.ts, updated callback/route.ts
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-require-email-verification-before-login/38-RESEARCH.md
@src/actions/auth.ts
@src/app/proxy.ts
@src/app/auth/callback/route.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify auth actions (signUp, signIn, resendVerification)</name>
  <files>src/actions/auth.ts</files>
  <action>
Modify the signUp action:
- Change the return type to include `redirectToVerify?: string` alongside existing `error?` and `success?`
- After successful `supabase.auth.signUp()`, instead of returning `{ success: 'Check your email...' }`, return `{ redirectToVerify: parsed.data.email }` so the client component can redirect to `/verify-email?email=xxx`
- Keep the error handling unchanged

Modify the signIn action:
- Change the return type to include `redirectToVerify?: string` alongside existing `error?`
- After `signInWithPassword` returns an error, check if `error.message` contains 'Email not confirmed' (case-insensitive check recommended, or match exact Supabase string 'Email not confirmed')
- If email_not_confirmed: return `{ redirectToVerify: parsed.data.email }` instead of `{ error: error.message }`
- All other errors continue returning `{ error: error.message }` as before

Add new resendVerification server action:
```typescript
export async function resendVerification(
  _prevState: { error?: string; success?: boolean; sentAt?: number } | null,
  formData: FormData
) {
  const email = formData.get('email') as string
  if (!email) return { error: 'Email is required' }

  const supabase = await createClient()
  const { error } = await supabase.auth.resend({
    type: 'signup',
    email,
    options: {
      emailRedirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/auth/callback`,
    },
  })

  if (error) return { error: error.message }
  return { success: true, sentAt: Date.now() }
}
```
  </action>
  <verify>Run `npx tsc --noEmit` and confirm no type errors in src/actions/auth.ts. Verify all three exports (signUp, signIn, resendVerification) are present.</verify>
  <done>signUp returns redirectToVerify on success, signIn returns redirectToVerify on email_not_confirmed error, resendVerification action exists and calls supabase.auth.resend</done>
</task>

<task type="auto">
  <name>Task 2: Update proxy AUTH_PAGES and callback expired link handling</name>
  <files>src/app/proxy.ts, src/app/auth/callback/route.ts</files>
  <action>
In src/app/proxy.ts:
- Add '/verify-email' to the AUTH_PAGES array: `const AUTH_PAGES = ['/login', '/signup', '/forgot-password', '/update-password', '/auth', '/verify-email']`
- This ensures unauthenticated users can access the verify-email page without being redirected to /login
- Also ensures authenticated users on the verify-email page get redirected to /dashboard (correct behavior -- if they're already verified and have a session, they don't need this page)

In src/app/auth/callback/route.ts:
- When `exchangeCodeForSession` returns an error (the `if (code)` block where `error` is truthy), check if the error indicates an expired or invalid token
- Supabase may return different error messages for expired tokens. Check for common patterns: error.message containing 'expired', 'invalid', or error.code being 'otp_expired'
- If the error looks like an expired/invalid verification link, extract the user's email if possible from the error context (may not be available -- if not, redirect without email)
- Redirect to `/verify-email?expired=true` (and `&email=xxx` if email is available) instead of `/login?error=auth_callback_failed`
- For truly unexpected errors (not expiration-related), keep the existing redirect to `/login?error=auth_callback_failed`
- Practical approach: Since Supabase may not always distinguish expired from invalid, treat ALL code exchange failures as potential expired links and redirect to `/verify-email?expired=true`. This is safe because the verify-email page provides a resend button, which is the correct action regardless of whether the link was expired or corrupted.
  </action>
  <verify>Run `npx tsc --noEmit` and confirm no type errors. Verify '/verify-email' appears in AUTH_PAGES array. Verify callback route redirects to /verify-email on error.</verify>
  <done>Proxy allows unauthenticated access to /verify-email. Callback redirects expired/failed verification links to /verify-email?expired=true instead of generic login error.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `grep -n 'verify-email' src/app/proxy.ts` shows the route in AUTH_PAGES
- `grep -n 'resendVerification' src/actions/auth.ts` shows the new action
- `grep -n 'redirectToVerify' src/actions/auth.ts` shows both signUp and signIn returning this field
- `grep -n 'expired' src/app/auth/callback/route.ts` shows expired link handling
</verification>

<success_criteria>
- signUp action returns `{ redirectToVerify: email }` on success (not inline success text)
- signIn action returns `{ redirectToVerify: email }` when Supabase returns email_not_confirmed error
- resendVerification action exists and calls `supabase.auth.resend({ type: 'signup' })`
- /verify-email is in AUTH_PAGES and accessible to unauthenticated users
- Failed verification link clicks redirect to /verify-email?expired=true
- All existing auth functionality (login, signup errors, password reset, sign out) unchanged
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-require-email-verification-before-login/38-01-SUMMARY.md`
</output>
