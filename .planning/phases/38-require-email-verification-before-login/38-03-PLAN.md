---
phase: 38-require-email-verification-before-login
plan: 03
type: execute
wave: 2
depends_on: ["38-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "New email signup shows branded verify-email page with correct email address"
    - "Resend button sends verification email and shows 60-second cooldown"
    - "Unverified email login attempt redirects to verify-email page"
    - "Google OAuth sign-in bypasses verification entirely"
    - "Verification link in email auto-logs teacher into dashboard"
    - "Existing verified teacher can log in normally without any change in experience"
---

<objective>
Verify the complete email verification flow end-to-end across all user paths.

Purpose: Ensure all locked decisions are correctly implemented and the flow works for new signups, unverified login attempts, Google OAuth, resend, expired links, and existing grandfathered users.

Output: Verified flow, any issues documented for fix
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-require-email-verification-before-login/38-RESEARCH.md
@.planning/phases/38-require-email-verification-before-login/38-01-SUMMARY.md
@.planning/phases/38-require-email-verification-before-login/38-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete email verification flow</name>
  <files>src/app/(auth)/verify-email/page.tsx, src/components/auth/verify-email-card.tsx, src/components/auth/signup-form.tsx, src/components/auth/login-form.tsx, src/actions/auth.ts, src/app/proxy.ts, src/app/auth/callback/route.ts</files>
  <action>
Human verification of the complete email verification enforcement flow built in plans 38-01 and 38-02:
- Branded verify-email blocking page with resend, cooldown, Google sign-in, sign out
- Signup redirects to verify-email page instead of showing inline text
- Login with unverified email redirects to verify-email page
- Expired verification links redirect to verify-email with "link expired" message
- Google OAuth bypasses verification entirely
- Existing verified accounts log in normally (grandfathered)

Test the following 10 scenarios:

**Test 1: New Email Signup** -- Go to /signup, create a new account. After submitting, verify redirect to /verify-email with full email address, resend button, Google sign-in, sign out link, and branded styling.

**Test 2: Resend with Cooldown** -- Click "Resend verification email". Verify button shows "Resend in 60s" countdown and is disabled during cooldown.

**Test 3: Unverified Login Attempt** -- Go to /login, sign in with unverified email. Verify redirect to /verify-email (not generic error).

**Test 4: Verification Link Click** -- Check inbox, click verification link. Verify auto-login to dashboard.

**Test 5: Google OAuth Bypass** -- Sign out, sign in with Google. Verify normal dashboard access (no verification page).

**Test 6: Existing Account Login** -- Sign out, log in with verified email. Verify normal login (completely invisible).

**Test 7: Already-Verified Re-click** -- Click verification link again. Verify silent redirect to dashboard.

**Test 8: Expired Link** -- Navigate to /verify-email?expired=true&email=test@example.com. Verify "link expired" UI with resend option.

**Test 9: Sign Out from Verify Page** -- Navigate to /verify-email, click "Sign out". Verify redirect to /login.

**Test 10: Google Sign-In from Verify Page** -- On /verify-email, click "Sign in with Google instead". Verify OAuth flow initiates.

Resume signal: Type "approved" if all tests pass, or describe which tests failed and what went wrong.
  </action>
  <verify>All 10 test scenarios pass without errors.</verify>
  <done>Complete email verification flow verified: new signups see branded verify page, resend works with cooldown, unverified login redirects, Google OAuth bypasses, verification link auto-logs in, existing accounts unaffected, expired links handled.</done>
</task>

</tasks>

<verification>
All 10 test scenarios pass without errors.
</verification>

<success_criteria>
- New email signups see branded verify-email page (not inline text)
- Resend button works with 60-second cooldown
- Unverified login redirects to verify-email page
- Google OAuth bypasses verification
- Verification link auto-logs in to dashboard
- Existing accounts unaffected
- Expired links show appropriate message
</success_criteria>

<output>
After completion, create `.planning/phases/38-require-email-verification-before-login/38-03-SUMMARY.md`
</output>
