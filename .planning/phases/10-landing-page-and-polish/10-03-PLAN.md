---
phase: 10-landing-page-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/components/dashboard/sidebar-nav.tsx
  - src/components/dashboard/shell.tsx
  - src/components/dashboard/dashboard-skeleton.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/loading.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher dashboard has a visually refreshed design with brand colors, modern card styles, and clear visual hierarchy"
    - "Dashboard sidebar shows the SparkVotEDU logo and has upgraded visual treatment with brand accent colors"
    - "Dark mode toggle is accessible in the dashboard header"
    - "Loading states use skeleton screens instead of spinners"
    - "Auth pages display the SparkVotEDU logo instead of plain text"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Redesigned dashboard layout with logo, theme toggle, and branded header"
      min_lines: 25
    - path: "src/components/dashboard/sidebar-nav.tsx"
      provides: "Visually refreshed sidebar with brand colors and improved active states"
      min_lines: 60
    - path: "src/components/dashboard/shell.tsx"
      provides: "Redesigned dashboard home with modern card styles and visual hierarchy"
      min_lines: 80
    - path: "src/components/dashboard/dashboard-skeleton.tsx"
      provides: "Skeleton loading state for dashboard home"
      exports: ["DashboardSkeleton"]
    - path: "src/app/(dashboard)/dashboard/loading.tsx"
      provides: "Next.js loading.tsx using DashboardSkeleton"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "ThemeToggle in header"
      pattern: "ThemeToggle"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "public/logo-icon.png"
      via: "next/image for logo in header"
      pattern: "logo-icon"
---

<objective>
Visually refresh the teacher dashboard with brand colors, modern card styles, upgraded typography, skeleton loading states, and dark mode toggle integration. This is a visual refresh per user decision — rethink styles and hierarchy while keeping the same structure.

Purpose: The teacher dashboard should feel sleek and intuitive with a consistent design language that reflects the SparkVotEDU brand.
Output: Redesigned dashboard layout, sidebar, home shell, skeleton loading, and branded auth pages.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-and-polish/10-RESEARCH.md
@.planning/phases/10-landing-page-and-polish/10-01-SUMMARY.md
@src/app/(dashboard)/layout.tsx
@src/components/dashboard/sidebar-nav.tsx
@src/components/dashboard/shell.tsx
@src/app/(auth)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard layout redesign with logo, theme toggle, and branded sidebar</name>
  <files>
    src/app/(dashboard)/layout.tsx
    src/components/dashboard/sidebar-nav.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **Dashboard layout** — Redesign `src/app/(dashboard)/layout.tsx`:
       - Header: Replace plain "SparkVotEDU" text with logo icon (next/image, `/logo-icon.png`, ~28px) + "SparkVotEDU" text in brand font weight
       - Add ThemeToggle component next to SignOutButton in the header
       - Upgrade header styling: subtle brand-blue accent (e.g., thin top border or subtle gradient underline)
       - Keep existing structure: header + sidebar + main content area
       - Sidebar stays `hidden md:block` for now (responsive nav is plan 10-05)
       - Add subtle visual elevation to header (shadow-sm or border treatment)

    2. **Sidebar navigation** — Redesign `src/components/dashboard/sidebar-nav.tsx`:
       - Add SparkVotEDU icon logo at top of sidebar (small, above nav items) or use the existing header logo for branding
       - Upgrade active state: Use brand-blue background tint (bg-brand-blue/10) with brand-blue text instead of generic bg-accent
       - Upgrade hover states: Subtle brand-blue hover (bg-brand-blue/5)
       - Add visual polish: Better spacing, icon sizing, typography weight
       - Improve the Activities section indentation and sub-item styling
       - Keep the same information architecture: Dashboard, Sessions, Activities (Brackets, Polls), Billing, Analytics
       - Consider adding a subtle separator between main nav items and the Activities section
       - Ensure all colors work in both light and dark modes

    Design language: The sidebar should feel modern and branded. Use brand-blue for interactive/active states, maintain clean whitespace, and ensure icons are well-sized (h-4 w-4 minimum for touch targets).
  </action>
  <verify>
    - `grep "ThemeToggle" src/app/(dashboard)/layout.tsx` confirms theme toggle in header
    - `grep "logo-icon" src/app/(dashboard)/layout.tsx` confirms logo usage
    - `grep "brand-blue" src/components/dashboard/sidebar-nav.tsx` confirms brand color usage
    - No hardcoded color values — all through Tailwind classes
  </verify>
  <done>
    Dashboard header shows logo + "SparkVotEDU" text + ThemeToggle + SignOutButton. Sidebar has branded active/hover states using brand-blue. Both support light and dark modes. Same IA preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard home shell redesign, skeleton loading, and auth page branding</name>
  <files>
    src/components/dashboard/shell.tsx
    src/components/dashboard/dashboard-skeleton.tsx
    src/app/(dashboard)/dashboard/loading.tsx
    src/app/(dashboard)/dashboard/page.tsx
    src/app/(auth)/layout.tsx
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/page.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **DashboardShell redesign** — Edit `src/components/dashboard/shell.tsx`:
       - Upgrade the welcome section: Larger heading, add subtle brand accent (e.g., "Welcome" in brand-blue)
       - Redesign the card grid: Modern card styles with subtle gradients, better shadows (shadow-sm), rounded-xl corners
       - "Create Session" card: Add brand-blue gradient or accent border, make it visually prominent as the primary CTA
       - "Plan & Usage" card: Cleaner layout with progress indicator for limits (use small bars or fraction display)
       - Active sessions section: Upgrade session cards with status indicators, brand-amber accent for session codes
       - Empty state: Add an illustration-style empty state with brand colors (use Lucide icons creatively)
       - Add subtle visual hierarchy through size, weight, and color differentiation
       - Keep all existing functionality (sessions, billing info, links) — just upgrade the visual treatment

    2. **Dashboard skeleton** — Create `src/components/dashboard/dashboard-skeleton.tsx`:
       - Use shadcn Skeleton component (from 10-01)
       - Mirror the exact layout of DashboardShell: header skeleton, 2-card grid skeleton, session cards skeleton
       - Match dimensions: same grid columns, card heights, spacing
       - Export as `DashboardSkeleton`

    3. **Dashboard loading.tsx** — Create `src/app/(dashboard)/dashboard/loading.tsx`:
       - Import and render `DashboardSkeleton`
       - This is Next.js App Router convention for loading states

    4. **Dashboard page** — If `src/app/(dashboard)/dashboard/page.tsx` doesn't use Suspense with DashboardShell, wrap it:
       - `<Suspense fallback={<DashboardSkeleton />}><DashboardShell /></Suspense>`

    5. **Auth pages branding** — Update auth layout and pages:
       - `src/app/(auth)/layout.tsx`: Add SparkVotEDU logo image (next/image, `/logo-horizontal.png` or `/logo-icon.png`) above the auth card
       - Update background to use a subtle brand-blue tint or gradient
       - Login/signup pages: If they show "SparkVotEDU" as text, replace with logo image
       - Ensure auth pages look good in both light and dark modes
  </action>
  <verify>
    - `npm run build` succeeds
    - `grep "Skeleton" src/components/dashboard/dashboard-skeleton.tsx` confirms skeleton usage
    - `grep "DashboardSkeleton" src/app/(dashboard)/dashboard/loading.tsx` confirms loading state
    - `grep "logo" src/app/(auth)/layout.tsx` confirms auth branding
    - Shell uses brand-* color classes for visual accents
  </verify>
  <done>
    Dashboard home has modernized cards with brand accents, better visual hierarchy, and upgraded typography. Skeleton loading screens replace spinners. Auth pages show SparkVotEDU logo. All dark-mode compatible.
  </done>
</task>

</tasks>

<verification>
- Dashboard layout shows logo in header with theme toggle
- Sidebar has branded active/hover states
- Dashboard home cards have modern styling with brand colors
- Loading state shows skeleton screens (not spinners or "Loading..." text)
- Auth pages display logo image instead of plain text
- All views look correct in both light and dark modes
- Build passes with zero errors
</verification>

<success_criteria>
1. Teacher dashboard header displays SparkVotEDU logo and dark mode toggle
2. Sidebar navigation uses brand colors for active/hover states
3. Dashboard home has modern card design with visual hierarchy and brand accents
4. Loading states use skeleton screens
5. Auth pages are branded with logo
6. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-and-polish/10-03-SUMMARY.md`
</output>
