---
phase: 10-landing-page-and-polish
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(student)/layout.tsx
  - src/app/(student)/join/page.tsx
  - src/components/student/join-form.tsx
  - src/components/student/welcome-screen.tsx
  - src/components/student/session-header.tsx
  - src/components/student/activity-card.tsx
  - src/app/(student)/session/[sessionId]/layout.tsx
  - src/components/bracket/celebration-screen.tsx
  - src/components/bracket/winner-reveal.tsx
  - src/components/bracket/podium-celebration.tsx
autonomous: true

must_haves:
  truths:
    - "Student layout shows the SparkVotEDU logo instead of plain text"
    - "Student join flow is clean, focused, and uses brand colors"
    - "Celebration and winner reveal screens have enhanced, more impactful animations"
    - "Student session loading states use skeleton screens instead of plain text"
    - "Student interface prioritizes clarity with minimal distractions"
  artifacts:
    - path: "src/app/(student)/layout.tsx"
      provides: "Branded student layout with logo image"
      min_lines: 15
    - path: "src/components/student/welcome-screen.tsx"
      provides: "Polished welcome screen with brand colors and smooth animations"
      min_lines: 40
    - path: "src/components/bracket/celebration-screen.tsx"
      provides: "Enhanced celebration with brand colors and more impactful animations"
      min_lines: 80
    - path: "src/components/bracket/winner-reveal.tsx"
      provides: "Enhanced winner reveal with more dramatic reveal sequence"
      min_lines: 80
  key_links:
    - from: "src/app/(student)/layout.tsx"
      to: "public/logo-icon.png"
      via: "next/image for student branding"
      pattern: "logo-icon"
    - from: "src/components/bracket/celebration-screen.tsx"
      to: "canvas-confetti"
      via: "Enhanced confetti with brand colors"
      pattern: "confetti"
---

<objective>
Polish the student experience: branded layout, clean join flow, enhanced celebration screens, and skeleton loading states. The student interface should be simple, require zero instruction to use, and prioritize clarity over excitement.

Purpose: Students interact with SparkVotEDU briefly but intensely during class. The interface must be instantly understandable and the celebration moments must be exciting enough to energize the classroom.
Output: Polished student layout, join flow, session views, and celebration/winner reveal screens.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-and-polish/10-RESEARCH.md
@.planning/phases/10-landing-page-and-polish/10-01-SUMMARY.md
@src/app/(student)/layout.tsx
@src/app/(student)/join/page.tsx
@src/components/student/join-form.tsx
@src/components/student/welcome-screen.tsx
@src/components/student/session-header.tsx
@src/components/student/activity-card.tsx
@src/app/(student)/session/[sessionId]/layout.tsx
@src/components/bracket/celebration-screen.tsx
@src/components/bracket/winner-reveal.tsx
@src/components/bracket/podium-celebration.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Student layout branding, join flow polish, and session skeleton loading</name>
  <files>
    src/app/(student)/layout.tsx
    src/app/(student)/join/page.tsx
    src/components/student/join-form.tsx
    src/components/student/welcome-screen.tsx
    src/components/student/session-header.tsx
    src/components/student/activity-card.tsx
    src/app/(student)/session/[sessionId]/layout.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **Student layout** — Edit `src/app/(student)/layout.tsx`:
       - Replace plain "SparkVotEDU" text with logo icon image (next/image, `/logo-icon.png`, ~24px)
       - Add subtle brand-blue accent (e.g., thin top border line in brand-blue)
       - Keep layout minimal and clean — students need focus, not chrome
       - Ensure the layout works equally well on mobile and desktop

    2. **Join flow** — Polish `src/app/(student)/join/page.tsx` and `src/components/student/join-form.tsx`:
       - Make the 6-digit code input the dominant UI element (per research recommendation)
       - Add SparkVotEDU logo above the code input
       - Use brand colors for the submit button (brand-blue background)
       - Ensure generous touch targets on mobile (min 44px)
       - Clean transitions between states (idle -> submitting -> success)
       - Keep existing flow structure — just polish the visual treatment

    3. **Welcome screen** — Polish `src/components/student/welcome-screen.tsx`:
       - Add brand color accents to the welcome animation
       - Use brand-amber for the fun name highlight
       - Improve the auto-redirect experience (show a progress indicator)
       - Smooth entrance animations using motion/react
       - Keep it simple: fun name display + "Joining..." state

    4. **Session header** — Polish `src/components/student/session-header.tsx`:
       - Add logo icon next to session name
       - Use brand colors for the student name/fun name display
       - Clean, compact header that doesn't waste vertical space

    5. **Activity card** — Polish `src/components/student/activity-card.tsx`:
       - Modern card styling consistent with teacher dashboard cards
       - Use brand-blue for bracket cards, brand-amber for poll cards (visual differentiation)
       - Better status indicators (active = brand-blue pulse dot, completed = checkmark)
       - Smooth hover/tap states

    6. **Session layout loading** — Edit `src/app/(student)/session/[sessionId]/layout.tsx`:
       - Replace "Loading..." text with Skeleton components
       - Replace "Redirecting..." text with Skeleton or branded loading state
       - Use the Skeleton component from shadcn (installed in 10-01)

    Design principles per user decision: Clean & focused, minimal distractions, clear choices, smooth transitions, prioritize clarity over excitement.
  </action>
  <verify>
    - `grep "logo-icon" src/app/(student)/layout.tsx` confirms logo image usage
    - `grep "brand-blue" src/components/student/join-form.tsx` or join page confirms brand colors
    - `grep "Skeleton" src/app/(student)/session/[sessionId]/layout.tsx` confirms skeleton loading
    - `grep "brand-amber" src/components/student/activity-card.tsx` or similar confirms brand accent
    - All files compile without TypeScript errors
  </verify>
  <done>
    Student layout has branded logo. Join flow has prominent code input with brand-colored CTA. Welcome screen uses brand accents. Session header is compact and branded. Activity cards have visual differentiation. Loading states use skeletons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced celebration, winner reveal, and podium animations</name>
  <files>
    src/components/bracket/celebration-screen.tsx
    src/components/bracket/winner-reveal.tsx
    src/components/bracket/podium-celebration.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    Per user decision: Enhanced celebration/winner screens — more impactful animations, more dramatic reveals. The classroom moment when a winner is revealed should feel exciting.

    1. **CelebrationScreen** — Enhance `src/components/bracket/celebration-screen.tsx`:
       - Replace generic yellow-400 accent with brand-amber color
       - Multi-wave confetti: Initial burst + delayed secondary burst for sustained celebration
       - Use brand colors in confetti (mix of brand-blue, brand-amber, white)
       - Add subtle screen pulse or glow effect (motion/react spring animation)
       - Enhanced winner name display: Larger, bolder, with entrance animation (scale-up spring)
       - Add "sparkle" visual effect around the winner name using brand-amber
       - Longer celebration duration (give the classroom time to react)
       - Continue to respect `prefers-reduced-motion` (per existing pattern)

    2. **WinnerReveal** — Enhance `src/components/bracket/winner-reveal.tsx`:
       - More dramatic countdown: Larger numbers, pulsing scale animation, brand-blue color
       - Add anticipation buildup: Darken background gradually during countdown, add subtle drum-roll visual
       - Winner name entrance: Dramatic spring-in from scale 0 to full size with overshoot
       - Brand-amber glow/highlight behind the winner name
       - Enhanced confetti trigger on reveal (synced with the winner name appearance)
       - Consider adding a brief pause before the winner name appears (after "1") for maximum impact
       - Keep the 3-2-1 countdown structure (it works well)

    3. **PodiumCelebration** — Enhance `src/components/bracket/podium-celebration.tsx`:
       - Use brand colors for podium positions: brand-amber for 1st, brand-blue for 2nd, lighter accent for 3rd
       - Stagger podium entrance: 3rd rises first, then 2nd, then 1st (dramatic reveal order)
       - Enhanced crown/trophy iconography for 1st place using brand-amber
       - Confetti triggered when 1st place podium reaches full height
       - Subtle floating particles or sparkle effects (motion/react)
       - Continue to respect `prefers-reduced-motion`

    IMPORTANT: Read each existing file fully before editing. These components already have working animation logic with motion/react and canvas-confetti. Enhance and upgrade — do not rebuild from scratch. Preserve all existing props, function signatures, and integration points.
  </action>
  <verify>
    - `npm run build` succeeds with no type errors
    - `grep "brand-amber" src/components/bracket/celebration-screen.tsx` confirms brand color usage
    - `grep "prefers-reduced-motion" src/components/bracket/celebration-screen.tsx` confirms a11y preserved
    - All three files maintain their existing export names and prop interfaces
    - canvas-confetti still imported and used (not removed)
  </verify>
  <done>
    CelebrationScreen has multi-wave brand-colored confetti with enhanced winner display. WinnerReveal has dramatic countdown with brand-blue numbers and spring-animated reveal. PodiumCelebration has staggered entrance with brand-colored podium positions. All respect prefers-reduced-motion. All existing props and integration points preserved.
  </done>
</task>

</tasks>

<verification>
- Student layout shows logo image instead of plain text
- Join flow has branded class code input with brand-blue CTA button
- Session loading states show skeleton components (not "Loading..." text)
- Activity cards visually differentiate brackets from polls
- Celebration screen has multi-wave confetti with brand colors
- Winner reveal has dramatic countdown and spring-animated reveal
- Podium celebration has staggered entrance and brand-colored positions
- All animations respect prefers-reduced-motion
- Build passes with zero errors
</verification>

<success_criteria>
1. Student interface is clean, focused, and branded with SparkVotEDU identity
2. Join flow is polished with dominant code input and brand-colored CTA
3. Celebration screens have enhanced, more impactful animations with brand colors
4. Loading states use skeleton screens throughout the student experience
5. All existing functionality preserved (props, integration points, a11y)
6. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-and-polish/10-04-SUMMARY.md`
</output>
