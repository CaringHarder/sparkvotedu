---
phase: 10-landing-page-and-polish
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/components/dashboard/mobile-nav.tsx
  - src/app/(student)/layout.tsx
  - src/components/student/mobile-bottom-nav.tsx
  - src/components/student/simple-voting-view.tsx
  - src/components/student/advanced-voting-view.tsx
  - src/components/bracket/bracket-zoom-wrapper.tsx
autonomous: false

must_haves:
  truths:
    - "Teacher dashboard has mobile navigation accessible via hamburger menu on screens below md breakpoint"
    - "Student interface has bottom navigation bar on mobile for primary actions"
    - "Landing page sections stack cleanly and are fully readable on mobile screens (375px)"
    - "Bracket diagrams are usable on mobile devices (pinch-zoom with existing mini-map or simplified view)"
    - "No broken layouts or unusable interactions on desktop, tablet, or mobile"
  artifacts:
    - path: "src/components/dashboard/mobile-nav.tsx"
      provides: "Hamburger menu that opens the sidebar navigation as a mobile drawer/sheet"
      exports: ["MobileNav"]
    - path: "src/components/student/mobile-bottom-nav.tsx"
      provides: "Bottom navigation bar for student mobile experience"
      exports: ["MobileBottomNav"]
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/dashboard/mobile-nav.tsx"
      via: "MobileNav shown on md:hidden, sidebar shown on hidden md:block"
      pattern: "MobileNav"
    - from: "src/app/(student)/layout.tsx"
      to: "src/components/student/mobile-bottom-nav.tsx"
      via: "MobileBottomNav shown at bottom on mobile"
      pattern: "MobileBottomNav"
---

<objective>
Complete the responsive design audit across all device sizes. Add mobile navigation for both teacher (hamburger menu) and student (bottom nav) interfaces. Ensure all pages render correctly on desktop, tablet, and mobile with no broken layouts.

Purpose: Application must work well on desktop, tablet, and mobile with no broken layouts or unusable interactions (per phase success criteria). Teachers use laptops/desktops; students use phones and laptops equally.
Output: Mobile navigation for teacher and student, responsive fixes, and verified layouts across breakpoints.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-and-polish/10-RESEARCH.md
@.planning/phases/10-landing-page-and-polish/10-01-SUMMARY.md
@.planning/phases/10-landing-page-and-polish/10-02-SUMMARY.md
@.planning/phases/10-landing-page-and-polish/10-03-SUMMARY.md
@.planning/phases/10-landing-page-and-polish/10-04-SUMMARY.md
@src/app/(dashboard)/layout.tsx
@src/app/(student)/layout.tsx
@src/components/bracket/bracket-zoom-wrapper.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teacher mobile hamburger navigation and student bottom navigation bar</name>
  <files>
    src/components/dashboard/mobile-nav.tsx
    src/app/(dashboard)/layout.tsx
    src/components/student/mobile-bottom-nav.tsx
    src/app/(student)/layout.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **Teacher mobile nav** — Create `src/components/dashboard/mobile-nav.tsx`:
       - Hamburger menu button (Menu icon from lucide-react), visible only on `md:hidden`
       - On click, opens a side drawer/sheet (use shadcn Sheet component if available, or a simple sliding panel)
       - Sheet contains: SparkVotEDU logo at top + full SidebarNav component + SignOutButton at bottom + ThemeToggle
       - Sheet slides from left with smooth animation
       - Overlay backdrop that closes on click
       - Close button (X icon) inside the sheet
       - Minimum 44x44px touch targets for all interactive elements
       - "use client" directive

    2. **Dashboard layout update** — Edit `src/app/(dashboard)/layout.tsx`:
       - Add MobileNav component to the header (visible on md:hidden)
       - The existing sidebar remains `hidden md:block` for desktop
       - Header layout: MobileNav hamburger (mobile) | Logo + Title | ThemeToggle + SignOutButton
       - Ensure header content doesn't overflow on small screens

    3. **Student bottom nav** — Create `src/components/student/mobile-bottom-nav.tsx`:
       - Fixed bottom navigation bar, visible only on mobile (md:hidden or sm)
       - 3-4 tabs maximum (per research recommendation):
         * Home (session activity list)
         * Current Activity (the bracket/poll they're currently viewing, if any)
         * My Results / Leaderboard (if applicable)
       - Active tab highlighted with brand-blue
       - Icons from lucide-react (Home, Vote, Trophy or similar)
       - 44px minimum touch target height
       - Safe area insets for notched devices: `pb-[env(safe-area-inset-bottom)]`
       - "use client" directive

    4. **Student layout update** — Edit `src/app/(student)/layout.tsx`:
       - Import and render MobileBottomNav at the bottom of the layout
       - Add bottom padding to main content area to prevent overlap with bottom nav
       - Bottom nav only shows when student is inside a session (check route or use context)
       - If student is on the /join page, bottom nav should NOT show

    Design note per user decision: Students on both phones and laptops equally. The bottom nav is for quick navigation between session activities on phone. On desktop (md+), existing navigation is sufficient.
  </action>
  <verify>
    - `ls src/components/dashboard/mobile-nav.tsx` confirms file exists
    - `ls src/components/student/mobile-bottom-nav.tsx` confirms file exists
    - `grep "MobileNav" src/app/(dashboard)/layout.tsx` confirms integration
    - `grep "MobileBottomNav" src/app/(student)/layout.tsx` or session layout confirms integration
    - `grep "md:hidden" src/components/dashboard/mobile-nav.tsx` confirms mobile-only visibility
    - Components have "use client" directive
  </verify>
  <done>
    Teacher has hamburger menu with sliding sidebar on mobile. Student has bottom navigation bar on mobile. Both use brand colors and 44px minimum touch targets. Desktop layouts unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Responsive fixes for bracket diagrams, voting views, and landing page</name>
  <files>
    src/components/student/simple-voting-view.tsx
    src/components/student/advanced-voting-view.tsx
    src/components/bracket/bracket-zoom-wrapper.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **Bracket diagram mobile experience** — Audit and fix `src/components/bracket/bracket-zoom-wrapper.tsx`:
       - The existing BracketZoomWrapper uses pan-zoom with BracketMiniMap
       - On mobile (<768px): Ensure zoom controls are visible and touch-friendly (larger buttons)
       - Add a hint/tooltip on first mobile visit: "Pinch to zoom, drag to pan"
       - Ensure mini-map is properly sized for mobile screens
       - If bracket-mini-map.tsx exists, ensure it renders at appropriate mobile size
       - Per Claude's discretion (research recommendation): Keep existing pinch-zoom as the approach since BracketMiniMap already exists; add visual hints for discoverability

    2. **Voting views responsive** — Audit and fix voting components:
       - `src/components/student/simple-voting-view.tsx`: Ensure matchup cards stack properly on narrow screens, buttons have 44px+ touch targets, text is readable at mobile font sizes
       - `src/components/student/advanced-voting-view.tsx`: Ensure the bracket-embedded voting view works on mobile (may need to defer to zoom wrapper)

    3. **General responsive audit** — Check and fix common mobile issues across modified files:
       - Ensure all text uses responsive font sizes (text-sm on mobile, text-base or larger on desktop)
       - Confirm padding/margin uses responsive values (px-4 on mobile, px-6 or px-8 on desktop)
       - Verify grids collapse properly: `sm:grid-cols-2 lg:grid-cols-3` patterns
       - Test that no horizontal scroll occurs on any page at 375px width
       - Verify that the landing page (from 10-02) sections work well on mobile (they should already if built responsively, but verify and fix if needed)
       - Check that landing page nav stacks properly on mobile
       - Check that pricing cards don't overflow on mobile

    Per user decision: Teachers primarily laptop/desktop, students on both phones and laptops equally.
  </action>
  <verify>
    - `npm run build` succeeds
    - No components use fixed pixel widths that would break on mobile
    - Touch targets are >= 44px (check button/link heights)
    - Bracket zoom wrapper has mobile-friendly controls
  </verify>
  <done>
    Bracket diagrams are usable on mobile with pinch-zoom hints. Voting views stack cleanly on narrow screens with appropriate touch targets. Landing page and all modified pages render correctly at 375px, 768px, and 1024px+ widths. No horizontal overflow on any page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete responsive design across all device sizes including:
    - Teacher hamburger menu navigation on mobile
    - Student bottom navigation bar on mobile
    - Bracket diagram mobile pinch-zoom with hints
    - Responsive voting views
    - Landing page mobile layout
  </what-built>
  <how-to-verify>
    1. Open the app in your browser
    2. Use browser DevTools to test at these sizes:
       - Mobile (375px): Check landing page, join flow, student session, teacher dashboard
       - Tablet (768px): Check landing page, dashboard with sidebar, student session
       - Desktop (1280px): Check all pages render with full sidebar
    3. On mobile dashboard: Tap hamburger icon — sidebar should slide out with full nav
    4. On mobile student session: Check bottom navigation bar with Home/Activity tabs
    5. On mobile bracket view: Verify pinch-zoom works and hint is shown
    6. Toggle dark mode from both desktop and mobile — verify all pages look correct
    7. Check landing page: all sections stack cleanly on mobile with no horizontal scroll
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found at specific breakpoints/pages</resume-signal>
</task>

</tasks>

<verification>
- Teacher dashboard usable on mobile with hamburger menu providing full navigation
- Student session usable on mobile with bottom navigation bar
- Landing page readable and functional at 375px width
- Bracket diagrams navigable on mobile with pinch-zoom
- Voting views have 44px+ touch targets and stack properly on mobile
- No horizontal overflow on any page at any tested breakpoint
- Dark mode works correctly at all breakpoints
- Human verification checkpoint passes
</verification>

<success_criteria>
1. Teacher mobile navigation (hamburger menu with drawer) works on screens < 768px
2. Student bottom nav provides quick session navigation on mobile
3. All pages render correctly at 375px, 768px, and 1280px+ widths
4. Bracket diagrams usable on mobile with zoom hints
5. No broken layouts or unusable interactions at any breakpoint
6. Human verification confirms responsive quality
7. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-and-polish/10-05-SUMMARY.md`
</output>
