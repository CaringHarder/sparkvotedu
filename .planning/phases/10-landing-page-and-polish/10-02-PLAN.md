---
phase: 10-landing-page-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/page.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/how-it-works-section.tsx
  - src/components/landing/features-section.tsx
  - src/components/landing/pricing-section.tsx
  - src/components/landing/final-cta-section.tsx
  - src/components/landing/landing-nav.tsx
  - src/components/landing/landing-footer.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees a branded landing page with the SparkVotEDU logo, 'Ignite Student Voice Through Voting' motto, feature pitch, and pricing comparison"
    - "Student can enter a class code on the landing page and be routed to the join flow"
    - "Teacher can click Sign In to reach the login page"
    - "Landing page uses brand colors (sky blue, amber/gold) and looks professional"
    - "PricingCards component is reused from /billing for the pricing section"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete landing page composing all sections"
      min_lines: 30
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with logo, motto headline, class code input, and sign-in CTA"
      exports: ["HeroSection"]
    - path: "src/components/landing/features-section.tsx"
      provides: "Icon-driven feature showcase (brackets, polls, analytics, etc.)"
      exports: ["FeaturesSection"]
    - path: "src/components/landing/pricing-section.tsx"
      provides: "Pricing section reusing PricingCards component"
      exports: ["PricingSection"]
    - path: "src/components/landing/landing-nav.tsx"
      provides: "Minimal nav with logo + Sign In button"
      exports: ["LandingNav"]
  key_links:
    - from: "src/components/landing/hero-section.tsx"
      to: "src/components/student/home-join-input.tsx"
      via: "HomeJoinInput component reuse"
      pattern: "HomeJoinInput"
    - from: "src/components/landing/pricing-section.tsx"
      to: "src/components/billing/pricing-cards.tsx"
      via: "PricingCards with no props (public mode)"
      pattern: "<PricingCards"
---

<objective>
Build the complete public-facing landing page with branded hero, feature showcase, pricing comparison, and dual entry points (student class code + teacher sign-in).

Purpose: The landing page is the first impression for visitors. It must communicate SparkVotEDU's value proposition, showcase features, display pricing, and provide immediate entry points for both students and teachers.
Output: Full marketing landing page at the root URL with 6 sections.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-and-polish/10-RESEARCH.md
@.planning/phases/10-landing-page-and-polish/10-01-SUMMARY.md
@src/app/page.tsx
@src/components/student/home-join-input.tsx
@src/components/billing/pricing-cards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page navigation, hero section, and how-it-works section</name>
  <files>
    src/components/landing/landing-nav.tsx
    src/components/landing/hero-section.tsx
    src/components/landing/how-it-works-section.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **LandingNav** — Create `src/components/landing/landing-nav.tsx`:
       - Sticky/fixed top navigation with white/transparent background
       - Left: SparkVotEDU logo (use next/image with `/logo-horizontal.png` on desktop, `/logo-icon.png` on mobile)
       - Right: "Sign In" button (Link to /login) — use Button component with brand styling
       - Minimal: NO multi-link nav bar (per user decision). Just logo + Sign In.
       - Full-width with centered max-w-6xl content container
       - Supports dark mode (ThemeToggle can be placed here next to Sign In)
       - Clean, professional look with subtle bottom border or shadow

    2. **HeroSection** — Create `src/components/landing/hero-section.tsx`:
       - Full-width centered hero section with generous vertical padding (py-20 to py-32)
       - Headline: "Ignite Student Voice Through Voting" (per user decision — this is the motto, use as H1)
       - Sub-headline: Short description focusing on how brackets and polls spark student engagement in the classroom (per user decision — not just "voting tool")
       - Two CTAs side-by-side or stacked:
         a. Student class code input: Reuse `HomeJoinInput` component from `@/components/student/home-join-input`
         b. Teacher CTA: "Get Started Free" or "Create Your First Bracket" button linking to /signup
       - Visual accent: Use brand-blue and brand-amber colors for gradient or accent elements
       - Clean white background (per user decision)
       - Consider subtle decorative elements using the spark/ray motif from the brand

    3. **HowItWorksSection** — Create `src/components/landing/how-it-works-section.tsx`:
       - 3-step process shown in icon-driven cards (per user decision: icons + short descriptions, not screenshots)
       - Steps: (1) Create — Teacher builds a bracket or poll in minutes, (2) Launch — Share a simple class code with students, (3) Engage — Watch participation happen in real time
       - Each step: numbered icon in brand-blue circle + title + short description
       - Light background section (brand-blue-light or slightly tinted) to create visual separation from hero
       - Responsive: 3 columns on desktop, stacked on mobile

    Copy guidelines: Focus messaging on how bracketing and polling SPARK student engagement. The tone should be enthusiastic but professional — educational technology for real classrooms.
  </action>
  <verify>
    - All three files exist and export named components
    - `grep "HomeJoinInput" src/components/landing/hero-section.tsx` confirms reuse
    - Components use brand-blue and brand-amber color classes
    - No hardcoded colors — all through CSS variables / Tailwind classes
  </verify>
  <done>
    Landing nav shows logo + Sign In. Hero displays motto headline with class code input and teacher CTA. How-it-works shows 3-step process. All use brand colors and support dark mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Features, pricing, final CTA sections, footer, and page assembly</name>
  <files>
    src/components/landing/features-section.tsx
    src/components/landing/pricing-section.tsx
    src/components/landing/final-cta-section.tsx
    src/components/landing/landing-footer.tsx
    src/app/page.tsx
  </files>
  <action>
    Use the `frontend-design` skill (invoke via Skill tool with skill='frontend-design') for implementing these components to ensure distinctive, production-grade design quality.

    1. **FeaturesSection** — Create `src/components/landing/features-section.tsx`:
       - Icon-driven feature showcase (per user decision: icons + short descriptions, NOT screenshots)
       - Features to highlight (6-8 cards in a responsive grid):
         * Tournament Brackets — Single-elimination, double-elimination, round-robin formats
         * Quick Polls — Simple or ranked polls with instant results
         * Real-Time Results — Live voting with results updating as students participate
         * Predictive Brackets — Students predict outcomes, scored leaderboard ranks accuracy
         * Sports Integration — Import real NCAA tournaments for classroom predictions
         * Analytics & Export — Track participation, view distributions, export CSV data
         * Tier-Based Access — Free, Pro, and Pro Plus plans for every classroom need
       - Each feature: Lucide icon in brand-colored container + title + 1-2 sentence description
       - Grid: 2 cols on sm, 3 cols on lg
       - White background section

    2. **PricingSection** — Create `src/components/landing/pricing-section.tsx`:
       - Section heading: "Plans for Every Classroom" or similar
       - Reuse existing `<PricingCards />` component with NO props (public mode shows "Get Started" links per research)
       - Import from `@/components/billing/pricing-cards`
       - Light tinted background for visual separation
       - Centered container max-w-5xl

    3. **FinalCTASection** — Create `src/components/landing/final-cta-section.tsx`:
       - Repeat the class code input for students who scrolled past the hero
       - Headline: "Ready to Spark Engagement?" or similar
       - Two entry points: class code input (HomeJoinInput) + teacher sign-up button
       - Brand-blue gradient or solid background section for strong visual impact
       - White text on brand-blue background

    4. **LandingFooter** — Create `src/components/landing/landing-footer.tsx`:
       - Simple footer with copyright, SparkVotEDU branding
       - Links: Sign In, Create Account, Privacy (placeholder href)
       - Minimal — not a mega-footer

    5. **Page assembly** — Rewrite `src/app/page.tsx`:
       - Import and compose all sections in order: LandingNav > HeroSection > HowItWorksSection > FeaturesSection > PricingSection > FinalCTASection > LandingFooter
       - Each section is a full-width block
       - Page is a simple vertical stack of section components
       - Remove all existing content (current page is 42 lines of minimal markup)

    Section order (Claude's discretion, following research recommendation):
    Nav > Hero > How It Works > Features > Pricing > Final CTA > Footer
  </action>
  <verify>
    - `npm run build` succeeds
    - `src/app/page.tsx` imports all 7 landing components
    - `grep "PricingCards" src/components/landing/pricing-section.tsx` confirms reuse
    - `grep "HomeJoinInput" src/components/landing/final-cta-section.tsx` confirms class code repeat
    - No hardcoded colors — all via Tailwind brand-* or semantic color classes
  </verify>
  <done>
    Complete landing page renders with: nav (logo + Sign In), hero (motto + class code + teacher CTA), how-it-works (3 steps), features (icon grid), pricing (reused PricingCards), final CTA (class code repeat), and footer. All sections use brand colors and are dark-mode compatible.
  </done>
</task>

</tasks>

<verification>
- Landing page renders at localhost:3000 with all 7 sections visible
- Student can type a class code in the hero OR final CTA section and be routed to /join/{code}
- "Sign In" button navigates to /login
- PricingCards shows Free, Pro, Pro Plus tiers with "Get Started" links (public mode)
- Brand colors (sky blue, amber/gold) are visually prominent throughout
- Page looks good in both light and dark modes
- Responsive: sections stack cleanly on mobile
</verification>

<success_criteria>
1. Branded landing page visible at root URL with logo, motto, feature pitch, and pricing
2. Student class code input works on landing page (both hero and final CTA)
3. Teacher sign-in/sign-up paths accessible from landing page
4. PricingCards reused from /billing in public mode
5. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-and-polish/10-02-SUMMARY.md`
</output>
