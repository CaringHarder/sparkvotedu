---
phase: 01-sports-bracket-import-reliability
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/bracket/tournament-browser.tsx
  - src/components/bracket/bracket-detail.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Import flow shows Final Four pairing picker before confirming import"
    - "Import warnings are displayed to the teacher after successful import"
    - "Bracket settings panel shows Final Four pairing dropdown for sports brackets"
    - "Changing pairing in settings shows confirmation warning if predictions exist past R4"
    - "Pairing dropdown shows actual region names from the bracket, not hardcoded geographic names"
    - "Bracket settings has a Refresh from ESPN button for sports brackets"
  artifacts:
    - path: "src/components/bracket/tournament-browser.tsx"
      provides: "Final Four pairing picker in import flow, import warning display"
      contains: "getFinalFourPairings"
    - path: "src/components/bracket/bracket-detail.tsx"
      provides: "Final Four pairing dropdown in settings, Refresh from ESPN button"
      contains: "finalFourPairing"
  key_links:
    - from: "src/components/bracket/tournament-browser.tsx"
      to: "src/actions/sports.ts"
      via: "importTournament call receives warnings in response"
      pattern: "warnings"
    - from: "src/components/bracket/bracket-detail.tsx"
      to: "src/actions/bracket.ts"
      via: "updateBracketSettings with finalFourPairing, triggerSportsSync for refresh"
      pattern: "updateBracketSettings.*finalFourPairing"
---

<objective>
Add Final Four pairing picker to import flow and bracket settings, display import warnings, and add Refresh from ESPN button.

Purpose: Surface the backend capabilities from Plans 01-02 in the teacher-facing UI.
Output: Updated tournament-browser.tsx with pairing picker + warnings, updated bracket-detail.tsx with pairing settings + refresh button.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-sports-bracket-import-reliability-auto-fix-play-in-entrant-placement-in-r1-slots-by-tournament-seed-add-final-four-pairing-configuration-fix-r0-entrant-assignment/01-CONTEXT.md
@.planning/phases/01-sports-bracket-import-reliability-auto-fix-play-in-entrant-placement-in-r1-slots-by-tournament-seed-add-final-four-pairing-configuration-fix-r0-entrant-assignment/01-01-SUMMARY.md
@.planning/phases/01-sports-bracket-import-reliability-auto-fix-play-in-entrant-placement-in-r1-slots-by-tournament-seed-add-final-four-pairing-configuration-fix-r0-entrant-assignment/01-02-SUMMARY.md

@src/components/bracket/tournament-browser.tsx
@src/components/bracket/bracket-detail.tsx
@src/lib/sports/pairings.ts
@src/actions/sports.ts
@src/actions/bracket.ts

<interfaces>
<!-- From Plans 01 and 02 -->

From src/lib/sports/pairings.ts:
```typescript
export interface FinalFourPairing {
  label: string   // e.g., "East vs West, South vs Midwest"
  value: string   // e.g., "East-West,South-Midwest"
  semis: [string, string][]
}
export function getFinalFourPairings(regions: string[]): FinalFourPairing[]
```

From src/actions/sports.ts (updated by Plan 02):
```typescript
// importTournament now returns:
{ bracket: { id: string; name: string }, warnings: string[] }
// or { error: string }
```

From src/actions/bracket.ts (updated by Plan 02):
```typescript
// updateBracketSettings returns:
{ success: true, predictionWarning?: boolean }
// when finalFourPairing is changed and R5+ predictions exist, predictionWarning = true
```

From src/components/bracket/tournament-browser.tsx:
```typescript
interface TournamentBrowserProps {
  sessions: { id: string; name: string; joinCode: string }[]
}
// handleImport currently calls importTournament({ tournamentId, season, sessionId })
// importResult state: { success: boolean; message: string; bracketId?: string }
```

From src/components/bracket/bracket-detail.tsx:
```typescript
// bracket prop includes: bracketType, dataSource, finalFourPairing, sportGender
// isSports = bracket.bracketType === 'sports'
// Existing settings section: viewingMode, showSeedNumbers, showVoteCounts toggles
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import flow pairing picker and warnings display</name>
  <files>src/components/bracket/tournament-browser.tsx</files>
  <action>
1. **Import warnings display:**
   - Update the `importResult` state type to include `warnings?: string[]`.
   - In `handleImport`, when the result has `bracket` and `warnings`, store warnings in state.
   - After successful import, if warnings.length > 0, render a yellow/amber info box below the success message showing the warnings as a bulleted list. Use Tailwind: `bg-amber-50 border-amber-200 text-amber-800 rounded-lg p-3 text-sm`. Prefix with "Import completed with notes:" heading.
   - If no warnings, show only the success message (existing behavior).

2. **Note about pairing picker in import flow:**
   The import flow currently is a single "Import" button click. Rather than adding a multi-step wizard (which would be over-engineering for 3 preset options), the Final Four pairing will be configured post-import in bracket settings (per the user decision: "Available during import flow AND in bracket settings"). For the import flow, add a brief note after successful import: "You can configure Final Four pairings in bracket settings." This keeps import simple -- one click -- while pointing teachers to settings for customization.

   If the auto-detection from Plan 01 picked a pairing, show it: "Final Four pairing auto-detected: {pairing label}. Change in bracket settings."
  </action>
  <verify>
    <automated>cd /Users/davidreynoldsjr/VibeCoding/SparkVotEDU && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Import result displays warnings in amber info box. Success message includes note about auto-detected pairing and link to bracket settings for customization.</done>
</task>

<task type="auto">
  <name>Task 2: Final Four pairing dropdown and Refresh button in bracket settings</name>
  <files>src/components/bracket/bracket-detail.tsx</files>
  <action>
1. **Final Four pairing dropdown (sports brackets only):**
   - Add state: `const [finalFourPairing, setFinalFourPairing] = useState(bracket.finalFourPairing ?? '')`.
   - After the existing DisplaySettingsSection (with viewingMode/showSeedNumbers/showVoteCounts), add a new section visible only when `isSports`:
     - Heading: "Final Four Pairings" with a small info icon.
     - Get the bracket's region names from its matchups. The bracket prop should include matchups with bracketRegion. Extract unique non-null region names from R1 matchups. Call `getFinalFourPairings(regions)` to get the 3 options.
     - Render a `<select>` dropdown with the 3 pairing options plus an "Auto (position-based)" default option (value = "").
     - Selected value = `finalFourPairing` state.
     - onChange: call `updateBracketSettings({ bracketId: bracket.id, finalFourPairing: value || null })`.
     - If the result has `predictionWarning: true`, show a toast or inline warning: "Note: Students have predictions for Final Four and beyond. Changing pairings may affect their brackets."
     - Show the warning BEFORE saving if possible: check prediction count via a quick indicator, or show warning AFTER the save returns predictionWarning.
   - Style: Use existing pattern from DisplaySettingsSection -- Tailwind select with consistent spacing.

2. **Refresh from ESPN button (sports brackets only):**
   - Below the pairing section (still within `isSports` conditional), add a "Refresh from ESPN" button.
   - Import `triggerSportsSync` from `@/actions/sports`.
   - On click: call `triggerSportsSync()`, show loading state on button, then show a brief success toast (use existing toast pattern in the component -- look for `setSuccessMessage` or similar state).
   - Use a `RefreshCw` icon from lucide-react on the button.
   - Disable button while syncing.

3. **Ensure bracket query includes region data:**
   - Check that the bracket prop passed to this component includes matchup bracketRegion data. If not, the parent page query (likely in a server component) needs to include `matchups: { select: { bracketRegion: true } }` or the unique regions need to be passed as a prop. If the bracket object already has matchups loaded, extract regions from there. If not, add a `regions` field to the bracket query in the parent page.
  </action>
  <verify>
    <automated>cd /Users/davidreynoldsjr/VibeCoding/SparkVotEDU && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Sports bracket settings show Final Four pairing dropdown with 3 options derived from actual region names. Changing pairing triggers re-wire and warns about predictions. Refresh from ESPN button triggers sync with loading state and success toast.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of full import and settings flow</name>
  <files>n/a</files>
  <action>
Teacher verifies the complete sports bracket import reliability flow end-to-end:
- Import an NCAA tournament and check for warnings display
- Verify Final Four pairing auto-detection and dropdown in settings
- Test pairing change and Refresh from ESPN button
- Check play-in resolution if applicable
  </action>
  <what-built>Complete sports bracket import reliability: auto-fix, warnings, Final Four pairing config, play-in resolution, Refresh from ESPN</what-built>
  <how-to-verify>
    1. Go to localhost:3001, log in as teacher
    2. Create a new class session if needed
    3. Import an NCAA tournament bracket (Men's or Women's)
    4. Verify: Import succeeds, any warnings shown in amber box
    5. Verify: Success message mentions auto-detected Final Four pairing
    6. Go to the imported bracket's detail/settings page
    7. Verify: "Final Four Pairings" dropdown visible with 3 options using actual region names
    8. Change the pairing selection -- verify it saves (page should reflect new pairing)
    9. Verify: "Refresh from ESPN" button visible, click it, verify toast shows sync result
    10. If any play-in games have completed, verify combined entries show winner team name/logo
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Teacher approves full end-to-end flow: import with warnings, pairing configuration, ESPN refresh, and play-in resolution all working correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Import flow shows warnings when ESPN data has issues
- Bracket settings shows pairing dropdown for sports brackets
- Refresh from ESPN button triggers sync
- Teacher can visually verify correct pairing and play-in resolution
</verification>

<success_criteria>
- Import warnings displayed in amber info box after import
- Final Four pairing dropdown in bracket settings with 3 region-based options
- Pairing changes trigger re-wiring and warn about predictions
- Refresh from ESPN button works with loading state and toast
- Human verification confirms full flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-sports-bracket-import-reliability-auto-fix-play-in-entrant-placement-in-r1-slots-by-tournament-seed-add-final-four-pairing-configuration-fix-r0-entrant-assignment/01-03-SUMMARY.md`
</output>
