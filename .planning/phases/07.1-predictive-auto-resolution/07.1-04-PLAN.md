---
phase: 07.1-predictive-auto-resolution
plan: 04
type: execute
wave: 3
depends_on: ["07.1-02"]
files_modified:
  - src/components/bracket/prediction-reveal.tsx
  - src/components/bracket/podium-celebration.tsx
  - src/components/bracket/countdown-overlay.tsx
  - src/components/bracket/prediction-leaderboard.tsx
  - src/hooks/use-predictions.ts
  - src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees two tabs (Bracket and Leaderboard) during reveal, default to Leaderboard"
    - "Bracket tab fills in progressively with winners as rounds are revealed"
    - "Correct predictions show green highlight, wrong predictions show red with strikethrough"
    - "'X/Y correct' counter displayed at top of bracket tab"
    - "Leaderboard updates live after each round reveal with animated rank changes"
    - "Leaderboard shows top 10 plus the viewing student's own rank if outside top 10"
    - "3-2-1 countdown animation plays between reveal rounds"
    - "Top 3 predictors get gold/silver/bronze podium celebration after final reveal"
    - "Student prediction hook handles reveal_round and reveal_complete broadcast events"
  artifacts:
    - path: "src/components/bracket/prediction-reveal.tsx"
      provides: "Student reveal experience with tabs, accuracy overlay"
      min_lines: 100
    - path: "src/components/bracket/podium-celebration.tsx"
      provides: "Top-3 gold/silver/bronze podium celebration"
      min_lines: 60
    - path: "src/components/bracket/countdown-overlay.tsx"
      provides: "3-2-1 countdown animation between rounds"
      min_lines: 30
    - path: "src/components/bracket/prediction-leaderboard.tsx"
      provides: "Extended leaderboard with top-10 limit and rank animation"
      contains: "LayoutGroup"
  key_links:
    - from: "src/components/bracket/prediction-reveal.tsx"
      to: "src/hooks/use-predictions.ts"
      via: "real-time updates"
      pattern: "usePredictions"
    - from: "src/hooks/use-predictions.ts"
      to: "src/lib/realtime/broadcast.ts"
      via: "broadcast subscription"
      pattern: "reveal_round|reveal_complete"
    - from: "src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx"
      to: "src/components/bracket/prediction-reveal.tsx"
      via: "component routing"
      pattern: "PredictionReveal"
---

<objective>
Build the student reveal experience: progressive bracket fill-in with color-coded accuracy overlay, animated leaderboard with top-10 + self-rank, 3-2-1 countdown between rounds, and top-3 podium celebration. Update the prediction hook to handle reveal broadcast events.

Purpose: This is the student-facing payoff -- the classroom experience where predictions come alive as results are revealed round by round. The leaderboard-first design puts the competition front and center.
Output: 3 new components (PredictionReveal, PodiumCelebration, CountdownOverlay), extended PredictionLeaderboard, updated usePredictions hook, student page routing for auto-mode reveal.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07.1-predictive-auto-resolution/07.1-01-SUMMARY.md
@.planning/phases/07.1-predictive-auto-resolution/07.1-02-SUMMARY.md
@src/components/bracket/prediction-leaderboard.tsx
@src/components/bracket/winner-reveal.tsx
@src/components/bracket/celebration-screen.tsx
@src/components/bracket/bracket-diagram.tsx
@src/hooks/use-predictions.ts
@src/hooks/use-realtime-bracket.ts
@src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CountdownOverlay, PodiumCelebration, and extended PredictionLeaderboard</name>
  <files>
    src/components/bracket/countdown-overlay.tsx
    src/components/bracket/podium-celebration.tsx
    src/components/bracket/prediction-leaderboard.tsx
    src/hooks/use-predictions.ts
  </files>
  <action>
    1. Create countdown-overlay.tsx:
       - Reuse the WinnerReveal countdown pattern (3-2-1 with motion/react)
       - Props: roundNumber: number, onComplete: () => void
       - Full-screen dark overlay (bg-black/80, fixed inset-0, z-50)
       - 3-2-1 countdown with scale + fade animation (motion.div, scale: [0.5, 1.2, 1], opacity: [0, 1, 0])
       - After countdown: "Round {roundNumber} Results" text with fade-in
       - Auto-calls onComplete after a brief pause (1.5s)
       - Respects prefers-reduced-motion: if reduced, skip animation and call onComplete immediately
       - Use AnimatePresence for mount/unmount transitions

    2. Create podium-celebration.tsx:
       - Props: top3: Array<{ rank: 1|2|3, name: string, points: number, correctPicks: number }>, bracketName: string, onDismiss: () => void
       - Full-screen dark overlay (same style as CelebrationScreen)
       - "PREDICTION CHAMPIONS" header (text-3xl font-bold text-white)
       - Three podium blocks arranged: 2nd (left, shorter), 1st (center, tallest), 3rd (right, shortest)
       - Podium blocks use motion.div with staggered entrance: 3rd first (delay 0), 2nd (delay 0.5s), 1st (delay 1s)
       - Each block shows: RankBadge (reuse from prediction-leaderboard.tsx), name (text-white), points
       - Gold: amber-400 background accent, Silver: gray-300, Bronze: amber-600
       - Confetti burst after 1st place reveals (reuse canvas-confetti pattern from CelebrationScreen)
       - Multi-burst confetti: left, center, right (exact pattern from CelebrationScreen)
       - "Continue" button and auto-dismiss after 12 seconds
       - Respects prefers-reduced-motion: skip confetti if reduced motion

    3. Extend prediction-leaderboard.tsx:
       - Add animated rank changes using motion/react LayoutGroup + layout prop
       - Wrap leaderboard rows in LayoutGroup
       - Each row gets motion.div or motion.tr with `layout` prop and key={participantId}
       - Spring animation: type: 'spring', stiffness: 300, damping: 30
       - Add top-10 limit for student view:
         - New prop: isStudent?: boolean, currentParticipantId?: string
         - If isStudent: show top 10 entries. If current student is NOT in top 10, add a separator row ("...") then the student's own row with their actual rank
         - Rank calculation: shared scores get shared rank (dense ranking)
       - Keep teacher view showing all participants (isStudent=false or undefined)
       - Leaderboard shows rank + total points only for students (no per-round breakdown) per locked decision

    4. Extend use-predictions.ts hook:
       - Handle new broadcast event types in the existing bracket channel subscription:
         - 'reveal_round': payload { round } -> trigger refetch of bracket state + leaderboard scores
         - 'reveal_complete': trigger refetch + set a local flag (revealComplete: true)
         - 'results_prepared': trigger refetch
       - Add revealedUpToRound to the hook's return value (from bracket state)
       - Add revealComplete boolean to return value
       - The existing hook already subscribes to 'bracket_update' events on `bracket:{bracketId}` channel. Add event type checks for the new types within the existing handler.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    CountdownOverlay exports correctly.
    PodiumCelebration exports correctly.
    PredictionLeaderboard renders with motion layout animations.
    usePredictions hook handles reveal_round, reveal_complete, results_prepared events.
  </verify>
  <done>
    CountdownOverlay shows 3-2-1 with round title. PodiumCelebration shows top-3 with staggered reveal and confetti. PredictionLeaderboard has animated rank changes and top-10 + self-rank for students. usePredictions hook handles all reveal broadcast events.
  </done>
</task>

<task type="auto">
  <name>Task 2: PredictionReveal component and student page routing</name>
  <files>
    src/components/bracket/prediction-reveal.tsx
    src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
  </files>
  <action>
    1. Create prediction-reveal.tsx -- the student reveal experience:

       Interface PredictionRevealProps:
         bracket: BracketWithDetails
         participantId: string
         myPredictions: PredictionData[]
         initialScores: PredictionScore[]

       Component structure:
       - Use usePredictions hook for real-time updates
       - Two tabs: "Leaderboard" (default) and "Bracket"
       - Tab buttons at top, styled like existing PredictiveBracket tabs (segmented control style)

       Leaderboard tab:
       - Render PredictionLeaderboard with isStudent=true, currentParticipantId=participantId
       - Scores update live after each round reveal (from usePredictions hook)

       Bracket tab:
       - "X/Y correct" counter at top (computed from myPredictions vs decided matchups)
       - Render BracketDiagram with accuracy overlay
       - For each revealed matchup (status='decided'):
         - Find student's prediction for this matchup
         - If prediction matches winnerId: green background (bg-green-100 border-green-400 or dark:bg-green-900/30)
         - If prediction doesn't match: red background (bg-red-100 border-red-400 or dark:bg-red-900/30)
         - Wrong predictions: show student's predicted name with line-through text decoration next to actual winner
       - Unrevealed matchups (status='pending'): show as empty/locked (existing BracketDiagram behavior)
       - Implementation: pass an accuracyOverlay prop or custom entrant renderer to BracketDiagram
         - If BracketDiagram doesn't support custom rendering per matchup, wrap it and add SVG overlays (similar to the 04-04 vote overlay pattern with duplicated layout constants)
         - Alternative: add color-coded entrant highlighting via the existing `votedEntrantIds` / `entrantHighlights` pattern if available, or use a new `matchupStyles` Record<matchupId, { bgClass, borderClass }> prop

       Countdown between rounds:
       - When usePredictions detects a new round revealed (revealedUpToRound changes), show CountdownOverlay
       - CountdownOverlay.onComplete hides it and shows the updated bracket/leaderboard
       - Do NOT show countdown for Round 1 (first reveal starts immediately)

       Final reveal:
       - When revealComplete is true (all rounds revealed), show PodiumCelebration
       - Top 3 derived from leaderboard scores
       - PodiumCelebration.onDismiss returns to leaderboard view (completed state)

       Completed state (after celebration dismissed or bracket.predictionStatus='completed'):
       - Show both tabs read-only (bracket with full accuracy overlay, final leaderboard)
       - Follow existing bracket completion patterns for revisiting

    2. In student bracket page (page.tsx):
       - The page already routes by bracketType. For predictive brackets:
       - Add condition: if bracketType='predictive' && predictiveResolutionMode='auto' && predictionStatus is 'revealing' or 'completed':
         Render PredictionReveal instead of the standard PredictiveBracket component
       - If predictionStatus is 'previewing' or 'tabulating': show a waiting state ("Your teacher is preparing results...")
       - If predictionStatus is 'predictions_open': show existing prediction submission UI (unchanged)
       - Pass myPredictions (fetched server-side) and initialScores to PredictionReveal
       - Ensure the page fetches bracket.revealedUpToRound in its server component data fetch (it's part of BracketData which is already fetched)

    IMPORTANT per locked decisions:
    - Prediction vote counts are NEVER shown to students (only outcomes)
    - Leaderboard shows rank + total points only (no per-round breakdown for students)
    - Default tab is Leaderboard when reveal starts
    - Top 3 celebration is the finale (not single champion fanfare)
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    PredictionReveal renders two tabs with Leaderboard as default.
    Bracket tab shows accuracy overlay (green/red) for revealed matchups.
    Student page routes to PredictionReveal for auto-mode brackets in revealing/completed states.
  </verify>
  <done>
    Students see the full reveal experience: leaderboard-first tabs, progressive bracket fill-in with green/red accuracy coding, X/Y correct counter, 3-2-1 countdown between rounds, and top-3 podium celebration at the end. The student page correctly routes to PredictionReveal for auto-mode predictive brackets.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Student page renders PredictionReveal for auto-mode predictive brackets in revealing/completed state
- PredictionReveal shows Leaderboard tab by default
- Bracket tab shows accuracy overlay (green correct, red wrong with strikethrough)
- "X/Y correct" counter visible at top of bracket tab
- PredictionLeaderboard limits to top 10 + self for students with animated rank transitions
- CountdownOverlay plays 3-2-1 between rounds (not for Round 1)
- PodiumCelebration shows top 3 with staggered entrance and confetti
- usePredictions hook refetches on reveal_round and reveal_complete events
</verification>

<success_criteria>
Students experience a compelling classroom reveal: default to leaderboard showing live rank changes, switch to bracket to see accuracy-coded predictions fill in round by round. Countdown builds anticipation between rounds. Top-3 podium celebration rewards the best predictors. The experience works in real-time via broadcast and supports read-only revisiting after completion.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-predictive-auto-resolution/07.1-04-SUMMARY.md`
</output>
