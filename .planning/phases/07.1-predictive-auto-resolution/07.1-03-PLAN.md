---
phase: 07.1-predictive-auto-resolution
plan: 03
type: execute
wave: 3
depends_on: ["07.1-02"]
files_modified:
  - src/components/bracket/predictive-bracket.tsx
  - src/components/bracket/prediction-preview.tsx
  - src/components/bracket/bracket-form.tsx
  - src/components/teacher/live-dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher sees 'Prepare Results' button after predictions close (auto mode only)"
    - "Teacher sees full bracket preview with auto-resolved winners after tabulation"
    - "Teacher can click any matchup to override its winner in preview"
    - "Ties and no-prediction matchups are highlighted with amber border for mandatory resolution"
    - "Round-by-round tie resolution: Round 1 ties first, then Round 2 (cascade-aware)"
    - "'Release Results' button disabled until all matchups have winners"
    - "'Release Results' triggers confirmation dialog before proceeding"
    - "Teacher can reopen predictions from preview state"
    - "Teacher sees 'Reveal Next Round' button during reveal phase"
    - "Teacher has Bracket and Leaderboard tabs plus Reveal controls during reveal"
    - "Presentation mode available (F key) for projecting bracket or leaderboard"
    - "Teacher can click 'Complete Bracket' after all rounds revealed"
    - "Bracket form includes 'Predictive' option in resolution mode selector"
  artifacts:
    - path: "src/components/bracket/prediction-preview.tsx"
      provides: "Teacher preview with override controls"
      min_lines: 80
    - path: "src/components/bracket/predictive-bracket.tsx"
      provides: "Extended teacher flow for auto mode"
      contains: "prepareResults"
    - path: "src/components/bracket/bracket-form.tsx"
      provides: "Third resolution mode option"
      contains: "Predictive"
  key_links:
    - from: "src/components/bracket/predictive-bracket.tsx"
      to: "src/actions/prediction.ts"
      via: "server action calls"
      pattern: "prepareResults|releaseResults|revealNextRound"
    - from: "src/components/bracket/prediction-preview.tsx"
      to: "src/actions/prediction.ts"
      via: "overrideWinner action"
      pattern: "overrideWinner"
---

<objective>
Build the teacher-facing UI for the auto-resolution flow: bracket form "Predictive" option, preview with override controls, reveal controls with round-by-round pacing, and presentation mode.

Purpose: Teachers need to control the entire auto-resolution lifecycle from their dashboard -- preparing, reviewing, overriding, releasing, and pacing the reveal.
Output: New PredictionPreview component, extended PredictiveBracket teacher flow, updated bracket form, live dashboard reveal controls.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07.1-predictive-auto-resolution/07.1-01-SUMMARY.md
@.planning/phases/07.1-predictive-auto-resolution/07.1-02-SUMMARY.md
@src/components/bracket/predictive-bracket.tsx
@src/components/bracket/bracket-form.tsx
@src/components/bracket/bracket-diagram.tsx
@src/components/bracket/prediction-leaderboard.tsx
@src/components/teacher/live-dashboard.tsx
@src/components/poll/presentation-mode.tsx
@src/actions/prediction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bracket form + PredictionPreview component</name>
  <files>
    src/components/bracket/bracket-form.tsx
    src/components/bracket/prediction-preview.tsx
  </files>
  <action>
    1. In bracket-form.tsx:
       - Add 'auto' to the resolution mode radio buttons alongside 'manual' and 'vote_based'
       - Label: "Predictive" with description "Predictions auto-resolve the bracket. Teacher paces the reveal."
       - Update the Step 4 review summary to show "Predictive" for auto mode
       - The radio name="pred-resolution" already exists -- add a third option

    2. Create prediction-preview.tsx -- New component for teacher preview with override controls:

       Interface PredictionPreviewProps:
         bracket: BracketWithDetails
         tabulationResults: TabulationResult[]
         onOverride: (matchupId: string, winnerId: string) => void
         onRelease: () => void
         onReopen: () => void
         unresolvedCount: number
         isPending: boolean

       Component structure:
       - Header: "Preview Results" with unresolvedCount badge (amber if > 0)
       - If unresolvedCount > 0: amber alert banner "X matchups need winners before release"
       - Full bracket diagram rendered via BracketDiagram
       - Each matchup in the diagram is clickable (via onMatchupClick callback)
       - Clicking a matchup opens a modal showing the two entrants with radio buttons to select winner
       - Matchups with status 'tie' or 'no_predictions' have amber border/background highlighting
       - Group unresolved matchups by round with "Round N" headers at top for quick navigation
       - Show vote counts for each entrant ONLY in the override modal (teacher only, never students)
       - "Release Results" button (disabled when unresolvedCount > 0) with confirmation dialog:
         Title: "Release Results?"
         Body: "Are you sure? This will begin the round-by-round reveal to students. This cannot be undone."
         Confirm: "Yes, Release Results"
         Cancel: "Cancel"
       - "Reopen Predictions" button (secondary/outline style) for safety net

       Override modal details:
       - Modal title: "Round {N}, Match {position}"
       - Entrant 1 name with vote count (e.g., "12 predictions")
       - Entrant 2 name with vote count
       - If tie: "Tied — teacher decides" label
       - If no predictions: "No predictions — teacher decides" label
       - Radio buttons to select winner
       - "Confirm Override" button calls onOverride(matchupId, selectedWinnerId)
       - Show warning: "Overriding will recalculate all downstream matchups"

    The component follows the existing modal pattern (useState toggle, overlay with Card content, similar to BracketLifecycleControls delete confirmation).
  </action>
  <verify>
    `npx tsc --noEmit` passes with no type errors.
    bracket-form.tsx includes 'auto' in resolution mode options.
    prediction-preview.tsx exports PredictionPreview component.
  </verify>
  <done>
    Bracket form has "Predictive" as a third resolution mode. PredictionPreview component renders the full bracket with override controls, tie highlighting, confirmation dialog, and reopen button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teacher auto-resolution flow in PredictiveBracket + LiveDashboard</name>
  <files>
    src/components/bracket/predictive-bracket.tsx
    src/components/teacher/live-dashboard.tsx
  </files>
  <action>
    1. In predictive-bracket.tsx, extend TeacherPredictiveView for auto mode:

       The existing teacher view shows status buttons (Open Predictions, Close Predictions).
       For auto mode (bracket.predictiveResolutionMode === 'auto'), add new states:

       - predictionStatus 'predictions_open': Show "Close Predictions" button (existing) + "Prepare Results" button. "Prepare Results" calls prepareResults action. Show loading spinner with "Tabulating predictions..." text during transition (useTransition).

       - predictionStatus 'tabulating': Show "Preparing results..." loading state (brief, usually < 1 second).

       - predictionStatus 'previewing': Render PredictionPreview component.
         Pass tabulationResults from bracket matchups (derive from matchup data).
         Wire onOverride to overrideWinner action.
         Wire onRelease to releaseResults action.
         Wire onReopen to reopenPredictions action.

       - predictionStatus 'revealing': Show two tabs (Bracket + Leaderboard) plus "Reveal Next Round" button.
         Show current reveal progress: "Round {revealedUpToRound} of {totalRounds} revealed"
         "Reveal Next Round" button calls revealNextRound action with round = revealedUpToRound + 1.
         3-2-1 countdown overlay between rounds (reuse WinnerReveal countdown pattern -- import CountdownOverlay from countdown-overlay.tsx, to be created in Plan 04).
         After all rounds revealed: show "Complete Bracket" button.
         Bracket tab shows BracketDiagram with decided matchups filled in.
         Leaderboard tab shows PredictionLeaderboard (existing component).
         Add presentation mode toggle (F key shortcut): wrap content in PresentationMode when active.
         Two sub-buttons in presentation mode: "Show Bracket" / "Show Leaderboard" to toggle content.

       - predictionStatus 'completed': Show completed state (existing pattern, read-only bracket + leaderboard).

       For manual/vote_based modes: behavior unchanged (existing code path).

    2. In live-dashboard.tsx:
       The live dashboard already routes by bracketType. For predictive brackets with auto mode, the dashboard should render the extended TeacherPredictiveView from predictive-bracket.tsx.
       - Check if the predictive bracket section in the live dashboard already delegates to PredictiveBracket. If so, the auto mode flow will work automatically through the updated TeacherPredictiveView.
       - If not, add routing: when bracketType='predictive' && predictiveResolutionMode='auto', render PredictiveBracket with isTeacher=true.
       - Ensure the live dashboard passes the bracket's revealedUpToRound to PredictiveBracket (already available via BracketWithDetails).

    Note: For the countdown overlay between reveal rounds, import from '@/components/bracket/countdown-overlay' which will be created in Plan 04. If that component doesn't exist yet at execution time, create a minimal placeholder (a simple 3-2-1 motion animation component) inline or skip it -- Plan 04 will build the full version. Use a conditional import or a local fallback.

    IMPORTANT: The "Release is committed" decision means: once releaseResults is called, there is no undo. The UI should NOT show a "cancel reveal" button during the revealing phase.
  </action>
  <verify>
    `npx tsc --noEmit` passes with no type errors.
    TeacherPredictiveView handles all 5 auto-mode states: predictions_open, tabulating, previewing, revealing, completed.
    Presentation mode works via F key (reuses PresentationMode component from polls).
  </verify>
  <done>
    Teacher can manage the full auto-resolution lifecycle from the UI: close predictions, prepare results, preview/override, release, reveal round by round, and complete bracket. Presentation mode available for classroom projection.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Bracket form shows 3 resolution modes: Manual, Vote Based, Predictive
- TeacherPredictiveView renders correct UI for each auto-mode predictionStatus
- PredictionPreview shows clickable matchups with tie/no-prediction highlighting
- Override modal works with confirmation
- Release Results has confirmation dialog
- Reveal controls show round progress and "Reveal Next Round" button
- Presentation mode toggles with F key
- "Complete Bracket" button shown after all rounds revealed
</verification>

<success_criteria>
Teachers can create a predictive bracket with "Predictive" resolution mode, manage the full auto-resolution lifecycle through the dashboard (prepare, preview/override, release, reveal round-by-round, complete), and project results via presentation mode.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-predictive-auto-resolution/07.1-03-SUMMARY.md`
</output>
