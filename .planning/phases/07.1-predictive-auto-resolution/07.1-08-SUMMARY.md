---
phase: "07.1"
plan: "08"
subsystem: "predictive-bracket-student-routing, teacher-ui-buttons"
tags: [gap-closure, bug-fix, auto-mode, predictive, routing, ui]
dependency_graph:
  requires: ["07.1-01", "07.1-02", "07.1-03", "07.1-04"]
  provides: ["completed-auto-mode-podium-routing", "combined-close-prepare-button"]
  affects: ["student-bracket-page", "teacher-predictive-view"]
tech_stack:
  added: []
  patterns: ["conditional-state-routing-by-resolution-mode"]
key_files:
  created: []
  modified:
    - src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx
    - src/components/bracket/predictive-bracket.tsx
key_decisions:
  - "Route completed auto-mode predictive brackets to 'ready' state (PredictiveStudentView) instead of 'completed' state, enabling PredictionReveal podium"
  - "Use participantId non-null assertion in completed routing since early return at line 109 guarantees non-null by line 148"
  - "Single combined button replaces Fragment-wrapped two-button layout, eliminating invalid auto-mode status transition"
metrics:
  duration: "~1.5m"
  completed: "2026-02-14T20:28:53Z"
---

# Phase 07.1 Plan 08: R2 UAT Gap Closure -- Auto-Mode Routing & Button Fix

Fix completed auto-mode predictive brackets routing to PredictionReveal podium celebration instead of "Predictions are closed", and replace non-functional Close Predictions button with single combined action button.

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~1.5m |
| Start | 2026-02-14T20:27:28Z |
| End | 2026-02-14T20:28:53Z |
| Tasks | 2/2 |
| Files Modified | 2 |

## Accomplishments

1. **Student routing fix**: Completed auto-mode predictive brackets now route through PredictiveStudentView which renders PredictionReveal with podium celebration, instead of falling through to the generic completed branch that shows "Predictions are closed"

2. **Teacher UI button fix**: Auto-mode predictions_open state now shows a single "Close Predictions & Prepare Results" button calling handlePrepareResults() instead of two buttons (one non-functional "Close Predictions" calling invalid handleStatusChange('active') and a separate "Prepare Results")

## Task Commits

| Task | Description | Commit | Key Changes |
|------|-------------|--------|-------------|
| 1 | Route completed auto-mode predictive brackets to PredictiveStudentView | d0178e0 | Added special-case routing before generic completed branch |
| 2 | Replace non-functional Close Predictions button with combined action | 81c21e8 | Single button replacing Fragment with two buttons |

## Files Modified

| File | Changes |
|------|---------|
| `src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx` | Added predictiveResolutionMode === 'auto' check in completed branch, routes to 'ready' state |
| `src/components/bracket/predictive-bracket.tsx` | Replaced Fragment-wrapped two-button layout with single combined "Close Predictions & Prepare Results" button |

## Decisions Made

1. **Routing to 'ready' state**: For completed auto-mode predictive brackets, routing to 'ready' state (not a new state) is the minimal fix. PredictiveStudentView computes effectiveStatus from predictionStatus, which for a completed bracket will be 'completed', triggering the PredictionReveal path at line 774.

2. **participantId non-null assertion**: By line 148, participantId is guaranteed non-null because the early return at line 109 catches the null case. Used `participantId!` to assert this to TypeScript.

3. **Combined button handler**: Kept handlePrepareResults as the handler (same as original "Prepare Results" button) since it performs the complete close + tabulate workflow for auto-mode.

## Deviations from Plan

None -- plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

Phase 7.1 gap closure is now fully complete. All 8 plans (4 original + 4 gap closure) delivered. Both R2 UAT issues resolved. Ready for Phase 8 (Sports Integration).

## Self-Check: PASSED

- [x] `src/app/(student)/session/[sessionId]/bracket/[bracketId]/page.tsx` -- FOUND
- [x] `src/components/bracket/predictive-bracket.tsx` -- FOUND
- [x] git log --grep="07.1-08" returns 2 commits (d0178e0, 81c21e8)
