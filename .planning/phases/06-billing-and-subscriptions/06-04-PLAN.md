---
phase: 06-billing-and-subscriptions
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/components/billing/upgrade-prompt.tsx
  - src/components/billing/plan-badge.tsx
  - src/app/(dashboard)/billing/welcome/page.tsx
  - src/components/dashboard/sidebar-nav.tsx
  - src/components/dashboard/shell.tsx
autonomous: true

must_haves:
  truths:
    - "Lock icon + tooltip appears inline when teacher encounters a locked feature"
    - "Upgrade prompt says 'Upgrade to Pro' or 'Upgrade to Pro Plus' based on current tier and required tier"
    - "After successful checkout, teacher lands on welcome page showing what they unlocked"
    - "Sidebar navigation includes a Billing link"
    - "Dashboard shows current plan badge with key usage metrics"
  artifacts:
    - path: "src/components/billing/upgrade-prompt.tsx"
      provides: "Inline lock icon with tooltip upgrade prompt"
      exports: ["UpgradePrompt"]
    - path: "src/components/billing/plan-badge.tsx"
      provides: "Current plan display badge component"
      exports: ["PlanBadge"]
    - path: "src/app/(dashboard)/billing/welcome/page.tsx"
      provides: "Post-checkout welcome/confirmation page"
    - path: "src/components/dashboard/sidebar-nav.tsx"
      provides: "Updated sidebar with Billing nav item"
  key_links:
    - from: "src/components/billing/upgrade-prompt.tsx"
      to: "src/lib/gates/features.ts"
      via: "Uses SubscriptionTier type for tier comparison"
      pattern: "SubscriptionTier"
    - from: "src/app/(dashboard)/billing/welcome/page.tsx"
      to: "src/lib/stripe.ts"
      via: "stripe.checkout.sessions.retrieve for session verification"
      pattern: "stripe\\.checkout\\.sessions\\.retrieve"
    - from: "src/components/dashboard/shell.tsx"
      to: "src/components/billing/plan-badge.tsx"
      via: "PlanBadge displayed on dashboard"
      pattern: "PlanBadge"
---

<objective>
Create the upgrade prompt component, plan badge, post-checkout welcome page, and update sidebar navigation and dashboard to integrate billing awareness throughout the teacher experience.

Purpose: These components make the billing system visible and useful in the teacher's day-to-day experience. The UpgradePrompt is the passive nudge shown when encountering locked features. The PlanBadge shows current plan at a glance. The welcome page confirms a successful subscription. The sidebar and dashboard updates make billing accessible and informative.

Output: UpgradePrompt component, PlanBadge component, welcome page at /billing/welcome, sidebar Billing link, dashboard plan card with usage metrics.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-billing-and-subscriptions/06-CONTEXT.md
@.planning/phases/06-billing-and-subscriptions/06-RESEARCH.md
@.planning/phases/06-billing-and-subscriptions/06-02-SUMMARY.md
@src/components/dashboard/sidebar-nav.tsx
@src/components/dashboard/shell.tsx
@src/lib/gates/tiers.ts
@src/lib/gates/features.ts
@src/lib/stripe.ts
@src/config/pricing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: UpgradePrompt and PlanBadge components</name>
  <files>
    src/components/billing/upgrade-prompt.tsx
    src/components/billing/plan-badge.tsx
  </files>
  <action>
    1. Create `src/components/billing/upgrade-prompt.tsx`:
       - Client component (needs hover state for tooltip)
       - Props: `{ currentTier: SubscriptionTier, requiredTier: SubscriptionTier, featureName: string }`
       - Renders inline: small Lock icon (lucide-react, h-3.5 w-3.5) + hover tooltip
       - Tooltip text: "Upgrade to {tierLabel} to unlock {featureName}"
       - `tierLabel` maps: 'pro' -> 'Pro', 'pro_plus' -> 'Pro Plus'
       - Tooltip uses CSS-only approach: `invisible group-hover:visible` on an absolute-positioned span
       - Include an optional `href` prop pointing to `/billing` for clickable upgrade prompts
       - If href provided, wrap in a Link; otherwise just show the icon + tooltip
       - Styling: `text-muted-foreground` for the icon, tooltip in `bg-popover text-popover-foreground shadow-md`
       - Do NOT show if `currentTier >= requiredTier` (caller should check, but add a safety guard)

    2. Create `src/components/billing/plan-badge.tsx`:
       - Server component (no client interactivity needed)
       - Props: `{ tier: SubscriptionTier }`
       - Renders a small badge showing the plan name:
         - 'free': "Free" with neutral styling (bg-muted text-muted-foreground)
         - 'pro': "Pro" with primary styling (bg-primary/10 text-primary)
         - 'pro_plus': "Pro Plus" with accent styling (bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-200 or similar)
       - Badge uses `rounded-full px-2.5 py-0.5 text-xs font-medium` (consistent with existing plan badge in dashboard shell)

    IMPORTANT: UpgradePrompt must be non-intrusive (per CONTEXT.md). Lock icon + tooltip only -- no modals, no banners, no blocking overlays. Passive nudge that only appears when teacher encounters the locked feature.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - UpgradePrompt renders lock icon with hover tooltip
    - PlanBadge renders appropriate badge for each tier
  </verify>
  <done>UpgradePrompt component shows non-intrusive lock icon + tooltip for locked features with smart tier targeting. PlanBadge component renders color-coded plan name badge for Free/Pro/Pro Plus tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Welcome page, sidebar nav update, and dashboard plan card</name>
  <files>
    src/app/(dashboard)/billing/welcome/page.tsx
    src/components/dashboard/sidebar-nav.tsx
    src/components/dashboard/shell.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/billing/welcome/page.tsx`:
       - Server component with auth guard
       - Read `session_id` from searchParams (URL query parameter from Stripe redirect)
       - If no session_id, redirect to /billing
       - Retrieve the Checkout Session from Stripe server-side:
         ```typescript
         const checkoutSession = await stripe.checkout.sessions.retrieve(sessionId, {
           expand: ['subscription'],
         })
         ```
       - Extract tier from the subscription's price ID using `priceIdToTier()`
       - Display a confirmation page:
         - Heading: "Welcome to {Plan Name}!" (e.g., "Welcome to Pro!")
         - Subheading: "Your subscription is active. Here's what you just unlocked:"
         - Feature list from PLANS config for the new tier
         - "Go to Dashboard" button linking to /dashboard
         - "Manage Subscription" button linking to /billing
       - If the checkout session isn't completed (payment_status !== 'paid'), show a "Processing..." state with a message: "Your payment is being processed. Your new features will be available shortly."
       - Handle errors gracefully (invalid session_id, Stripe API failure) -- redirect to /billing

    2. Update `src/components/dashboard/sidebar-nav.tsx`:
       - Add `CreditCard` icon import from lucide-react
       - Add a "Billing" nav item to the `navItems` array:
         ```typescript
         { label: 'Billing', href: '/billing', icon: CreditCard }
         ```
       - Place it after "Sessions" and before the Activities section

    3. Update `src/components/dashboard/shell.tsx`:
       - Replace the plain text tier badge with the `<PlanBadge tier={teacher.subscriptionTier as SubscriptionTier} />` component
       - Import `PlanBadge` from `@/components/billing/plan-badge`
       - Import `getTeacherBillingOverview` from `@/lib/dal/billing`
       - After existing session data, call `getTeacherBillingOverview(teacher.id)` to get usage data
       - Add a "Plan & Usage" card to the dashboard grid:
         - Shows PlanBadge
         - Shows usage: "{X} of {limit} live brackets", "{X} of {limit} draft brackets"
         - For paid tiers with higher limits, show the actual count without the "of" limit
         - "View Plan" link to /billing
       - Keep the existing session cards and layout

    IMPORTANT: Welcome page retrieves Checkout Session directly from Stripe (not dependent on webhook processing) to avoid the race condition where webhook hasn't updated the Teacher record yet. This is the recommended pattern from 06-RESEARCH.md.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - /billing/welcome page renders with session_id param
    - Sidebar shows Billing link with CreditCard icon
    - Dashboard shell shows PlanBadge component and Plan & Usage card
  </verify>
  <done>Welcome page confirms successful subscription by reading Checkout Session directly from Stripe. Sidebar navigation includes Billing link. Dashboard shell shows PlanBadge and usage metrics card with live/draft bracket counts.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. UpgradePrompt shows lock icon + tooltip with correct tier targeting
3. PlanBadge renders appropriate styling for all 3 tiers
4. /billing/welcome reads session_id and displays subscription confirmation
5. Sidebar nav includes Billing link
6. Dashboard shows plan badge and usage metrics
</verification>

<success_criteria>
- UpgradePrompt is non-intrusive (lock icon + tooltip only)
- Smart tier targeting: prompt says correct upgrade target tier
- Welcome page avoids webhook race condition by reading Stripe directly
- Sidebar includes Billing nav with CreditCard icon
- Dashboard plan card shows current tier and usage counts
- Plan badge uses color-coded styling for each tier
</success_criteria>

<output>
After completion, create `.planning/phases/06-billing-and-subscriptions/06-04-SUMMARY.md`
</output>
