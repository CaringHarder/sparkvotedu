---
phase: 06-billing-and-subscriptions
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/components/billing/pricing-cards.tsx
  - src/components/billing/pricing-toggle.tsx
  - src/app/pricing/page.tsx
  - src/app/(dashboard)/billing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Public visitor can view pricing page at /pricing without authentication"
    - "Pricing page shows 3-column layout with Free, Pro (highlighted), and Pro Plus cards"
    - "Monthly/Annual toggle switches displayed prices with annual showing ~20% savings"
    - "Authenticated teacher sees their current plan highlighted and appropriate CTAs"
    - "Subscribe button initiates Stripe Checkout for the selected plan and interval"
  artifacts:
    - path: "src/components/billing/pricing-cards.tsx"
      provides: "3-column pricing card layout with tier comparison"
      exports: ["PricingCards"]
    - path: "src/components/billing/pricing-toggle.tsx"
      provides: "Monthly/Annual billing interval toggle"
      exports: ["PricingToggle"]
    - path: "src/app/pricing/page.tsx"
      provides: "Public pricing page"
    - path: "src/app/(dashboard)/billing/page.tsx"
      provides: "In-app billing management page for authenticated teachers"
  key_links:
    - from: "src/components/billing/pricing-cards.tsx"
      to: "src/actions/billing.ts"
      via: "createCheckoutSession server action on subscribe click"
      pattern: "createCheckoutSession"
    - from: "src/components/billing/pricing-cards.tsx"
      to: "src/config/pricing.ts"
      via: "PLANS and FREE_PLAN for card content"
      pattern: "import.*pricing"
    - from: "src/app/pricing/page.tsx"
      to: "src/components/billing/pricing-cards.tsx"
      via: "renders PricingCards without currentTier"
      pattern: "PricingCards"
    - from: "src/app/(dashboard)/billing/page.tsx"
      to: "src/lib/dal/auth.ts"
      via: "getAuthenticatedTeacher for current tier"
      pattern: "getAuthenticatedTeacher"
---

<objective>
Build the pricing page UI with 3-column card layout, Monthly/Annual toggle, and both public (/pricing) and in-app (/billing) routes. The pricing cards adapt based on whether the visitor is authenticated and what their current tier is.

Purpose: This is the primary surface where teachers discover and subscribe to paid plans. The public /pricing page is for visitors exploring the product. The in-app /billing page shows authenticated teachers their current plan and lets them manage it. Both share the same PricingCards and PricingToggle components with different context.

Output: PricingCards component, PricingToggle component, public /pricing page, in-app /billing page with current plan display and manage subscription button.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-billing-and-subscriptions/06-CONTEXT.md
@.planning/phases/06-billing-and-subscriptions/06-RESEARCH.md
@.planning/phases/06-billing-and-subscriptions/06-01-SUMMARY.md
@.planning/phases/06-billing-and-subscriptions/06-02-SUMMARY.md
@src/config/pricing.ts
@src/actions/billing.ts
@src/lib/dal/auth.ts
@src/components/dashboard/sidebar-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PricingToggle and PricingCards components</name>
  <files>
    src/components/billing/pricing-toggle.tsx
    src/components/billing/pricing-cards.tsx
  </files>
  <action>
    1. Create `src/components/billing/pricing-toggle.tsx`:
       - `'use client'` component
       - Props: `interval: BillingInterval`, `onIntervalChange: (interval: BillingInterval) => void`
       - Render a segmented control with "Monthly" and "Annual" options
       - "Annual" option shows a small "Save ~20%" badge
       - Use Tailwind for styling: rounded-full pill background, active segment gets `bg-primary text-primary-foreground`, inactive gets `text-muted-foreground`
       - Toggle defaults to "monthly" (controlled by parent)

    2. Create `src/components/billing/pricing-cards.tsx`:
       - `'use client'` component (needs state for interval toggle + form action)
       - Props:
         - `currentTier?: SubscriptionTier` (undefined for public visitors, set for authenticated teachers)
       - Internal state: `interval` (BillingInterval, defaults to 'monthly')
       - Import `PLANS`, `FREE_PLAN` from `@/config/pricing.ts`
       - Import `createCheckoutSession` from `@/actions/billing.ts`

       **Layout:** 3-column grid (responsive: 1 col on mobile, 3 on lg)
       - Column 1: Free tier card
       - Column 2: Pro card (visually emphasized with "Most Popular" badge, accent border or larger)
       - Column 3: Pro Plus card

       **Each card contains:**
       - Plan name
       - Price: "$X/mo" (or "$X/mo billed annually" for annual interval). Free shows "$0" or "Free forever"
       - Feature list with checkmarks (additive: Free shows base, Pro shows "Everything in Free plus...", Pro Plus "Everything in Pro plus...")
       - CTA button:
         - If `currentTier` not provided (public): Free shows "Get Started" (links to /signup), Pro/Pro Plus show "Start with {Plan}" (links to /signup)
         - If `currentTier === tier`: Show "Current Plan" (disabled button)
         - If `currentTier < tier`: Show "Upgrade to {Plan}" (calls createCheckoutSession with appropriate priceId based on interval)
         - If `currentTier > tier`: Show "Downgrade" or no button (downgrade happens via Customer Portal, not direct checkout)

       **Subscribe button implementation:**
       Use a form with a hidden input for priceId. The form's action calls `createCheckoutSession`. The priceId is selected from PLANS based on the plan key and current interval (monthly vs annual).

       **Pro card emphasis (from CONTEXT.md):**
       - Add "Most Popular" badge at top of Pro card
       - Use `ring-2 ring-primary` or `border-primary border-2` for visual emphasis
       - Slightly larger scale or shadow compared to other cards

       **Styling notes:**
       - Cards use `rounded-lg border bg-card` base (consistent with existing project card styles)
       - Feature list items use Check icon (lucide-react) in `text-primary` color
       - Price uses `text-4xl font-bold` for the dollar amount
       - Annual pricing shows monthly equivalent with "billed annually" note
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - PricingToggle renders Monthly/Annual options
    - PricingCards renders 3 columns with correct content from PLANS config
    - CTA buttons adapt based on currentTier prop
  </verify>
  <done>PricingToggle component provides Monthly/Annual billing interval selection. PricingCards component renders 3-column pricing layout with Free/Pro/Pro Plus cards, feature lists, interval-aware pricing, "Most Popular" emphasis on Pro, and context-aware CTAs (public vs authenticated, current tier vs upgrade).</done>
</task>

<task type="auto">
  <name>Task 2: Public /pricing page and in-app /billing page</name>
  <files>
    src/app/pricing/page.tsx
    src/app/(dashboard)/billing/page.tsx
  </files>
  <action>
    1. Create `src/app/pricing/page.tsx` -- Public pricing page:
       - Server component (no auth required -- public route)
       - Page title: "Pricing" or "Simple, transparent pricing"
       - Subtitle: "Choose the plan that fits your classroom"
       - Render `<PricingCards />` without `currentTier` prop (public visitor mode)
       - Add basic layout: centered content, max-w-6xl, padding
       - Include a "Already have an account? Sign in" link at bottom
       - Add the `/pricing` route to the `PUBLIC_PAGES` array in `src/app/proxy.ts` so it's accessible without auth

    2. Create `src/app/(dashboard)/billing/page.tsx` -- In-app billing page:
       - Server component with auth guard
       - Call `getAuthenticatedTeacher()` -- redirect to /login if null
       - Call `getTeacherBillingOverview(teacher.id)` from billing DAL for usage data
       - Page title: "Billing & Plan"
       - **Current plan section:**
         - Show plan name badge (Free / Pro / Pro Plus)
         - Show usage summary: "X of Y live brackets", "X of Y draft brackets" (from billing overview)
         - If subscription exists and subscription.cancelAtPeriodEnd: show "Your plan will be downgraded to Free on {date}"
       - Render `<PricingCards currentTier={teacher.subscriptionTier as SubscriptionTier} />`
       - **Manage subscription section** (only if teacher has stripeCustomerId):
         - "Manage Subscription" button that calls `createPortalSession` server action (opens Stripe Customer Portal)
         - Use a form with the server action, matching existing form-action patterns

    IMPORTANT: The public /pricing page must NOT import from `src/lib/dal/auth.ts` or any authenticated context. It renders PricingCards in public mode only. Update proxy.ts to add '/pricing' to PUBLIC_PAGES array.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `/pricing` is accessible without auth (added to PUBLIC_PAGES in proxy.ts)
    - `/billing` requires auth and shows current plan + usage
    - PricingCards receives correct currentTier on billing page
    - Manage Subscription button visible for subscribed teachers
  </verify>
  <done>Public /pricing page renders pricing cards for visitors without auth. In-app /billing page shows authenticated teacher's current plan, usage metrics, pricing comparison, and Manage Subscription button. Both pages share PricingCards component with different context.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. /pricing route accessible without authentication
3. /billing route requires authentication and shows current plan
4. 3-column pricing layout with Pro highlighted as "Most Popular"
5. Monthly/Annual toggle switches displayed prices
6. Subscribe buttons call createCheckoutSession with correct priceId
7. Manage Subscription button calls createPortalSession
</verification>

<success_criteria>
- Public /pricing page shows 3-column pricing with all tier features
- Monthly/Annual toggle works with ~20% annual savings displayed
- Pro card visually emphasized as "Most Popular"
- Authenticated teacher sees current plan highlighted
- Subscribe buttons initiate Stripe Checkout with correct price
- Billing page shows usage metrics alongside pricing
- Manage Subscription opens Stripe Customer Portal
</success_criteria>

<output>
After completion, create `.planning/phases/06-billing-and-subscriptions/06-03-SUMMARY.md`
</output>
