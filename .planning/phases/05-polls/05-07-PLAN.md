---
phase: 05-polls
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/poll/poll-form.tsx
  - src/app/(dashboard)/polls/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Teacher can edit a draft poll and click Update Poll without the button hanging forever"
    - "Clicking the Polls sub-item in the sidebar navigates to a working page (not 404)"
  artifacts:
    - path: "src/components/poll/poll-form.tsx"
      provides: "Fixed submit handler that resets isSubmitting on all paths"
      contains: "setIsSubmitting(false)"
    - path: "src/app/(dashboard)/polls/page.tsx"
      provides: "Polls index page listing teacher polls"
  key_links:
    - from: "src/components/dashboard/sidebar-nav.tsx"
      to: "src/app/(dashboard)/polls/page.tsx"
      via: "sidebar link href=/polls"
      pattern: "href.*polls"
---

<objective>
Fix two UAT failures: (1) poll update button hangs forever after successful save, and (2) sidebar Polls link leads to 404.

Purpose: These are blocking bugs that prevent basic poll management workflow. The update hang makes editing impossible, and the 404 breaks sidebar navigation trust.
Output: Fixed poll-form.tsx submit handler + new /polls/page.tsx index page
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polls/05-UAT.md

@src/components/poll/poll-form.tsx
@src/app/(dashboard)/brackets/page.tsx
@src/components/dashboard/sidebar-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix poll update button hang</name>
  <files>src/components/poll/poll-form.tsx</files>
  <action>
  The handleSubmit function in poll-form.tsx has a bug on the update success path. When isEditing is true and the updatePoll call succeeds (no error), execution reaches `router.refresh()` on line 113 but never calls `setIsSubmitting(false)`. The button stays in "Updating..." loading state permanently.

  Fix: Wrap the entire try block body in a try/finally so that `setIsSubmitting(false)` is called on ALL exit paths (success, error return, and catch). Remove the individual `setIsSubmitting(false)` calls from the error handling paths (lines 109, 130, 140) since finally handles them all.

  Specifically in the handleSubmit useCallback:
  1. Keep `setIsSubmitting(true)` and `setError(null)` before the try
  2. Change the outer try/catch to try/catch/finally
  3. Move `setIsSubmitting(false)` into the finally block
  4. Remove the `setIsSubmitting(false)` on line 109 (update error path)
  5. Remove the `setIsSubmitting(false)` on line 130 (create error path)
  6. Remove the `setIsSubmitting(false)` on line 140 (catch path)

  The finally block ensures isSubmitting is reset regardless of outcome. The error-path early returns still work correctly because finally runs before the function returns.

  Do NOT change any other behavior -- only the isSubmitting state management.
  </action>
  <verify>
  Open the app at /polls/[any-draft-poll-id]. Edit the question text. Click "Update Poll". The button should show "Updating..." briefly, then return to "Update Poll" (not hang). Verify with: `grep -n "finally" src/components/poll/poll-form.tsx` shows the finally block exists.
  </verify>
  <done>Update Poll button returns to normal state after successful save. No infinite loading spinner.</done>
</task>

<task type="auto">
  <name>Task 2: Create /polls index page</name>
  <files>src/app/(dashboard)/polls/page.tsx</files>
  <action>
  Create `src/app/(dashboard)/polls/page.tsx` as a dedicated polls list page, following the exact same pattern as `src/app/(dashboard)/brackets/page.tsx`.

  The page should:
  1. Be an async server component (default export `PollsPage`)
  2. Call `getAuthenticatedTeacher()` from `@/lib/dal/auth` and redirect to `/login` if null
  3. Call `getPollsByTeacherDAL(teacher.id)` from `@/lib/dal/poll` to fetch all polls
  4. Serialize each poll for client rendering: `{ id, question, description, pollType, status, createdAt: .toISOString(), updatedAt: .toISOString(), options: [...], _count }`
  5. Render a header row with "Polls" title (h1, text-2xl font-bold tracking-tight) and a "Create Poll" link button (href="/polls/new") with a Plus icon from lucide-react
  6. If no polls exist, show an empty state with dashed border, "No polls yet" message, and a "Create your first poll" link to /polls/new
  7. If polls exist, render them in a grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4)
  8. Each poll card should be a Link to `/polls/${poll.id}` wrapped in a Card component with: question as title, pollType badge (capitalize), PollStatusBadge component, option count, vote count from _count.votes, and relative timestamp

  Import PollStatusBadge from `@/components/poll/poll-status`. Use Card, CardContent, CardHeader from `@/components/ui/card`. Use BarChart3 and Plus icons from lucide-react.

  Keep it simple -- this is a list page, not a complex filtering view (the /activities page already has filtering). The poll cards here should be clickable and navigate to the detail page.
  </action>
  <verify>
  1. `ls src/app/\(dashboard\)/polls/page.tsx` confirms file exists
  2. Navigate to /polls in the browser -- should render the polls list (not 404)
  3. Sidebar "Polls" link should now navigate successfully
  4. `npx tsc --noEmit 2>&1 | head -20` shows no type errors
  </verify>
  <done>Sidebar Polls link navigates to /polls which displays a working polls list page. No 404.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Poll update button no longer hangs (try/finally pattern)
3. /polls page renders polls list (no 404)
4. Sidebar navigation to Polls works end-to-end
</verification>

<success_criteria>
- Update Poll button returns to normal state after save (not stuck on "Updating...")
- /polls route renders a polls list page matching the /brackets page pattern
- Zero TypeScript compilation errors
- Both UAT Test 4 and Test 7 would now pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-polls/05-07-SUMMARY.md`
</output>
