---
phase: 05-polls
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/lib/poll/templates.ts
  - src/components/poll/poll-form.tsx
  - src/components/poll/poll-wizard.tsx
  - src/components/poll/option-list.tsx
  - src/components/poll/poll-card.tsx
  - src/components/poll/poll-status.tsx
  - src/app/(dashboard)/polls/new/page.tsx
  - src/app/(dashboard)/polls/[pollId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher can create a simple poll by filling a quick-create form with question + options and clicking create"
    - "Teacher can create a ranked poll by toggling poll type and configuring ranking depth"
    - "Teacher can edit a draft poll's question, description, options, and settings"
    - "Teacher can duplicate an existing poll to create a new one"
    - "Teacher can select from curated poll templates as a starting point"
    - "Poll options support reorder via drag-and-drop and add/remove"
  artifacts:
    - path: "src/lib/poll/templates.ts"
      provides: "Curated poll templates by category (icebreakers, classroom decisions, academic, fun/trivia, feedback)"
      min_lines: 50
    - path: "src/components/poll/poll-form.tsx"
      provides: "Quick-create inline poll form with question, type toggle, options"
      min_lines: 60
    - path: "src/components/poll/poll-wizard.tsx"
      provides: "Multi-step wizard for poll creation (question, options, settings, review)"
      min_lines: 80
    - path: "src/components/poll/option-list.tsx"
      provides: "Editable option list with add, remove, reorder, image URL placeholder"
      min_lines: 50
    - path: "src/app/(dashboard)/polls/new/page.tsx"
      provides: "Poll creation page with template selection and creation forms"
    - path: "src/app/(dashboard)/polls/[pollId]/page.tsx"
      provides: "Poll detail/edit page for viewing and modifying draft polls"
  key_links:
    - from: "src/components/poll/poll-form.tsx"
      to: "src/actions/poll.ts"
      via: "createPoll server action call"
      pattern: "import.*createPoll.*from.*actions/poll"
    - from: "src/components/poll/poll-wizard.tsx"
      to: "src/actions/poll.ts"
      via: "createPoll server action call"
      pattern: "createPoll"
    - from: "src/app/(dashboard)/polls/[pollId]/page.tsx"
      to: "src/lib/dal/poll.ts"
      via: "server component fetches poll data"
      pattern: "getPollByIdDAL"
---

<objective>
Build the teacher-facing poll creation and editing UI: quick-create form, multi-step wizard, templates, option management, and teacher poll pages.

Purpose: Teachers need to create and manage polls before students can vote on them. This provides the creation path.
Output: Working poll creation (two paths: quick and wizard), template selection, option editing, and poll detail/edit pages.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polls/05-RESEARCH.md
@.planning/phases/05-polls/05-01-SUMMARY.md
@.planning/phases/05-polls/05-02-SUMMARY.md
@src/components/bracket/bracket-form.tsx
@src/app/(dashboard)/brackets/new/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create poll templates, option list, and form components</name>
  <files>src/lib/poll/templates.ts, src/components/poll/poll-form.tsx, src/components/poll/option-list.tsx, src/components/poll/poll-status.tsx, src/components/poll/poll-card.tsx</files>
  <action>
**Create src/lib/poll/templates.ts** -- Curated poll templates by category, following the same pattern as src/lib/bracket/curated-topics.ts:

Export a POLL_TEMPLATES array with 15-20 templates across 5 categories:
- Icebreakers (4): e.g., "Would you rather...?", "What's your favorite season?", "Best pizza topping?", "Dream superpower?"
- Classroom Decisions (3): e.g., "What should we do for our class reward?", "Which topic should we cover next?", "Best day for study sessions?"
- Academic Debates (3): e.g., "Was [historical event] inevitable?", "Is math discovered or invented?", "Best scientific discovery?"
- Fun/Trivia (3): e.g., "Best movie of all time?", "Cats vs Dogs?", "Best video game genre?"
- Feedback (3): e.g., "How well do you understand today's lesson?", "Rate today's activity", "What would improve this class?"

Each template: { id: string, category: string, question: string, options: string[], pollType: 'simple' | 'ranked' }

Export POLL_TEMPLATE_CATEGORIES array of unique category names.

**Create src/components/poll/poll-status.tsx** -- Status badge component:
- Takes status: PollStatus prop
- Returns a badge with appropriate color: draft=gray, active=green, closed=amber, archived=slate
- Small rounded pill with status text, same visual style as bracket status badges

**Create src/components/poll/poll-card.tsx** -- Poll card for list views:
- Props: poll: PollData & {_count?: {votes: number}}, onEdit, onDuplicate, onDelete
- Displays: question (truncated), poll type badge (simple=indigo, ranked=purple), status badge, vote count, updatedAt
- Actions: Edit (pencil icon), Duplicate (copy icon), Delete (trash icon) with confirmation
- Use Card from shadcn/ui
- Poll type icon: BarChart3 for simple, ListOrdered for ranked (from lucide-react)

**Create src/components/poll/option-list.tsx** -- Editable option list:
- Props: options: {id: string, text: string, imageUrl?: string}[], onChange, maxOptions, minOptions (default 2)
- Each option row: drag handle, text input, remove button (X)
- "Add option" button at bottom (disabled when at maxOptions)
- HTML5 native drag-and-drop for reorder (same pattern as bracket form from 03-06)
- Auto-assign position from array index
- nanoid for client-side temp IDs (same as bracket form pattern)

**Create src/components/poll/poll-form.tsx** -- Quick-create inline form:
- 'use client' component
- Fields: question (text input), poll type toggle (simple/ranked), options (OptionList component)
- For ranked type: show rankingDepth selector (dropdown: "Rank all" or "Rank top N")
- Settings section: allowVoteChange toggle, showLiveResults toggle
- Submit button calls createPoll server action
- useActionState pattern (React 19) for form submission with loading/error states
- On success: redirect to the new poll detail page using router.push
- Template quick-start: if template is passed as prop, pre-fill question and options
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit` passes. Templates file exports POLL_TEMPLATES and POLL_TEMPLATE_CATEGORIES. Components render without errors.
  </verify>
  <done>Poll templates provide 15-20 curated starting points across 5 categories. OptionList supports add/remove/reorder with drag-and-drop. PollForm provides quick-create with all poll settings. PollCard and PollStatus provide list/display components.</done>
</task>

<task type="auto">
  <name>Task 2: Create poll wizard and teacher poll pages</name>
  <files>src/components/poll/poll-wizard.tsx, src/app/(dashboard)/polls/new/page.tsx, src/app/(dashboard)/polls/[pollId]/page.tsx</files>
  <action>
**Create src/components/poll/poll-wizard.tsx** -- Multi-step wizard:
- 'use client' component
- Use custom segmented control tabs (same pattern as bracket wizard from 03-06, NOT Radix Tabs)
- Steps:
  1. **Question**: question input, optional description textarea, poll type toggle (simple/ranked)
  2. **Options**: OptionList component, "From Template" button that opens template picker, option count validation against tier limits
  3. **Settings**: allowVoteChange toggle, showLiveResults toggle, rankingDepth (if ranked), session assignment dropdown (optional, populated from teacher's active sessions)
  4. **Review**: Summary of all fields, "Create Poll" button
- Each step has Next/Back buttons
- Step validation: Step 1 requires question (min 1 char), Step 2 requires min 2 options
- Template picker: modal/sheet showing POLL_TEMPLATE_CATEGORIES as tabs and templates as clickable cards. Selecting a template fills question + options and closes the picker.
- On create: calls createPoll server action with combined data, redirects to poll detail on success

**Create src/app/(dashboard)/polls/new/page.tsx** -- Poll creation page:
- Server component
- Displays two creation paths side by side (or tabs):
  1. "Quick Create" -- renders PollForm component
  2. "Step-by-Step" -- renders PollWizard component
- Template browser section at top: shows POLL_TEMPLATE_CATEGORIES as horizontal scrollable chips. Clicking a category shows templates. Clicking a template starts PollForm or PollWizard with that template pre-filled.
- Page header: "Create New Poll" with breadcrumb back to activities/polls list

**Create src/app/(dashboard)/polls/[pollId]/page.tsx** -- Poll detail/edit page:
- Server component that fetches poll via getPollByIdDAL
- Verifies teacher ownership by checking poll.teacherId against getAuthenticatedTeacher().id
- If poll not found or not owned: redirect to /activities
- For draft polls: renders editable PollForm pre-filled with existing data, "Update" button calls updatePoll action
- For active/closed polls: renders read-only poll details (question, options, settings) + link to live results page (/polls/[pollId]/live -- built in 05-05)
- Status actions section: status change buttons (Activate, Close, Archive, Reopen) based on current status. Each calls updatePollStatus action.
- Delete button with confirmation modal (same pattern as bracket detail from 03-07). Calls deletePoll action, redirects to /activities on success.
- Duplicate button: calls duplicatePoll action, redirects to new poll detail page.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit` passes. Pages are accessible at /polls/new and /polls/[pollId]. Poll creation form submits and creates a poll in the database.
  </verify>
  <done>Teacher can create polls via quick-create or step-by-step wizard. Template picker provides curated starting points. Poll detail page shows read-only view for active/closed polls and edit form for drafts. Status transitions and delete are accessible from the detail page.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Navigate to /polls/new -- page renders with quick-create and wizard options
3. Fill quick-create form with question + 3 options, submit -- poll created (check DB)
4. Navigate to /polls/[newPollId] -- detail page shows poll data
5. Template picker shows categories and templates, selecting one pre-fills the form
6. Edit a draft poll's question -- update persists
7. Click Duplicate -- new poll created with " (Copy)" suffix
8. Click Delete with confirmation -- poll removed
</verification>

<success_criteria>
- Quick-create form allows teacher to create a poll in under 30 seconds (question + 2-6 options)
- Wizard provides step-by-step experience for more deliberate poll creation
- Templates pre-fill question and options (15-20 templates across 5 categories)
- Option list supports add, remove, reorder (drag-and-drop), and enforces tier limits
- Poll detail page renders correctly for draft (editable) and active/closed (read-only)
- Status transitions, delete, and duplicate are functional from detail page
</success_criteria>

<output>
After completion, create `.planning/phases/05-polls/05-03-SUMMARY.md`
</output>
