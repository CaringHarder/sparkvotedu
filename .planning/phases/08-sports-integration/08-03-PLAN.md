---
phase: 08-sports-integration
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/bracket/tournament-browser.tsx
  - src/components/bracket/sports-matchup-box.tsx
  - src/app/(dashboard)/brackets/import/page.tsx
  - src/components/bracket/bracket-diagram.tsx
  - src/components/bracket/bracket-card.tsx
  - src/components/bracket/bracket-detail.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher can see a list of available NCAA March Madness tournaments (men's and women's)"
    - "Teacher can click to import a tournament as a sports bracket for their class"
    - "Imported sports bracket displays team logos alongside team names in matchup boxes"
    - "Sports matchup boxes show game status (scheduled time, LIVE badge, FINAL badge)"
    - "Sports matchup boxes show scores for in-progress and completed games"
    - "Sports brackets appear in the bracket list with a 'Sports' type badge"
  artifacts:
    - path: "src/components/bracket/tournament-browser.tsx"
      provides: "Tournament browsing UI with import buttons"
      min_lines: 60
    - path: "src/components/bracket/sports-matchup-box.tsx"
      provides: "Enhanced matchup box with logos, scores, and status badges"
      min_lines: 40
    - path: "src/app/(dashboard)/brackets/import/page.tsx"
      provides: "Import page with tournament browser and session selector"
      min_lines: 30
  key_links:
    - from: "src/components/bracket/tournament-browser.tsx"
      to: "src/actions/sports.ts"
      via: "calls getAvailableTournaments and importTournament"
      pattern: "getAvailableTournaments|importTournament"
    - from: "src/components/bracket/bracket-diagram.tsx"
      to: "src/components/bracket/sports-matchup-box.tsx"
      via: "renders SportsMatchupBox for sports brackets"
      pattern: "SportsMatchupBox"
    - from: "src/components/bracket/bracket-detail.tsx"
      to: "sports bracket routing"
      via: "routes sports bracket type to correct detail view"
      pattern: "bracketType.*sports"
---

<objective>
Build the teacher-facing UI for browsing NCAA March Madness tournaments, importing them as classroom brackets, and displaying sports-specific information (logos, scores, status badges) in the bracket diagram.

Purpose: This is the primary user-facing deliverable -- teachers interact with tournament browsing and see enhanced bracket displays. Without this plan, the sports data layer from 08-01/08-02 has no user interface.

Output: Tournament browser component + import page + sports matchup box component + bracket diagram integration
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sports-integration/08-RESEARCH.md
@.planning/phases/08-sports-integration/08-01-SUMMARY.md
@.planning/phases/08-sports-integration/08-02-SUMMARY.md
@src/components/bracket/bracket-diagram.tsx
@src/components/bracket/bracket-card.tsx
@src/components/bracket/bracket-detail.tsx
@src/components/bracket/region-bracket-view.tsx
@src/components/bracket/bracket-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tournament browser component + import page</name>
  <files>
    src/components/bracket/tournament-browser.tsx
    src/app/(dashboard)/brackets/import/page.tsx
  </files>
  <action>
    **TournamentBrowser component** -- Create `src/components/bracket/tournament-browser.tsx` as a 'use client' component:

    Props: `{ sessions: Array<{ id: string; code: string; name: string | null }> }`

    State:
    - `tournaments: SportsTournament[]` (fetched on mount)
    - `loading: boolean` (initial fetch)
    - `importing: string | null` (tournament ID being imported, for loading state)
    - `error: string | null`
    - `selectedSessionId: string` (defaults to first session)
    - `importResult: { success: boolean; message: string; bracketId?: string } | null`

    On mount (useEffect):
    - Call `getAvailableTournaments()` server action
    - If error (including feature gate), show error message with upgrade prompt if `upgradeTarget` returned
    - If tournaments returned, set state

    Render:
    - **Session selector**: Dropdown/select to pick which class session to import into. Shows session name + code. If no sessions, show message "Create a class session first to import tournaments."
    - **Tournament cards grid**: For each tournament, render a card showing:
      - Tournament name (e.g., "NCAA Men's March Madness 2026")
      - Gender badge: "Men's" or "Women's" with blue/pink color
      - Status badge: "Upcoming" (yellow), "Active" (green), "Completed" (gray)
      - Season year
      - Team count (68 teams)
      - Date range (startDate - endDate formatted)
      - "Import as Bracket" button (primary button, disabled during import or if tournament already imported)
    - **Import flow**: On button click:
      - Set `importing` to tournament ID
      - Call `importTournament({ tournamentId, season, sessionId: selectedSessionId })`
      - On success: Show success toast/message with link to new bracket
      - On error: Show error message
      - Reset `importing` to null
    - **Empty state**: If no tournaments available, show message: "No active NCAA tournaments at this time. Tournaments are available during March-April each year."
    - **Error state**: If feature gate fails, show UpgradePrompt-style message with upgrade path

    Style with Tailwind matching existing card patterns (see bracket-card.tsx). Use shadcn Button, Card components.

    **Import page** -- Create `src/app/(dashboard)/brackets/import/page.tsx` as a server component:
    - Authenticate teacher via `getAuthenticatedTeacher()`
    - Fetch active sessions for this teacher: `prisma.classSession.findMany({ where: { teacherId: teacher.id, status: 'active' } })`
    - Serialize sessions to plain objects
    - Render page with heading "Import Sports Tournament" and TournamentBrowser component
    - Add breadcrumb navigation: Brackets > Import Tournament
    - Add link back to /brackets
  </action>
  <verify>Navigate to /brackets/import and verify the page renders. If SPORTSDATAIO_API_KEY is not set, verify a descriptive error message appears. If set, verify tournament cards are displayed (or empty state if no active tournaments).</verify>
  <done>Teacher can navigate to /brackets/import, see available NCAA March Madness tournaments, select a session, and click to import. Import creates a sports bracket and redirects or shows success.</done>
</task>

<task type="auto">
  <name>Task 2: Sports matchup box + bracket diagram integration + bracket list/detail updates</name>
  <files>
    src/components/bracket/sports-matchup-box.tsx
    src/components/bracket/bracket-diagram.tsx
    src/components/bracket/bracket-card.tsx
    src/components/bracket/bracket-detail.tsx
  </files>
  <action>
    **SportsMatchupBox component** -- Create `src/components/bracket/sports-matchup-box.tsx` as a client component:

    Props: `{ matchup: MatchupData, entrants: BracketEntrantData[], isSports: boolean }`

    This component is rendered inside the SVG bracket diagram as a foreignObject overlay, OR as a standalone card depending on rendering context. Design it as a React component (not raw SVG) that can be used as an overlay.

    Content for each matchup slot (home team / away team):
    - **Team logo**: If `entrant.logoUrl` is set, render an `<img>` (not next/image in SVG context) with 16x16px size. If no logo, render team abbreviation text in a small colored circle.
    - **Team name**: `entrant.name` in normal weight
    - **Seed number**: `(entrant.seedPosition)` in muted text before team name, like "(1) Duke"
    - **Score**: If `matchup.homeScore`/`matchup.awayScore` is not null, show score in bold. Winner's score highlighted.
    - **Winner highlight**: If `matchup.winnerId === entrant.id`, apply bold text + subtle green accent (matching existing winner highlighting pattern).

    **Status badge** (rendered once per matchup, not per team):
    - If `matchup.gameStatus === 'in_progress'`: Pulsing red "LIVE" badge (small dot + text)
    - If `matchup.gameStatus === 'final'`: Muted "FINAL" text
    - If `matchup.gameStatus === 'scheduled'` and `matchup.gameStartTime`: Format date/time in muted small text (e.g., "Mar 20, 7:00 PM")
    - If `matchup.gameStatus === 'postponed'`: Yellow "POSTPONED" badge
    - If no gameStatus: render nothing (standard bracket matchup)

    Export a helper function `isSportsBracket(bracket: BracketData): boolean` that returns `bracket.bracketType === 'sports'`.

    **BracketDiagram updates** -- Modify `src/components/bracket/bracket-diagram.tsx`:
    - Accept new optional prop: `isSports?: boolean`
    - When `isSports` is true, enhance the matchup box rendering to show sports-specific content:
      - In the matchup box rect area, add the team logo (if entrant has `logoUrl`), show score (if matchup has `homeScore`/`awayScore`), and show status badge (if matchup has `gameStatus`).
      - Increase matchup box height slightly (from current height to accommodate score + status line). Use a sports-specific box height constant (e.g., `SPORTS_MATCHUP_HEIGHT = 70` vs standard `MATCHUP_HEIGHT`).
      - Keep the existing rendering for non-sports brackets completely unchanged.
    - Alternatively, if modifying BracketDiagram SVG internals is too invasive, create the sports enhancements as an overlay approach (similar to how accuracy badges were done in 07.1-10 with `accuracyMap`). Pass sports data through props and render additional SVG elements on top of standard matchup boxes. This approach is preferred if it avoids touching core SVG layout math.

    **Bracket card updates** -- Modify `src/components/bracket/bracket-card.tsx`:
    - Add 'sports' to the `BRACKET_TYPE_LABELS` map: `'sports': 'Sports'`
    - Add sports badge color: use emerald (green) for sports brackets to match the sports theme
    - If the bracket has `sportGender`, show gender indicator (e.g., small "Men's" or "Women's" text)

    **Bracket detail updates** -- Modify `src/components/bracket/bracket-detail.tsx`:
    - Add routing for `bracketType === 'sports'`:
      - Sports brackets render the standard BracketDiagram with `isSports={true}` prop
      - For 64+ entrant sports brackets, use RegionBracketView with region navigation (matching existing 32+/64+ patterns)
      - Show a "Last synced" timestamp from `bracket.lastSyncAt`
      - Show a "Manual Sync" button that calls `triggerSportsSync` action
      - Show prediction controls (Open Predictions / Close Predictions) matching existing predictive bracket pattern
      - Sports brackets are predictive by nature -- students submit predictions, then results auto-fill from API
    - Add an "Import Tournament" link/button to the brackets list page (or as a nav item) that navigates to `/brackets/import`

    **Navigation update**: Add a link to `/brackets/import` from the brackets list page. This could be a secondary button next to the existing "Create Bracket" button, labeled "Import Tournament" with a sports/trophy icon.
  </action>
  <verify>Navigate to a sports bracket detail page and verify team logos appear (or abbreviation fallback), scores show for decided games, and status badges render correctly. Verify the bracket list shows a green "Sports" type badge for sports brackets.</verify>
  <done>Sports brackets render with team logos, scores, and status badges in the diagram. Bracket list shows sports type badge. Bracket detail routes sports brackets to the correct view with sync controls. Import Tournament navigation is accessible from brackets page.</done>
</task>

</tasks>

<verification>
1. /brackets/import page renders with tournament browser
2. Tournament cards show name, gender, status, team count, and import button
3. Import button creates a sports bracket and shows success feedback
4. Sports bracket detail page shows enhanced matchup boxes with logos/scores/status
5. Bracket list shows green "Sports" badge for sports brackets
6. 64-entrant sports brackets use RegionBracketView with region navigation
7. "Last synced" timestamp and "Manual Sync" button appear on sports bracket detail
</verification>

<success_criteria>
- Teacher can browse tournaments at /brackets/import
- Teacher can import a tournament as a sports bracket
- Sports brackets display team logos, scores, and game status badges
- Navigation between brackets list and import page works
- Existing bracket types render unchanged (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/08-sports-integration/08-03-SUMMARY.md`
</output>
