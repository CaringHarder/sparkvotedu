---
phase: 29-pause-resume-go-live
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/components/ui/switch.tsx
  - src/components/teacher/live-dashboard.tsx
  - src/app/(dashboard)/polls/[pollId]/live/client.tsx
  - src/components/bracket/bracket-card.tsx
  - src/components/bracket/bracket-detail.tsx
  - src/components/poll/poll-detail-view.tsx
  - src/components/bracket/bracket-status.tsx
  - src/components/poll/poll-status.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Teacher sees a toggle switch in the live dashboard header that flips between Active and Paused states"
    - "Teacher sees an amber 'Activity Paused' banner across the dashboard when activity is paused"
    - "Toggle switch works identically on bracket live dashboard and poll live dashboard"
    - "All 'View Live' labels now read 'Go Live' throughout the app"
    - "'Go Live' button is always visible regardless of activity state (draft, active, paused, completed)"
    - "'Go Live' button shows a live indicator (pulsing green dot) when activity is active or paused"
    - "Bracket and poll status badges display a styled 'Paused' badge for paused activities"
  artifacts:
    - path: "src/components/ui/switch.tsx"
      provides: "Radix Switch primitive component"
      contains: "@radix-ui/react-switch"
    - path: "src/components/teacher/live-dashboard.tsx"
      provides: "Pause toggle and amber banner in bracket live dashboard"
      contains: "Switch"
    - path: "src/app/(dashboard)/polls/[pollId]/live/client.tsx"
      provides: "Pause toggle and amber banner in poll live dashboard"
      contains: "Switch"
  key_links:
    - from: "src/components/teacher/live-dashboard.tsx"
      to: "src/actions/bracket.ts"
      via: "updateBracketStatus call with paused/active"
      pattern: "updateBracketStatus.*paused"
    - from: "src/app/(dashboard)/polls/[pollId]/live/client.tsx"
      to: "src/actions/poll.ts"
      via: "updatePollStatus call with paused/active"
      pattern: "updatePollStatus.*paused"
    - from: "src/components/bracket/bracket-card.tsx"
      to: "Go Live"
      via: "Label rename and always-visible button"
      pattern: "Go Live"
---

<objective>
Add teacher-facing pause/resume controls to both bracket and poll live dashboards, rename all "View Live" buttons to "Go Live" with visual state indicators, and update status badges to handle the paused state.

Purpose: Teachers need instant, visible control over activity voting state. The toggle switch provides the fastest possible interaction (no confirmation dialog), the amber banner makes paused state impossible to miss, and the Go Live rename with state indicators gives teachers clarity about what clicking the button does.
Output: Switch UI component, pause toggle + banner in both dashboards, Go Live buttons with state indicators, paused status badges.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-pause-resume-go-live/29-RESEARCH.md
@.planning/phases/29-pause-resume-go-live/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix Switch, create Switch UI component, add pause toggle + banner to both live dashboards</name>
  <files>
    package.json
    src/components/ui/switch.tsx
    src/components/teacher/live-dashboard.tsx
    src/app/(dashboard)/polls/[pollId]/live/client.tsx
  </files>
  <action>
    **Install dependency:**
    Run `npm install @radix-ui/react-switch` -- this is a prior v2.0 decision. Follows existing Radix pattern (dialog, dropdown-menu, label already in project).

    **Create Switch UI component:**
    Create `src/components/ui/switch.tsx` following the shadcn/ui pattern used by existing Radix components in the project (dialog.tsx, dropdown-menu.tsx, label.tsx):
    - 'use client' directive
    - Import `@radix-ui/react-switch` as SwitchPrimitive
    - forwardRef wrapping SwitchPrimitive.Root with cn() className merging
    - Thumb child element with translate animation on state change
    - Color coding: `data-[state=checked]:bg-green-600` (active = green), `data-[state=unchecked]:bg-amber-500` (paused = amber)
    - Standard focus ring, disabled state, and transition styling
    - Export named `Switch`

    **Bracket live dashboard -- pause toggle + banner:**
    In `src/components/teacher/live-dashboard.tsx`:
    - Import `Switch` from `@/components/ui/switch`
    - Add `isPaused` state initialized from `bracket.status === 'paused'`
    - Add `handlePauseToggle(checked: boolean)` handler that calls `updateBracketStatus({ bracketId, status: checked ? 'active' : 'paused' })` wrapped in `startTransition`. On success, update isPaused state. On error, set error state.
    - No confirmation dialog per user decision -- instant toggle.
    - Place the toggle in the top bar/header area, next to existing controls:
      ```
      <div className="flex items-center gap-2">
        <Switch checked={!isPaused} onCheckedChange={handlePauseToggle} disabled={isPending} />
        <span className="text-xs font-medium">{isPaused ? 'Paused' : 'Active'}</span>
      </div>
      ```
    - Only show the toggle when bracket status is 'active' or 'paused' (not for draft or completed -- pause is a mid-activity tool per user decision).
    - Add amber banner below the header when paused:
      ```
      {isPaused && (
        <div className="rounded-lg bg-amber-100 px-4 py-2 text-center text-sm font-medium text-amber-800 dark:bg-amber-900/30 dark:text-amber-300">
          Activity Paused -- Students cannot vote
        </div>
      )}
      ```
    - Sync isPaused with bracket.status changes from realtime updates (if the bracket object is reactive, use useEffect to sync).

    **Poll live dashboard -- pause toggle + banner:**
    In `src/app/(dashboard)/polls/[pollId]/live/client.tsx`:
    - Same pattern as bracket dashboard above
    - Import Switch, add isPaused state from `poll.status === 'paused'`
    - Handler calls `updatePollStatus({ pollId, status: checked ? 'active' : 'paused' })`
    - Toggle placed in the top bar near existing controls, only shown when poll is 'active' or 'paused'
    - Same amber banner when paused
    - Same realtime sync behavior
  </action>
  <verify>
    Run `npm install @radix-ui/react-switch` and confirm it installs. Run `npx tsc --noEmit` and confirm no type errors. Verify Switch component file exists with `cat src/components/ui/switch.tsx`. Grep for 'isPaused' in live-dashboard.tsx and the poll live client to confirm toggle integration.
  </verify>
  <done>
    @radix-ui/react-switch installed. Switch UI primitive created following project patterns. Both bracket and poll live dashboards have a pause/resume toggle switch in the header area with an amber "Activity Paused" banner. Toggle is instant with no confirmation dialog.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename "View Live" to "Go Live" with state indicators and update status badges</name>
  <files>
    src/components/bracket/bracket-card.tsx
    src/components/bracket/bracket-detail.tsx
    src/components/poll/poll-detail-view.tsx
    src/components/bracket/bracket-status.tsx
    src/components/poll/poll-status.tsx
  </files>
  <action>
    **"Go Live" rename + always visible + state indicator (LIVE-04):**

    There are 3 files with "View Live" text to change. In each file:

    1. `src/components/bracket/bracket-card.tsx` (line ~258):
       - Change "View Live" text to "Go Live"
       - Remove the `bracket.status === 'active'` conditional that gates visibility -- the button must ALWAYS be visible per user decision
       - Add visual state indicator: when bracket is `active` or `paused`, show a pulsing green dot and use green background styling. When bracket is `draft` or `completed`, use muted/neutral appearance (border, muted text). Implementation:
         ```
         <Link
           href={`/brackets/${bracket.id}/live`}
           className={cn(
             'inline-flex items-center gap-1.5 rounded-md px-3 py-1.5 text-xs font-medium transition-colors',
             bracket.status === 'active' || bracket.status === 'paused'
               ? 'bg-green-600 text-white shadow-sm hover:bg-green-700'
               : 'border text-muted-foreground hover:bg-accent hover:text-foreground'
           )}
         >
           {(bracket.status === 'active' || bracket.status === 'paused') && (
             <span className="relative flex h-2 w-2">
               <span className="absolute inline-flex h-full w-full animate-ping rounded-full bg-green-300 opacity-75" />
               <span className="relative inline-flex h-2 w-2 rounded-full bg-green-400" />
             </span>
           )}
           Go Live
         </Link>
         ```

    2. `src/components/bracket/bracket-detail.tsx` (line ~149): Same changes -- "View Live" to "Go Live", remove active-only conditional, add state indicator with same pattern.

    3. `src/components/poll/poll-detail-view.tsx` (line ~187): Same changes for polls -- "View Live" to "Go Live", remove active-only conditional, add state indicator. Use `poll.status` instead of `bracket.status`. The href pattern will point to the poll live page.

    **Status badge updates for 'paused':**

    4. `src/components/bracket/bracket-status.tsx`: Find the `statusStyles` or equivalent mapping object that provides badge colors per status. Add a `paused` entry with amber/yellow styling to match the pause theme: e.g., `paused: 'bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300'`. Add 'Paused' as the display label for this status.

    5. `src/components/poll/poll-status.tsx`: Same -- add `paused` entry to the poll status badge styles with amber/yellow theme and 'Paused' display label.
  </action>
  <verify>
    Grep for "View Live" across the entire src/ directory -- should return zero results. Grep for "Go Live" -- should return results in bracket-card.tsx, bracket-detail.tsx, and poll-detail-view.tsx. Grep for "paused" in bracket-status.tsx and poll-status.tsx to confirm badge styles exist. Run `npx tsc --noEmit` for type safety.
  </verify>
  <done>
    All "View Live" labels changed to "Go Live" across 3 files. Go Live button is always visible regardless of activity state. Active/paused activities show a pulsing green dot indicator. Draft/completed activities show muted button appearance. Bracket and poll status badges display styled "Paused" badge with amber theme.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `grep -r "View Live" src/` -- should return zero results
3. `grep -r "Go Live" src/` -- should return results in 3 files
4. `grep "Switch" src/components/teacher/live-dashboard.tsx` -- confirms toggle integration
5. `grep "Switch" src/app/\(dashboard\)/polls/*/live/client.tsx` -- confirms poll toggle
6. `grep "isPaused" src/components/teacher/live-dashboard.tsx` -- confirms state management
7. `grep "paused" src/components/bracket/bracket-status.tsx` -- confirms badge style
8. `grep "paused" src/components/poll/poll-status.tsx` -- confirms badge style
9. `cat src/components/ui/switch.tsx` -- confirms Radix Switch component exists
10. `cat package.json | grep "react-switch"` -- confirms dependency installed
</verification>

<success_criteria>
- @radix-ui/react-switch is installed and Switch UI component created
- Bracket live dashboard has pause/resume toggle switch + amber banner
- Poll live dashboard has pause/resume toggle switch + amber banner
- All "View Live" labels replaced with "Go Live" (zero "View Live" remaining)
- Go Live buttons always visible with green pulsing dot for active/paused states
- Bracket and poll status badges handle paused state with amber styling
</success_criteria>

<output>
After completion, create `.planning/phases/29-pause-resume-go-live/29-02-SUMMARY.md`
</output>
