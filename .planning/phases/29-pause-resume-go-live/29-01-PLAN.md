---
phase: 29-pause-resume-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bracket/types.ts
  - src/lib/poll/types.ts
  - src/lib/utils/validation.ts
  - src/lib/dal/bracket.ts
  - src/lib/dal/poll.ts
  - src/lib/realtime/broadcast.ts
  - src/actions/bracket.ts
  - src/actions/poll.ts
  - src/actions/vote.ts
  - src/hooks/use-realtime-bracket.ts
  - src/hooks/use-realtime-poll.ts
  - src/app/api/sessions/[sessionId]/activities/route.ts
  - src/app/(dashboard)/brackets/[bracketId]/live/page.tsx
autonomous: true

must_haves:
  truths:
    - "TypeScript compiles with 'paused' as a valid BracketStatus and PollStatus"
    - "Server accepts status transition from active to paused and from paused to active for both brackets and polls"
    - "A vote submitted against a paused bracket is rejected with 'Voting is paused' error"
    - "A vote submitted against a paused poll is rejected with 'Voting is paused' error"
    - "Paused brackets and polls remain visible in the student activity grid"
    - "Realtime hooks refetch state when bracket_paused, bracket_resumed, poll_paused, or poll_resumed events arrive"
    - "Teacher can access the live dashboard page for a paused bracket or poll"
  artifacts:
    - path: "src/lib/bracket/types.ts"
      provides: "BracketStatus with 'paused' value"
      contains: "'paused'"
    - path: "src/lib/poll/types.ts"
      provides: "PollStatus with 'paused' value"
      contains: "'paused'"
    - path: "src/lib/realtime/broadcast.ts"
      provides: "bracket_paused, bracket_resumed, poll_paused, poll_resumed event types"
      contains: "bracket_paused"
  key_links:
    - from: "src/actions/vote.ts"
      to: "bracket.status === 'paused'"
      via: "bracket-level status check in castVote"
      pattern: "bracket.*status.*paused"
    - from: "src/actions/poll.ts"
      to: "poll.status === 'paused'"
      via: "paused guard in castPollVote"
      pattern: "status.*paused"
    - from: "src/actions/bracket.ts"
      to: "src/lib/realtime/broadcast.ts"
      via: "broadcastBracketUpdate with bracket_paused/bracket_resumed"
      pattern: "bracket_paused|bracket_resumed"
---

<objective>
Add 'paused' as a valid status for brackets and polls across the entire backend stack: types, Zod validation, DAL transition maps, broadcast event types, server-side vote guards, realtime hook event handling, session API filters, and live page access guards.

Purpose: This is the foundation layer that every other plan in Phase 29 depends on. Without these backend changes, the teacher toggle and student overlay have nothing to wire into.
Output: All type, validation, DAL, action, hook, and API changes needed to support pause/resume lifecycle.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-pause-resume-go-live/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paused status to types, validation, and DAL transition maps</name>
  <files>
    src/lib/bracket/types.ts
    src/lib/poll/types.ts
    src/lib/utils/validation.ts
    src/lib/dal/bracket.ts
    src/lib/dal/poll.ts
  </files>
  <action>
    **Types (do FIRST -- everything else depends on these):**

    1. `src/lib/bracket/types.ts` line 6: Change `BracketStatus` from `'draft' | 'active' | 'completed'` to `'draft' | 'active' | 'paused' | 'completed'`

    2. `src/lib/poll/types.ts` line 5: Change `PollStatus` from `'draft' | 'active' | 'closed' | 'archived'` to `'draft' | 'active' | 'paused' | 'closed' | 'archived'`

    **Zod validation schemas:**

    3. `src/lib/utils/validation.ts` line 89: Change `updateBracketStatusSchema` status enum from `z.enum(['draft', 'active', 'completed'])` to `z.enum(['draft', 'active', 'paused', 'completed'])`

    4. `src/lib/utils/validation.ts` line 223: Change `updatePollStatusSchema` status enum from `z.enum(['draft', 'active', 'closed', 'archived'])` to `z.enum(['draft', 'active', 'paused', 'closed', 'archived'])`

    **DAL transition maps:**

    5. `src/lib/dal/bracket.ts`: Find the `VALID_TRANSITIONS` Record. Add `'paused'` to the `active` array, and add a new `paused: ['active', 'completed', 'archived']` entry. Result:
    ```
    draft: ['active', 'completed', 'archived'],
    active: ['paused', 'completed', 'archived'],
    paused: ['active', 'completed', 'archived'],
    completed: ['archived'],
    archived: [],
    ```

    6. `src/lib/dal/poll.ts`: Find the `VALID_POLL_TRANSITIONS` Record. Add `'paused'` to the `active` array, and add a new `paused: ['active', 'closed', 'archived']` entry. Result:
    ```
    draft: ['active', 'archived'],
    active: ['paused', 'closed', 'archived'],
    paused: ['active', 'closed', 'archived'],
    closed: ['archived', 'draft'],
    archived: [],
    ```
  </action>
  <verify>Run `npx tsc --noEmit` and confirm no type errors related to BracketStatus or PollStatus. Grep for VALID_TRANSITIONS and VALID_POLL_TRANSITIONS to confirm paused entries exist.</verify>
  <done>BracketStatus and PollStatus types include 'paused'. Zod schemas accept 'paused'. DAL transition maps allow active->paused and paused->active transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add broadcast events, vote guards, realtime hooks, API filters, and live page guards</name>
  <files>
    src/lib/realtime/broadcast.ts
    src/actions/bracket.ts
    src/actions/poll.ts
    src/actions/vote.ts
    src/hooks/use-realtime-bracket.ts
    src/hooks/use-realtime-poll.ts
    src/app/api/sessions/[sessionId]/activities/route.ts
    src/app/(dashboard)/brackets/[bracketId]/live/page.tsx
  </files>
  <action>
    **Broadcast event types:**

    1. `src/lib/realtime/broadcast.ts`: Add `'bracket_paused'` and `'bracket_resumed'` to the `BracketUpdateType` union (after `'results_prepared'`). Add `'poll_paused'` and `'poll_resumed'` to the `PollUpdateType` union (after `'poll_archived'`).

    **Broadcast calls in server actions:**

    2. `src/actions/bracket.ts`: In the `updateBracketStatus` action, after the status update succeeds, add broadcast logic:
       - If the new status is `'paused'`: call `broadcastBracketUpdate(bracketId, 'bracket_paused', {})` and `broadcastActivityUpdate(sessionId)` (sessionId from the bracket)
       - If the new status is `'active'` AND the old status was `'paused'` (i.e., resuming): call `broadcastBracketUpdate(bracketId, 'bracket_resumed', {})` and `broadcastActivityUpdate(sessionId)`
       - The existing broadcast calls for other transitions should remain unchanged

    3. `src/actions/poll.ts`: In the `updatePollStatus` action, after the status update succeeds:
       - If new status is `'paused'`: call `broadcastPollUpdate(pollId, 'poll_paused')` and `broadcastActivityUpdate(sessionId)`
       - If new status is `'active'` AND old status was `'paused'`: call `broadcastPollUpdate(pollId, 'poll_resumed')` and `broadcastActivityUpdate(sessionId)`

    **Server-side vote guards (CRITICAL for CTRL-06):**

    4. `src/actions/vote.ts`: In the `castVote` function, the matchup query currently selects `bracket: { select: { sessionId: true } }`. Extend this to also select `status: true` from the bracket: `bracket: { select: { sessionId: true, status: true } }`. Then, BEFORE the existing `matchup.status !== 'voting'` check, add:
       ```
       if (matchup.bracket.status === 'paused') {
         return { error: 'Voting is paused by your teacher' }
       }
       ```

    5. `src/actions/poll.ts`: In the `castPollVote` function, BEFORE the existing `poll.status !== 'active'` check, add a specific paused check:
       ```
       if (poll.status === 'paused') {
         return { error: 'Voting is paused by your teacher' }
       }
       ```
       This gives students a clear message instead of the generic "Poll is not active".

    **Realtime hook event handling:**

    6. `src/hooks/use-realtime-bracket.ts`: Find the bracket_update event handler that checks event types (winner_selected, round_advanced, voting_opened, bracket_completed, etc.). Add `'bracket_paused'` and `'bracket_resumed'` to the list of types that trigger `fetchBracketState()`.

    7. `src/hooks/use-realtime-poll.ts`: Find the poll_update event handler. Add `'poll_paused'` and `'poll_resumed'` to the list of types that trigger a refetch.

    **Session activities API filter:**

    8. `src/app/api/sessions/[sessionId]/activities/route.ts`: Find the bracket status filter `status: { in: ['active', 'completed'] }` and add `'paused'` to it: `['active', 'paused', 'completed']`. Find the poll status filter `status: { in: ['active', 'closed'] }` and add `'paused'`: `['active', 'paused', 'closed']`.

    **Live page access guard:**

    9. `src/app/(dashboard)/brackets/[bracketId]/live/page.tsx`: Find the status redirect guard (currently redirects if status is 'draft'). Ensure `paused` status is allowed through -- the teacher must access the live dashboard to toggle resume. If the guard checks for specific allowed statuses, add 'paused' to the allowed list. If it only blocks 'draft', no change needed, but verify.
  </action>
  <verify>
    Run `npx tsc --noEmit` and confirm no type errors. Grep for 'bracket_paused' in broadcast.ts, actions/bracket.ts, and use-realtime-bracket.ts to confirm wiring. Grep for 'poll_paused' in broadcast.ts, actions/poll.ts, and use-realtime-poll.ts. Grep for 'Voting is paused' in vote.ts and poll.ts.
  </verify>
  <done>
    Bracket and poll pause/resume broadcast events fire on status transitions. Students cannot vote on paused brackets or polls (server-side enforcement). Realtime hooks refetch on pause/resume events. Paused activities appear in student activity grid. Teacher can access live dashboard for paused brackets/polls.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. Confirm BracketStatus includes 'paused': `grep -n "paused" src/lib/bracket/types.ts`
3. Confirm PollStatus includes 'paused': `grep -n "paused" src/lib/poll/types.ts`
4. Confirm Zod schemas accept 'paused': `grep -n "paused" src/lib/utils/validation.ts`
5. Confirm DAL transitions include paused: `grep -A1 "paused" src/lib/dal/bracket.ts src/lib/dal/poll.ts`
6. Confirm broadcast types include paused events: `grep "paused\|resumed" src/lib/realtime/broadcast.ts`
7. Confirm vote guards exist: `grep "paused" src/actions/vote.ts src/actions/poll.ts`
8. Confirm realtime hooks handle paused events: `grep "paused\|resumed" src/hooks/use-realtime-bracket.ts src/hooks/use-realtime-poll.ts`
9. Confirm session API includes paused: `grep "paused" src/app/api/sessions/*/activities/route.ts`
</verification>

<success_criteria>
- TypeScript compiles cleanly with paused status across all layers
- Both bracket and poll DAL transition maps support active<->paused transitions
- Server actions reject votes on paused activities with clear error messages
- Broadcast events propagate pause/resume state changes to connected clients
- Session activities API returns paused activities to students
- Live dashboard pages are accessible for paused activities
</success_criteria>

<output>
After completion, create `.planning/phases/29-pause-resume-go-live/29-01-SUMMARY.md`
</output>
