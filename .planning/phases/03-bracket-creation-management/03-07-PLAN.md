---
phase: 03-bracket-creation-management
plan: 07
type: execute
wave: 4
depends_on: ["03-04", "03-05"]
files_modified:
  - src/app/(dashboard)/brackets/page.tsx
  - src/app/(dashboard)/brackets/[bracketId]/page.tsx
  - src/app/(dashboard)/brackets/[bracketId]/edit/page.tsx
  - src/components/bracket/bracket-card.tsx
  - src/components/bracket/bracket-status.tsx
  - src/components/bracket/bracket-detail.tsx
  - src/components/dashboard/sidebar-nav.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher can see a list of all their brackets at /brackets with name, status, size, and creation date"
    - "Teacher can click a bracket to see its detail page with the visual SVG tournament diagram"
    - "Teacher can change bracket status from draft to active or completed via status controls"
    - "Teacher can edit entrants of a draft bracket from the edit page"
    - "Teacher can delete a bracket with confirmation dialog"
    - "Sidebar navigation includes a 'Brackets' link that highlights when active"
    - "Dashboard shows all brackets with their current status at a glance (MGMT-08)"
  artifacts:
    - path: "src/app/(dashboard)/brackets/page.tsx"
      provides: "Bracket list page (server component)"
      min_lines: 20
    - path: "src/app/(dashboard)/brackets/[bracketId]/page.tsx"
      provides: "Bracket detail page (server component)"
      min_lines: 15
    - path: "src/app/(dashboard)/brackets/[bracketId]/edit/page.tsx"
      provides: "Bracket edit page (server component shell)"
      min_lines: 15
    - path: "src/components/bracket/bracket-card.tsx"
      provides: "Bracket summary card for list view"
      exports: ["BracketCard"]
      min_lines: 30
    - path: "src/components/bracket/bracket-status.tsx"
      provides: "Status badge and lifecycle controls"
      exports: ["BracketStatusBadge", "BracketLifecycleControls"]
      min_lines: 40
    - path: "src/components/bracket/bracket-detail.tsx"
      provides: "Bracket detail client component with diagram and controls"
      exports: ["BracketDetail"]
      min_lines: 50
    - path: "src/components/dashboard/sidebar-nav.tsx"
      provides: "Updated sidebar with Brackets link"
  key_links:
    - from: "src/app/(dashboard)/brackets/page.tsx"
      to: "src/lib/dal/bracket.ts"
      via: "getTeacherBrackets DAL call"
      pattern: "getTeacherBrackets"
    - from: "src/app/(dashboard)/brackets/[bracketId]/page.tsx"
      to: "src/lib/dal/bracket.ts"
      via: "getBracketWithDetails DAL call"
      pattern: "getBracketWithDetails"
    - from: "src/components/bracket/bracket-detail.tsx"
      to: "src/components/bracket/bracket-diagram.tsx"
      via: "BracketDiagram component for visual rendering"
      pattern: "import.*BracketDiagram"
    - from: "src/components/bracket/bracket-status.tsx"
      to: "src/actions/bracket.ts"
      via: "updateBracketStatus and deleteBracket server actions"
      pattern: "import.*(updateBracketStatus|deleteBracket)"
---

<objective>
Build the bracket list page, detail page, edit page, lifecycle controls, and update the sidebar navigation. This completes the bracket viewing and management experience.

Purpose: Teachers need to see their brackets at a glance (MGMT-08), view bracket details with the SVG diagram (BRKT-07), manage bracket lifecycle (BRKT-09), edit draft bracket entrants (BRKT-10), and delete brackets (BRKT-11). The sidebar needs a "Brackets" link for navigation.
Output: Complete bracket management pages (/brackets, /brackets/[id], /brackets/[id]/edit) with lifecycle controls and sidebar integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bracket-creation-management/03-RESEARCH.md
@src/lib/dal/bracket.ts
@src/actions/bracket.ts
@src/lib/dal/auth.ts
@src/components/bracket/bracket-diagram.tsx
@src/components/bracket/entrant-list.tsx
@src/components/dashboard/sidebar-nav.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bracket list page, bracket card, and sidebar navigation update</name>
  <files>src/app/(dashboard)/brackets/page.tsx, src/components/bracket/bracket-card.tsx, src/components/dashboard/sidebar-nav.tsx</files>
  <action>
  **Create src/app/(dashboard)/brackets/page.tsx (Server Component):**
  - Import `getAuthenticatedTeacher` from `@/lib/dal/auth`
  - Import `getTeacherBrackets` from `@/lib/dal/bracket`
  - Authenticate teacher, redirect to /login if not authenticated
  - Fetch all teacher brackets via `getTeacherBrackets(teacher.id)`
  - Render page layout:
    - Header row: "Brackets" title + "Create Bracket" button (Link to /brackets/new) with Plus icon
    - Grid of BracketCard components (3 cols on lg, 2 on md, 1 on sm)
    - Empty state if no brackets: "No brackets yet" message with "Create your first bracket" CTA button
  - Serialize bracket data for client components (convert Date to ISO string)

  **Create src/components/bracket/bracket-card.tsx ('use client'):**

  Props:
  ```typescript
  interface BracketCardProps {
    bracket: {
      id: string
      name: string
      description: string | null
      size: number
      status: string
      createdAt: string
      _count?: { entrants: number }
    }
  }
  ```

  Features:
  - Card component using shadcn Card (or div with card styling)
  - Clickable (Link to `/brackets/${bracket.id}`)
  - Display:
    - Bracket name (title, truncated if long)
    - Status badge (BracketStatusBadge component): color-coded (draft=gray, active=green, completed=blue)
    - Size indicator: "{size} entrants" with Trophy icon
    - Description (truncated to 2 lines, if present)
    - Created date (formatted: "Jan 29, 2026" or relative "2 days ago")
    - Entrant count if available from _count
  - Hover state with subtle shadow elevation

  **Update src/components/dashboard/sidebar-nav.tsx:**
  - Add a new nav item to the `navItems` array:
    ```typescript
    { label: 'Brackets', href: '/brackets', icon: Trophy }
    ```
  - Import `Trophy` from lucide-react (add to existing import)
  - Place it after "Sessions" in the nav order: Dashboard, Sessions, Brackets
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Navigate to /brackets -- list page renders. Sidebar shows "Brackets" link.</verify>
  <done>Bracket list page at /brackets shows all teacher brackets as cards with name, status, size, and date. Empty state shown when no brackets exist. Sidebar navigation includes "Brackets" link with Trophy icon.</done>
</task>

<task type="auto">
  <name>Task 2: Create bracket detail page, status controls, edit page, and detail component</name>
  <files>src/app/(dashboard)/brackets/[bracketId]/page.tsx, src/app/(dashboard)/brackets/[bracketId]/edit/page.tsx, src/components/bracket/bracket-detail.tsx, src/components/bracket/bracket-status.tsx</files>
  <action>
  **Create src/app/(dashboard)/brackets/[bracketId]/page.tsx (Server Component):**
  - Import `getAuthenticatedTeacher` from `@/lib/dal/auth`
  - Import `getBracketWithDetails` from `@/lib/dal/bracket`
  - Authenticate teacher, redirect to /login if not authenticated
  - Fetch bracket with details: `getBracketWithDetails(params.bracketId, teacher.id)`
  - If bracket is null, redirect to /brackets (not found or not owned)
  - Serialize bracket data (convert Dates to ISO strings)
  - Render BracketDetail client component with bracket data as props
  - Calculate totalRounds from bracket.size using `Math.log2(bracket.size)`

  **Create src/components/bracket/bracket-detail.tsx ('use client'):**

  Props: `{ bracket: BracketWithDetails }` (from types.ts, with totalRounds added)

  Layout:
  - Header: bracket name + status badge (BracketStatusBadge)
  - Description (if present)
  - Action bar:
    - Edit button (Link to `/brackets/${bracket.id}/edit`) -- only if status === 'draft'
    - BracketLifecycleControls component for status transitions + delete
  - Entrant list section: numbered list of entrants by seed position
  - SVG Bracket Diagram section: renders BracketDiagram with bracket's matchups and totalRounds
  - The diagram should be the visual centerpiece -- large and prominent

  **Create src/components/bracket/bracket-status.tsx ('use client'):**

  Export two components:

  1. **BracketStatusBadge** -- a small badge showing bracket status with color:
     - draft: gray/muted badge
     - active: green/success badge
     - completed: blue/info badge
     - Use shadcn Badge component or styled span

  2. **BracketLifecycleControls** -- action buttons based on current status:
     Props: `{ bracketId: string, status: string, bracketName: string }`

     - If draft: "Activate" button (green, calls updateBracketStatus with 'active')
     - If active: "Complete" button (blue, calls updateBracketStatus with 'completed')
     - If completed: no status change buttons (terminal state)
     - Delete button (red/destructive, always shown): opens confirmation dialog
       - Confirmation: "Are you sure you want to delete '{bracketName}'? This cannot be undone."
       - On confirm: calls deleteBracket server action, redirects to /brackets on success
     - Use useTransition for non-blocking action calls (show spinner during action)
     - Use AlertDialog from shadcn/ui (or simple confirm) for delete confirmation
     - On status update success: page revalidates automatically via revalidatePath in server action
     - On error: display error message via toast or inline

  **Create src/app/(dashboard)/brackets/[bracketId]/edit/page.tsx (Server Component):**
  - Authenticate teacher
  - Fetch bracket with details
  - If bracket is null or bracket.status !== 'draft': redirect to `/brackets/${bracketId}` (can't edit non-draft)
  - Render an edit form that:
    - Shows current bracket name and description (editable)
    - Shows current entrants in an EntrantList (from Plan 06) with reorder/edit/remove enabled
    - Allows adding new entrants (manual entry input + Add button)
    - Shows entrant counter: "X / {size} entrants"
    - "Save Changes" button: calls updateBracketEntrants server action with the modified entrants
    - "Cancel" link back to bracket detail page
  - This page reuses the EntrantList component from Plan 06
  - Create a client component wrapper (BracketEditForm) for the interactive form
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Navigate to a bracket detail page -- diagram renders with matchup data. Status controls appear based on bracket status.</verify>
  <done>Bracket detail page shows SVG diagram, entrant list, and lifecycle controls. Status badge is color-coded. Lifecycle controls allow status transitions with confirmation. Edit page allows modifying entrants for draft brackets. Delete shows confirmation dialog. All actions call server actions with loading states.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no type errors
2. /brackets lists all teacher brackets with status badges
3. /brackets/[id] shows bracket detail with SVG diagram
4. /brackets/[id]/edit allows entrant editing for draft brackets
5. Status controls: draft can activate or complete; active can complete; completed has no status buttons
6. Delete shows confirmation dialog and redirects to /brackets on success
7. Sidebar shows "Brackets" link with Trophy icon, active state on bracket pages
8. Empty state on /brackets when no brackets exist
</verification>

<success_criteria>
- Bracket list page shows all brackets with status, size, and date
- Bracket detail page renders SVG tournament diagram as visual centerpiece
- Lifecycle controls enforce valid status transitions (no backwards)
- Edit page only accessible for draft brackets (server-side redirect)
- Delete has confirmation dialog and cascades via server action
- Sidebar navigation includes Brackets link
- All pages follow SC+CC split pattern (SC fetches, CC handles interactivity)
</success_criteria>

<output>
After completion, create `.planning/phases/03-bracket-creation-management/03-07-SUMMARY.md`
</output>
