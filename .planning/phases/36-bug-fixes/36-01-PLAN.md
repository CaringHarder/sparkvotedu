---
phase: 36-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dal/poll.ts
  - src/actions/poll.ts
  - src/app/(dashboard)/polls/new/page.tsx
  - src/components/poll/poll-form.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher duplicates a poll, removes options, saves -- only remaining options persist (no ghost options)"
    - "Poll Quick Create includes an 'Assign to session (optional)' dropdown matching bracket Quick Create layout"
  artifacts:
    - path: "src/lib/dal/poll.ts"
      provides: "updatePollOptionsDAL deletes removed options via deleteMany NOT IN"
      contains: "notIn"
    - path: "src/app/(dashboard)/polls/new/page.tsx"
      provides: "Server component fetching active sessions for poll creation"
      contains: "classSession"
    - path: "src/components/poll/poll-form.tsx"
      provides: "Session dropdown in Quick Create mode"
      contains: "No session (assign later)"
  key_links:
    - from: "src/components/poll/poll-form.tsx"
      to: "src/actions/poll.ts"
      via: "createPoll action receives sessionId"
      pattern: "sessionId"
    - from: "src/lib/dal/poll.ts"
      to: "prisma.pollOption.deleteMany"
      via: "sync pattern deletes options not in provided list"
      pattern: "deleteMany.*notIn"
---

<objective>
Fix duplicate poll ghost options (FIX-01) and add session selector to Poll Quick Create (FIX-04).

Purpose: FIX-01 fixes a data integrity bug where removing options from a duplicated poll leaves ghost options in the database. FIX-04 adds feature parity with bracket Quick Create by including an "Assign to session" dropdown.
Output: Poll option sync works correctly on edit, and Poll Quick Create has a session dropdown matching bracket Quick Create layout exactly.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-bug-fixes/36-RESEARCH.md
@.planning/phases/34-poll-quick-create-image-polish/34-01-SUMMARY.md
@src/lib/dal/poll.ts
@src/actions/poll.ts
@src/components/poll/poll-form.tsx
@src/app/(dashboard)/polls/new/page.tsx
@src/components/bracket/bracket-quick-create.tsx
@src/app/(dashboard)/brackets/new/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ghost options by adding delete sync to updatePollOptionsDAL</name>
  <files>src/lib/dal/poll.ts, src/actions/poll.ts</files>
  <action>
In `src/lib/dal/poll.ts`, modify `updatePollOptionsDAL` to delete options that are no longer in the provided list. Inside the existing `$transaction`:
1. Before updating options, add a `tx.pollOption.deleteMany` call that removes any PollOption where `pollId` matches AND `id` is `notIn` the array of option IDs being submitted
2. The delete must happen FIRST in the transaction (before position updates) to avoid `@@unique([pollId, position])` constraint violations
3. Only delete when the options array has existing IDs (not new options being created) -- filter to only IDs that exist

In `src/actions/poll.ts`, verify the `updatePollOptions` server action correctly passes option IDs through to the DAL function. The form data from `poll-form.tsx` already sends option IDs -- ensure they are forwarded to `updatePollOptionsDAL` so the NOT IN filter works correctly.

No animation or fade-out for removed options -- the form already handles client-side removal instantly via the `removeOption` handler in `option-list.tsx`.
  </action>
  <verify>
1. Read `src/lib/dal/poll.ts` and confirm `deleteMany` with `notIn` filter exists inside `updatePollOptionsDAL`
2. Run `npx tsc --noEmit` to confirm no type errors
3. Trace the data flow: poll-form.tsx submit -> updatePollOptions action -> updatePollOptionsDAL -> deleteMany + updates in transaction
  </verify>
  <done>updatePollOptionsDAL deletes options not in the provided list before updating remaining options, all within a single Prisma transaction. Ghost options from duplicated polls are removed on save.</done>
</task>

<task type="auto">
  <name>Task 2: Add session dropdown to Poll Quick Create matching bracket pattern</name>
  <files>src/app/(dashboard)/polls/new/page.tsx, src/components/poll/poll-form.tsx, src/actions/poll.ts</files>
  <action>
**Step 1: Convert polls/new/page.tsx to server component**
The page currently uses `"use client"`. Convert it to a server component that fetches active sessions, following the exact pattern from `src/app/(dashboard)/brackets/new/page.tsx`:
- Import `prisma` and `getAuthenticatedTeacher`
- Fetch active sessions: `prisma.classSession.findMany({ where: { teacherId: teacher.id, status: 'active' }, select: { id, code, name, createdAt }, orderBy: { createdAt: 'desc' } })`
- Serialize dates (convert `createdAt` to ISO string)
- Pass `sessions` prop to the client component
- Extract any existing client-side state (tab toggle) into a child client component if needed. The page already renders `PollForm` and `PollWizard` -- wrap them in a client component (e.g., `PollCreationPage` or inline) that receives `sessions`

**Step 2: Add session dropdown to PollForm in Quick Create mode**
In `src/components/poll/poll-form.tsx`:
- Add `sessions` prop to PollForm (typed as `{ id: string; code: string; name: string | null; createdAt: string }[]`, default to `[]`)
- Add `selectedSessionId` state (initially `null`)
- When `mode === 'quick'`, render a session dropdown matching bracket-quick-create.tsx EXACTLY:
  ```
  <div className="space-y-2">
    <h2 className="text-sm font-medium text-muted-foreground">
      Assign to session (optional)
    </h2>
    <select
      value={selectedSessionId ?? ''}
      onChange={(e) => setSelectedSessionId(e.target.value || null)}
      className="w-full rounded-md border bg-background px-2 py-1.5 text-sm"
    >
      <option value="">No session (assign later)</option>
      {sessions.map(...)}
    </select>
  </div>
  ```
- On submit in Quick Create mode, pass `selectedSessionId` to the create action

**Step 3: Handle sessionId in createPoll action**
In `src/actions/poll.ts`, modify `createPoll` to accept an optional `sessionId` parameter. After creating the poll, if `sessionId` is provided, call `assignPollToSession` (or directly update the poll's sessionId) to link the poll to the session. Follow the same pattern as bracket creation.
  </action>
  <verify>
1. Read `src/app/(dashboard)/polls/new/page.tsx` and confirm it fetches sessions as a server component
2. Read `src/components/poll/poll-form.tsx` and confirm session dropdown renders in Quick Create mode
3. Run `npx tsc --noEmit` to confirm no type errors
4. Verify the dropdown label says "Assign to session (optional)" and default is "No session (assign later)"
  </verify>
  <done>Poll Quick Create shows a session dropdown identical to bracket Quick Create: same label, same dropdown component, same "No session (assign later)" default. Selected session is passed through to createPoll action.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Ghost option deletion: updatePollOptionsDAL contains deleteMany with notIn filter inside transaction
3. Session dropdown: polls/new/page.tsx is a server component fetching sessions, PollForm renders dropdown in quick mode
4. Session assignment: createPoll action handles optional sessionId parameter
</verification>

<success_criteria>
- Duplicating a poll, removing options, and saving correctly removes the deleted options from the database
- Poll Quick Create includes a session dropdown with "Assign to session (optional)" label and "No session (assign later)" default
- Session dropdown matches bracket Quick Create layout exactly
</success_criteria>

<output>
After completion, create `.planning/phases/36-bug-fixes/36-01-SUMMARY.md`
</output>
