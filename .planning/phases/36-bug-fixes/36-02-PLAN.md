---
phase: 36-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/student/simple-poll-vote.tsx
  - src/app/(student)/session/[sessionId]/poll/[pollId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Student viewing a poll with exactly 2 options sees them as side-by-side larger cards, centered on screen"
    - "On mobile, 2-option poll cards stack vertically"
    - "When Show Live Results toggle is ON, students see vote counts and bars updating in real time"
  artifacts:
    - path: "src/components/student/simple-poll-vote.tsx"
      provides: "2-option centered layout and live results display"
      contains: "options.length === 2"
  key_links:
    - from: "src/components/student/simple-poll-vote.tsx"
      to: "voteCounts prop or context"
      via: "Renders AnimatedBarChart or vote count bars when showLiveResults is true"
      pattern: "showLiveResults"
---

<objective>
Fix 2-option poll centering (FIX-02) and add student-facing live results display (FIX-05).

Purpose: FIX-02 makes polls with exactly 2 options display as large, centered side-by-side cards instead of small grid items. FIX-05 shows students the same live vote results the teacher sees when Show Live Results is toggled ON.
Output: 2-option polls look impactful with centered larger cards; students see real-time vote counts when enabled.
</objective>

<execution_context>
@/Users/davidreynoldsjr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidreynoldsjr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-bug-fixes/36-RESEARCH.md
@src/components/student/simple-poll-vote.tsx
@src/app/(student)/session/[sessionId]/poll/[pollId]/page.tsx
@src/components/poll/bar-chart.tsx
@src/hooks/use-realtime-poll.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 2-option centered layout and student live results display</name>
  <files>src/components/student/simple-poll-vote.tsx, src/app/(student)/session/[sessionId]/poll/[pollId]/page.tsx</files>
  <action>
**FIX-02: 2-option centering in simple-poll-vote.tsx**

Add a conditional layout when `poll.options.length === 2`:
1. Detect 2-option case: `const is2Options = poll.options.length === 2`
2. Replace the grid container classes conditionally:
   - 2 options: `"flex flex-col items-center gap-4 sm:flex-row sm:justify-center sm:gap-6"` (stack on mobile, side-by-side on sm+)
   - 3+ options: keep existing grid classes `"grid grid-cols-2 gap-3 sm:grid-cols-3"`
3. For 2-option cards, make them larger:
   - Add width classes: `is2Options && "w-full sm:w-64 md:w-72"` on each Card
   - Increase minimum height: `is2Options ? "min-h-[120px] sm:min-h-[140px]" : "min-h-[80px]"` on CardContent
4. No "VS" badge or divider between cards -- just a clean gap (the gap-6 handles spacing)
5. On mobile: `flex-col` makes them stack vertically naturally

**FIX-05: Live results display for students**

The student poll page already subscribes to realtime via `useRealtimePoll` and has `voteCounts` available. The fix is to pass vote data down and render results when `showLiveResults` is true.

1. In the student poll page (`src/app/(student)/session/[sessionId]/poll/[pollId]/page.tsx`), check how `voteCounts` from `useRealtimePoll` is being used. It should already be available in the page component. Pass `voteCounts`, `totalVotes`, and `showLiveResults` down to `SimplePollVote` as props.

2. In `simple-poll-vote.tsx`, accept new optional props: `voteCounts?: Record<string, number>`, `totalVotes?: number`, and `showLiveResults?: boolean`.

3. When `showLiveResults === true` and the student has already voted (or always if enabled), render a results section below the voting cards:
   - Show a simple horizontal bar for each option with vote count and percentage
   - Use a simple inline bar visualization (colored div with width percentage) -- similar to what the teacher sees
   - Reuse the existing color/styling patterns from the component
   - Results update in real time as `voteCounts` changes (already handled by React re-rendering from prop changes)

4. If `showLiveResults` is false or not provided, show nothing (existing behavior: "Results will be shown when the poll closes" or hide entirely).

5. The student page component already has `useRealtimePoll` returning voteCounts -- verify it passes these through. If `showLiveResults` is true on the poll object, pass the voteCounts to the voting component.
  </action>
  <verify>
1. Read `src/components/student/simple-poll-vote.tsx` and confirm:
   - `is2Options` conditional exists
   - 2-option layout uses flex with larger cards
   - Live results section renders when `showLiveResults === true`
2. Run `npx tsc --noEmit` to confirm no type errors
3. Verify no "VS" badge or divider exists between 2-option cards
  </verify>
  <done>Polls with 2 options display as large centered side-by-side cards (stacking vertically on mobile). When Show Live Results is ON, students see vote counts and bars updating in real time -- same data the teacher sees.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. 2-option layout: simple-poll-vote.tsx uses flex layout for 2 options with larger card sizing
3. Live results: voteCounts rendered below voting cards when showLiveResults is true
4. No VS badge or divider between 2-option cards
5. Mobile: 2-option cards stack vertically via flex-col
</verification>

<success_criteria>
- Student viewing a poll with exactly 2 options sees them as side-by-side larger cards, centered on screen
- On mobile (narrow screens), 2-option cards stack vertically
- When Show Live Results is ON, students see the same results display the teacher dashboard shows (vote counts + bars) updating in real time
</success_criteria>

<output>
After completion, create `.planning/phases/36-bug-fixes/36-02-SUMMARY.md`
</output>
